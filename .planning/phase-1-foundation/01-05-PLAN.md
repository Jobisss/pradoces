---
phase: 01-foundation
plan: 05
type: execute
gap_closure: true
wave: 2
depends_on: []
autonomous: false
files_modified:
  - Dockerfile
  - .dockerignore
  - docker-compose.yml
  - Caddyfile
  - scripts/setup-ufw.sh
  - scripts/test-origin-hidden.sh
  - postgres/postgresql.conf
  - .env.production.example
  - docs/DEPLOY.md
requirements: [INFRA-01, INFRA-02, INFRA-03]
user_setup:
  - service: cloudflare
    why: "Proxy laranja esconde o IP da VPS (INFRA-02) e termina TLS; SSL Full (Strict)"
    dashboard_config:
      - task: "Criar A record docesvalentina.com.br + www -> <VPS_IP>, ambos Proxied (nuvem laranja)"
        location: "Cloudflare Dashboard -> DNS"
      - task: "SSL/TLS mode = Full (Strict)"
        location: "Cloudflare Dashboard -> SSL/TLS -> Overview"
  - service: vps
    why: "UFW + Caddy rodam no host (não em container)"
    env_vars:
      - name: DEV_IP
        source: "IP público do dev (para liberar SSH na porta 22)"
      - name: POSTGRES_PASSWORD
        source: "Senha forte gerada para o Postgres de produção (.env.production, fora do Git)"
threat_model:
  - id: T-01-05-01
    severity: high
    category: Information Disclosure
    note: "IP de origem da VPS exposto permite bypass do Cloudflare e DDoS direto"
    mitigation: "UFW permite 80/443 só dos CIDRs Cloudflare (v4+v6); Cloudflare A records Proxied; scripts/test-origin-hidden.sh prova que curl direto ao IP é refused"
  - id: T-01-05-02
    severity: high
    category: Elevation of Privilege
    note: "SSH aberto ao mundo -> brute force"
    mitigation: "UFW default deny incoming; porta 22 só do DEV_IP"
  - id: T-01-05-03
    severity: medium
    category: Tampering
    note: "X-Forwarded-For spoofado se Caddy confiar em qualquer proxy"
    mitigation: "Caddy trusted_proxies cloudflare (plugin caddy-trusted-cloudflare); só CF é proxy confiável"
  - id: T-01-05-04
    severity: medium
    category: Information Disclosure
    note: "MitM entre Cloudflare e a origin"
    mitigation: "Cloudflare Full (Strict) + Caddy auto-LE (cert válido na origin)"

must_haves:
  truths:
    - "docker compose sobe app (Next standalone) + Postgres 16 com TZ America/Sao_Paulo"
    - "App escuta apenas em 127.0.0.1:3000 (loopback) — exposto ao mundo só via Caddy"
    - "UFW bloqueia tudo exceto 80/443 (IPs Cloudflare) e SSH (DEV_IP)"
    - "Caddy faz reverse_proxy para 127.0.0.1:3000 com trusted_proxies cloudflare e HSTS"
    - "curl direto ao IP cru da VPS é recusado (origin escondida)"
  artifacts:
    - path: "Dockerfile"
      provides: "Build multi-stage Next standalone"
      contains: "node server.js"
    - path: "docker-compose.yml"
      provides: "Stack app + db com healthcheck"
      contains: "postgres:16-alpine"
    - path: "Caddyfile"
      provides: "Reverse proxy host-level"
      contains: "reverse_proxy 127.0.0.1:3000"
    - path: "scripts/setup-ufw.sh"
      provides: "Firewall ruleset"
      contains: "cloudflare.com/ips-v4"
  key_links:
    - from: "Caddyfile"
      to: "app container (127.0.0.1:3000)"
      via: "reverse_proxy"
      pattern: "reverse_proxy 127.0.0.1:3000"
    - from: "docker-compose.yml app"
      to: "docker-compose.yml db"
      via: "depends_on service_healthy"
      pattern: "service_healthy"
---

<objective>
Entregar a infra de rede/VPS defensável que está totalmente ausente: Docker Compose (Next standalone + Postgres 16 + Caddy host), allowlist UFW só para Cloudflare, e config Cloudflare documentada com proxy laranja. Fecha INFRA-01, INFRA-02, INFRA-03.

Purpose: Sem isso o sistema só roda local/DEV; PROD não sobe e o IP da origin fica exposto a DDoS direto.
Output: Dockerfile + docker-compose.yml + Caddyfile + scripts UFW + postgres.conf + .env.production.example + docs/DEPLOY.md.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-1-foundation/01-RESEARCH.md
@.planning/phase-1-foundation/01-PATTERNS.md

<interfaces>
next.config.ts (Plan 01) — já tem `output: 'standalone'` -> o Dockerfile copia .next/standalone + .next/static + public.
prisma.config.ts + prisma/migrations/* (Plan 02) — PROD roda `prisma migrate deploy` (não db push).
@node-rs/argon2 — precisa de prebuilt musl no Alpine; schema.prisma já tem binaryTargets ["native","linux-musl-openssl-3.0.x"].
Datas: TZ=America/Sao_Paulo (lib/env.ts literal) + Postgres timezone (INFRA-10).
</interfaces>

Verbatim/refs: RESEARCH §Cloudflare Proxy + UFW + Caddy (lines 1692-1832): DNS records, UFW ruleset (1707-1734), Caddyfile (1747-1772), test origin (1774-1780), Docker Compose (1784-1811), next.config (já feito). PATTERNS §2.26 (Dockerfile 3-stage), §2.27 (compose), §2.28 (Caddyfile), §2.29 (setup-ufw).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile multi-stage + .dockerignore + docker-compose.yml + postgres.conf</name>
  <files>Dockerfile, .dockerignore, docker-compose.yml, postgres/postgresql.conf, .env.production.example</files>
  <read_first>
    - .planning/phase-1-foundation/01-PATTERNS.md §2.26, §2.27 — estrutura 3-stage + compose
    - .planning/phase-1-foundation/01-RESEARCH.md §Cloudflare/Caddy Docker Compose (lines 1784-1811)
    - next.config.ts — confirmar output:'standalone'
    - node_modules/next/dist/docs/01-app/02-guides/self-hosting.md — convenção standalone (server.js)
    - prisma/schema.prisma — binaryTargets musl
  </read_first>
  <action>
    Criar `Dockerfile` multi-stage (node:20-alpine): stage `deps` (`npm ci`), stage `builder` (copia deps + `npm run build` com output standalone + `npx prisma generate`), stage `runner` (copia .next/standalone, .next/static, public, prisma/, node_modules/.prisma; `USER node`; `EXPOSE 3000`; `CMD ["node","server.js"]`). Garantir prebuilt musl do @node-rs/argon2.
    Criar `.dockerignore` (node_modules, .next, .git, .planning, .env*, tests).
    Criar `docker-compose.yml` per RESEARCH 1784-1811: service `app` (build ., restart unless-stopped, `ports: ["127.0.0.1:3000:3000"]`, env_file .env.production, depends_on db com `condition: service_healthy`); service `db` (postgres:16-alpine, healthcheck `pg_isready`, env POSTGRES_PASSWORD/POSTGRES_DB=doces/TZ=America/Sao_Paulo/PGTZ=America/Sao_Paulo, volume pgdata + monta postgres/postgresql.conf). NÃO expor a porta do Postgres ao host.
    Criar `postgres/postgresql.conf` com `timezone = 'America/Sao_Paulo'` + `log_timezone = 'America/Sao_Paulo'` (INFRA-10).
    Criar `.env.production.example` (template, sem segredos reais): DATABASE_URL (aponta para service db), POSTGRES_PASSWORD, NODE_ENV=production, TZ, BETTER_AUTH_SECRET, BETTER_AUTH_URL=https://docesvalentina.com.br, NEXT_PUBLIC_URL, RESEND_API_KEY, RESEND_WEBHOOK_SECRET, ADMIN_EMAIL.
  </action>
  <acceptance_criteria>
    - `grep -c 'node server.js' Dockerfile` >= 1
    - `grep -c 'service_healthy' docker-compose.yml` == 1
    - `grep -c '127.0.0.1:3000:3000' docker-compose.yml` == 1
    - `grep -c 'postgres:16-alpine' docker-compose.yml` == 1
    - `grep -c "America/Sao_Paulo" postgres/postgresql.conf` >= 1
    - `docker compose config` (valida o YAML) exit 0
  </acceptance_criteria>
  <verify>
    <automated>docker compose config >/dev/null && echo COMPOSE_OK</automated>
  </verify>
  <done>Compose válido; app só em loopback; Postgres com TZ; template de env de produção pronto.</done>
</task>

<task type="auto">
  <name>Task 2: Caddyfile + scripts UFW + test-origin-hidden + docs/DEPLOY.md</name>
  <files>Caddyfile, scripts/setup-ufw.sh, scripts/test-origin-hidden.sh, docs/DEPLOY.md</files>
  <read_first>
    - .planning/phase-1-foundation/01-RESEARCH.md §Cloudflare/Caddy (lines 1707-1780) — UFW ruleset + Caddyfile + origin test verbatim
    - .planning/phase-1-foundation/01-PATTERNS.md §2.28, §2.29
    - proxy.ts (Plan 03) — confirma que CSP/X-Frame já vêm do app; Caddy NÃO duplica (Pitfall #10)
  </read_first>
  <action>
    Criar `Caddyfile` per RESEARCH 1747-1772: bloco global `email dpo@docesvalentina.com.br`; site `docesvalentina.com.br, www.docesvalentina.com.br` com `encode gzip zstd`, `servers { trusted_proxies cloudflare }`, header `Strict-Transport-Security "max-age=31536000; includeSubDomains"`, `reverse_proxy 127.0.0.1:3000`. NÃO setar CSP/X-Frame-Options aqui (proxy.ts é dono — Pitfall #10). Comentar a dependência do plugin caddy-trusted-cloudflare.
    Criar `scripts/setup-ufw.sh` per RESEARCH 1707-1734: `set -e`; `ufw --force reset`; default deny incoming / allow outgoing; `ufw allow from "${DEV_IP:?DEV_IP required}" to any port 22 proto tcp`; loops sobre `curl -fsS https://www.cloudflare.com/ips-v4` e `ips-v6` liberando 80/443; `ufw --force enable`; `ufw status numbered`.
    Criar `scripts/test-origin-hidden.sh`: `curl -k --max-time 5 https://<VPS_IP_RAW>/ -H 'Host: docesvalentina.com.br'` esperando connection refused/timeout (documentar exit esperado != 0). Aceitar VPS_IP por env var.
    Criar `docs/DEPLOY.md`: passo-a-passo (Cloudflare DNS Proxied + SSL Full-Strict; instalar Caddy host + plugin CF; rodar setup-ufw.sh com DEV_IP; cron.weekly refresh CF IPs; `docker compose up -d`; `docker compose exec app npx prisma migrate deploy`; `docker compose exec app npm run seed:admin`). Incluir o checklist de INFRA-02/03 e o comando test-origin-hidden.
  </action>
  <acceptance_criteria>
    - `grep -c 'reverse_proxy 127.0.0.1:3000' Caddyfile` == 1
    - `grep -c 'trusted_proxies cloudflare' Caddyfile` == 1
    - `grep -c 'Content-Security-Policy' Caddyfile` == 0 (não duplica o proxy.ts)
    - `grep -c 'cloudflare.com/ips-v4' scripts/setup-ufw.sh` == 1
    - `grep -c 'port 22' scripts/setup-ufw.sh` == 1
    - `bash -n scripts/setup-ufw.sh && bash -n scripts/test-origin-hidden.sh` exit 0 (sintaxe shell válida)
    - `grep -c 'migrate deploy' docs/DEPLOY.md` >= 1
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/setup-ufw.sh && bash -n scripts/test-origin-hidden.sh && echo SHELL_OK</automated>
  </verify>
  <done>Caddy reverse-proxy com trusted CF + HSTS (sem duplicar CSP); UFW só CF+SSH; runbook de deploy completo.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Dockerfile, docker-compose, Caddyfile, UFW e runbook prontos. Falta a config que só existe fora do código.</what-built>
  <how-to-verify>
    Seguir docs/DEPLOY.md na VPS:
    1. Cloudflare DNS: criar A records docesvalentina.com.br + www -> IP da VPS, ambos Proxied (nuvem laranja). SSL/TLS = Full (Strict).
    2. Na VPS: `export DEV_IP=<seu_ip>` e `sudo bash scripts/setup-ufw.sh`; conferir `ufw status numbered` mostra 22 (DEV_IP) + 80/443 (CF v4/v6).
    3. `docker compose up -d` + `docker compose exec app npx prisma migrate deploy`.
    4. Rodar `bash scripts/test-origin-hidden.sh` (com VPS_IP) — DEVE retornar connection refused/timeout (origin escondida = INFRA-02 OK).
    5. Abrir https://docesvalentina.com.br — responde via Cloudflare; conferir header HSTS presente e CSP vindo do app.
  </how-to-verify>
  <resume-signal>Digite "deploy ok" + cole a saída de `ufw status numbered` e do test-origin-hidden, ou descreva o que falhou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet -> Cloudflare -> Caddy -> app | Único caminho de entrada permitido (80/443 só CF) |
| Dev -> SSH (22) | Só do DEV_IP |
| Caddy -> app | Loopback 127.0.0.1:3000 (não exposto) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Information Disclosure | UFW + Cloudflare DNS | mitigate | UFW 80/443 só CIDRs CF; A records Proxied; test-origin-hidden prova refused (block-on:high) |
| T-01-05-02 | Elevation of Privilege | UFW SSH | mitigate | default deny; 22 só DEV_IP (block-on:high) |
| T-01-05-03 | Tampering | Caddyfile | mitigate | trusted_proxies cloudflare |
| T-01-05-04 | Information Disclosure | Cloudflare + Caddy TLS | mitigate | Full (Strict) + Caddy auto-LE |
</threat_model>

<verification>
- `docker compose config` valida o stack; `bash -n` valida os scripts.
- Checkpoint humano confirma INFRA-02 (origin escondida) e INFRA-03 (UFW) na VPS real.
- grep gates confirmam loopback-only, trusted CF e ausência de CSP duplicado.
</verification>

<success_criteria>
- INFRA-01: stack Docker Compose (Next standalone + Postgres 16 + Caddy) sobe.
- INFRA-02: IP de origem escondido atrás do Cloudflare proxy (test-origin-hidden refused).
- INFRA-03: UFW só 80/443 (CF) + SSH (dev).
</success_criteria>

<output>
Após completar, criar `.planning/phase-1-foundation/01-05-SUMMARY.md` usando @E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md.
</output>
