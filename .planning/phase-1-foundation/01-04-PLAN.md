---
phase: 01-foundation
plan: 04
type: execute
gap_closure: true
wave: 2
depends_on: [03]
autonomous: true
files_modified:
  - package.json
  - lib/email/resend.ts
  - lib/email/send-verification.tsx
  - lib/email/send-password-reset.tsx
  - lib/audit/log.ts
  - app/api/webhooks/resend/route.ts
  - lib/auth/server.ts
  - tests/auth/email-verify.test.ts
  - tests/audit/audit-service.test.ts
  - tests/webhooks/resend-svix.test.ts
requirements: [AUTH-04, AUTH-05, AUTH-11]
user_setup:
  - service: resend
    why: "Entrega real de email de verificação e reset (troca os NOOPs do Plan 03)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys (começa com re_) — já validado em lib/env.ts"
      - name: RESEND_WEBHOOK_SECRET
        source: "Resend Dashboard -> Webhooks -> Signing secret (começa com whsec_) — já validado em lib/env.ts"
    dashboard_config:
      - task: "Verificar domínio docesvalentina.com.br (SPF/DKIM/DMARC) para o from nao-responda@"
        location: "Resend Dashboard -> Domains"
      - task: "Criar webhook apontando para https://docesvalentina.com.br/api/webhooks/resend"
        location: "Resend Dashboard -> Webhooks"
threat_model:
  - id: T-01-04-01
    severity: high
    category: Spoofing
    note: "Webhook svix sem verificação de assinatura aceita payload forjado"
    mitigation: "svix Webhook(env.RESEND_WEBHOOK_SECRET).verify(rawBody, headers) sobre request.text() cru; assinatura inválida -> 400 antes de qualquer processamento"
  - id: T-01-04-02
    severity: medium
    category: Information Disclosure
    note: "Email/URL de reset vazando em logs"
    mitigation: "pino redactPaths (Plan 01) já cobre email/url/token; o envio usa void (não loga corpo); webhook loga só event_type + email_id"
  - id: T-01-04-03
    severity: medium
    category: Repudiation
    note: "Reset de senha sem trilha de auditoria"
    mitigation: "onPasswordReset grava AuditLog action=customer_password_reset (AUTH-11)"
  - id: T-01-04-04
    severity: low
    category: Denial of Service
    note: "Envio síncrono de email bloqueando a Server Action"
    mitigation: "callbacks usam void sendXxx(...) (não-bloqueante); Phase 4 move para pg-boss"

must_haves:
  truths:
    - "Cadastro dispara um email de verificação real via Resend (não mais NOOP)"
    - "Pedido de reset dispara um email de reset real via Resend"
    - "O link de verificação de email expira em 24h (emailVerification.expiresIn = 60*60*24) — a promessa de '24 horas' no copy é cumprida (AUTH-04)"
    - "Webhook do Resend rejeita payload sem assinatura svix válida (400)"
    - "Reset de senha bem-sucedido grava um evento customer_password_reset em audit_log"
    - "logAudit() persiste linhas em audit_log com IP/UA hasheados (nunca plaintext)"
  artifacts:
    - path: "lib/email/resend.ts"
      provides: "Resend client singleton"
      contains: "new Resend"
    - path: "lib/email/send-verification.tsx"
      provides: "React Email template + sendVerificationEmail()"
      exports: ["sendVerificationEmail"]
    - path: "lib/email/send-password-reset.tsx"
      provides: "React Email template + sendPasswordResetEmail()"
      exports: ["sendPasswordResetEmail"]
    - path: "lib/audit/log.ts"
      provides: "logAudit() write wrapper com hashing de IP/UA"
      exports: ["logAudit"]
    - path: "app/api/webhooks/resend/route.ts"
      provides: "Webhook receiver com svix verify"
      exports: ["POST"]
  key_links:
    - from: "lib/auth/server.ts"
      to: "lib/email/send-verification.tsx + lib/email/send-password-reset.tsx"
      via: "sendVerificationEmail / sendResetPassword callbacks"
      pattern: "sendVerificationEmail|sendPasswordResetEmail"
    - from: "lib/auth/server.ts onPasswordReset"
      to: "lib/audit/log.ts"
      via: "logAudit({ action: 'customer_password_reset' })"
      pattern: "logAudit"
    - from: "app/api/webhooks/resend/route.ts"
      to: "svix"
      via: "new Webhook(secret).verify"
      pattern: "Webhook\\("
---

<objective>
Trocar os NOOPs de email do Plan 03 por entrega real via Resend + React Email, criar o serviço de audit log (`logAudit`) que toda a fase consome, e montar o webhook do Resend com verificação svix. Fecha a parte de **entrega de email** (AUTH-04/AUTH-05) e a **fundação do audit log** (AUTH-11).

Purpose: Sem isso ninguém recebe link de confirmação/reset (cadastro fica inutilizável de ponta a ponta) e nenhum evento é registrado em `audit_log`.
Output: Resend client + 2 templates React Email, `lib/audit/log.ts`, webhook svix, e `lib/auth/server.ts` religado para envio real + audit no reset + janela de verificação de 24h (AUTH-04).
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-1-foundation/01-RESEARCH.md
@.planning/phase-1-foundation/01-PATTERNS.md
@.planning/phase-1-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Already on disk — executor uses directly, no exploration needed -->

lib/auth/server.ts (Plan 03) — current NOOP callbacks to REPLACE:
  emailAndPassword.sendResetPassword: async ({ user, url }) => { logger.info(...) }  // NOOP
  emailAndPassword.onPasswordReset:   async ({ user }) => { logger.info(...) }       // log only
  emailVerification.sendVerificationEmail: async ({ user, url }) => { logger.info(...) } // NOOP
  emailVerification atualmente NÃO seta expiresIn (Better Auth usa default de 1h) -> Task 3 adiciona 24h.
  user.additionalFields already declares role/telefone/isAdult/terms*/privacy* — DO NOT change.

lib/env.ts (Plan 01) — already exposes:
  env.RESEND_API_KEY (startsWith re_), env.RESEND_WEBHOOK_SECRET (startsWith whsec_), env.NEXT_PUBLIC_URL

prisma/schema.prisma (Plan 02) — AuditLog model:
  id BigInt autoincrement; actorType String; actorId String? @db.Uuid; action String;
  entityType String?; entityId String?; metadata Json?; ipHash String?; uaHash String?; ts Timestamptz
  -> actorId MUST be a valid UUID or null (column is @db.Uuid). For CLI/system use null.

tests/conftest.ts (Plan 02/03) — fixtures available: createTestUser(), truncateAll(), signInAsCustomer(), generateResetToken()
</interfaces>

Locked stack: Resend 6.12.2 + @react-email/components 1.0.12 + @react-email/render 2.0.8 + svix 1.92.2 (RESEARCH §Standard Stack). Verbatim code: RESEARCH §Email Infra (lines ~1460-1526), §Audit Log (lines ~1386-1453), §Better Auth init (lines ~954-1019).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar deps de email + lib/email (resend client + 2 templates React Email)</name>
  <files>package.json, lib/email/resend.ts, lib/email/send-verification.tsx, lib/email/send-password-reset.tsx</files>
  <read_first>
    - .planning/phase-1-foundation/01-RESEARCH.md §Email Infra (lines 1460-1497) — código verbatim do client + template send-verification
    - .planning/phase-1-foundation/01-PATTERNS.md §2.9, §2.10, §2.11 — copy literal pt-BR dos 2 templates (subjects, headings, bodies, from address)
    - lib/env.ts — confirmar env.RESEND_API_KEY / env.NEXT_PUBLIC_URL
    - node_modules/@react-email/render — confirmar assinatura de render() (Next 16 / React 19)
  </read_first>
  <action>
    Rodar `rtk npm install resend@6.12.2 @react-email/components@1.0.12 @react-email/render@2.0.8 svix@1.92.2`.
    Criar `lib/email/resend.ts`: `export const resend = new Resend(env.RESEND_API_KEY)` (importa env de '@/lib/env').
    Criar `lib/email/send-verification.tsx` exportando `async function sendVerificationEmail({ to, url }: { to: string; url: string })` que renderiza o template `VerifyEmail` via `render()` e chama `resend.emails.send({ from: 'Doces Valentina <nao-responda@docesvalentina.com.br>', to, subject: 'Confirma seu email — Doces Valentina', html })`. Copy literal do template (PATTERNS §2.10): heading "Confirma seu email pra terminar o cadastro", body "A gente só quer ter certeza que esse email é seu mesmo.", button "Confirmar email", footer "O link vale por 24 horas. Se já passou, é só fazer cadastro de novo."
    Criar `lib/email/send-password-reset.tsx` exportando `async function sendPasswordResetEmail({ to, url }: { to: string; url: string })`, mesmo esqueleto, copy PATTERNS §2.11: subject "Recuperar sua senha — Doces Valentina", heading "Pediu pra trocar a senha?", body "Clica no botão abaixo pra escolher uma nova. Se não foi você, pode ignorar — sua senha continua a mesma.", button "Criar senha nova", footer "O link vale por 1 hora."
    NÃO mover o `from` para placeholder/v1 — usar o endereço real do domínio.
  </action>
  <acceptance_criteria>
    - `grep -q "resend@" package.json` e `grep -q "svix" package.json` retornam 0
    - `grep -c "export async function sendVerificationEmail" lib/email/send-verification.tsx` == 1
    - `grep -c "export async function sendPasswordResetEmail" lib/email/send-password-reset.tsx` == 1
    - `grep -q "nao-responda@docesvalentina.com.br" lib/email/send-verification.tsx` retorna 0 (from real)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Deps instaladas; 3 arquivos lib/email/* existem, type-check passa, copy literal pt-BR presente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/audit/log.ts (serviço de audit com hash de IP/UA) + webhook Resend svix</name>
  <files>lib/audit/log.ts, app/api/webhooks/resend/route.ts, tests/audit/audit-service.test.ts, tests/webhooks/resend-svix.test.ts</files>
  <read_first>
    - .planning/phase-1-foundation/01-PATTERNS.md §2.12 — assinatura completa de logAudit + sha256 helper + lista de actions Phase 1
    - .planning/phase-1-foundation/01-RESEARCH.md §Email Infra webhook (lines 1499-1526) — handler svix verbatim
    - prisma/schema.prisma — model AuditLog (actorId @db.Uuid -> null para cli/system)
    - tests/conftest.ts — createTestUser/truncateAll
  </read_first>
  <behavior>
    - logAudit({ actorType:'customer', actorId:<uuid>, action:'customer_signup', rawIp:'1.2.3.4', rawUa:'x' }) cria 1 linha; ipHash != '1.2.3.4' (é sha256 hex, 64 chars); uaHash != 'x'
    - logAudit com actorId:null e actorType:'cli' persiste (coluna nullable) sem erro
    - POST /api/webhooks/resend com headers svix ausentes/inválidos -> 400 "invalid signature"
    - POST /api/webhooks/resend com payload corretamente assinado (svix Webhook(secret).sign em teste) -> 200 { ok:true }
  </behavior>
  <action>
    Criar `lib/audit/log.ts` com `export async function logAudit(input)` per PATTERNS §2.12: aceita actorType/actorId/action/entityType?/entityId?/metadata?/ipHash?/uaHash?/rawIp?/rawUa?; hasheia rawIp/rawUa via `crypto.createHash('sha256').update(s).digest('hex')` quando ipHash/uaHash não fornecidos; chama `prisma.auditLog.create`. NUNCA persistir IP/UA plaintext (T-01-04-02 / Pitfall #9).
    Criar `app/api/webhooks/resend/route.ts` per RESEARCH lines 1502-1523: `POST` lê `await request.text()` (corpo cru), monta headers svix-id/svix-timestamp/svix-signature, `new Webhook(env.RESEND_WEBHOOK_SECRET).verify(payload, headers)`, em sucesso `logger.info({ event_type, email_id }, 'resend webhook')` + `Response.json({ ok:true })`; em falha `new Response('invalid signature', { status: 400 })`. Phase 1 NÃO persiste email_events (defer Phase 4).
    Criar os 2 testes Vitest descritos em <behavior>. Para o webhook, usar `new Webhook(secret).sign(msgId, timestamp, payload)` da svix para gerar assinatura válida no caso 200.
  </action>
  <acceptance_criteria>
    - `grep -c "export async function logAudit" lib/audit/log.ts` == 1
    - `grep -c "createHash('sha256')" lib/audit/log.ts` >= 1
    - `grep -c "request.text()" app/api/webhooks/resend/route.ts` == 1
    - `grep -c "new Webhook" app/api/webhooks/resend/route.ts` == 1
    - `npx vitest run tests/audit/audit-service.test.ts tests/webhooks/resend-svix.test.ts` PASS
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/audit/audit-service.test.ts tests/webhooks/resend-svix.test.ts --reporter=dot</automated>
  </verify>
  <done>logAudit persiste com hash; webhook rejeita assinatura inválida (400) e aceita válida (200); testes verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Religar lib/auth/server.ts (envio real + audit no reset + verificação 24h) + teste de verificação de email</name>
  <files>lib/auth/server.ts, tests/auth/email-verify.test.ts</files>
  <read_first>
    - lib/auth/server.ts — callbacks NOOP atuais (sendResetPassword, onPasswordReset, sendVerificationEmail) e o bloco emailVerification (hoje sem expiresIn)
    - .planning/phase-1-foundation/01-RESEARCH.md §Better Auth init (lines 954-1019) — forma final dos callbacks
    - lib/email/send-verification.tsx + lib/email/send-password-reset.tsx (Task 1)
    - lib/audit/log.ts (Task 2)
    - node_modules/better-auth — confirmar que emailVerification aceita expiresIn (segundos)
  </read_first>
  <behavior>
    - sendVerificationEmail callback chama sendVerificationEmail({ to: user.email, url }) (via void, não-bloqueante)
    - sendResetPassword callback chama sendPasswordResetEmail({ to: user.email, url }) (via void)
    - onPasswordReset chama logAudit({ actorType: user.role==='admin'?'admin':'customer', actorId: user.id, action:'customer_password_reset' }) — admin reset é via CLI (D-07), web reset é customer
    - emailVerification.expiresIn === 60*60*24 (24h) — o copy do email promete "24 horas" (Task 1); default Better Auth de 1h é insuficiente para AUTH-04
    - Teste mocka lib/email/* (vi.mock) e exercita o callback de verificação no signup: signup -> sendVerificationEmail chamado 1x com o email do usuário
  </behavior>
  <action>
    Editar `lib/auth/server.ts`: importar `{ sendVerificationEmail }` de '@/lib/email/send-verification', `{ sendPasswordResetEmail }` de '@/lib/email/send-password-reset', `{ logAudit }` de '@/lib/audit/log'.
    Substituir o corpo NOOP de `emailVerification.sendVerificationEmail` por `void sendVerificationEmail({ to: user.email, url })`.
    Adicionar `expiresIn: 60 * 60 * 24` ao bloco `emailVerification` (com comentário `// 24h — AUTH-04: copy promete 24 horas; default Better Auth é 1h`). Esta é a ÚNICA adição permitida fora dos callbacks.
    Substituir o corpo NOOP de `emailAndPassword.sendResetPassword` por `void sendPasswordResetEmail({ to: user.email, url })`.
    Substituir `onPasswordReset` por chamada `await logAudit({ actorType: user.role === 'admin' ? 'admin' : 'customer', actorId: user.id, action: 'customer_password_reset' })`.
    NÃO alterar additionalFields, plugins, generateId:false, ou a ordem do nextCookies (último). NÃO mexer em resetPasswordTokenExpiresIn (1h, OWASP). Manter o import do logger se ainda usado.
    Criar `tests/auth/email-verify.test.ts` conforme <behavior> (vi.mock de lib/email/send-verification; usa auth.api.signUpEmail e asserta a chamada do mock).
  </action>
  <acceptance_criteria>
    - `grep -c "noop until Plan 04" lib/auth/server.ts` == 0 (NOOPs removidos)
    - `grep -c "sendVerificationEmail({ to: user.email" lib/auth/server.ts` == 1
    - `grep -c "sendPasswordResetEmail({ to: user.email" lib/auth/server.ts` == 1
    - `grep -c "expiresIn: 60 \* 60 \* 24" lib/auth/server.ts` == 1 (B2 — verificação de 24h)
    - `grep -c "customer_password_reset" lib/auth/server.ts` == 1
    - `npx vitest run tests/auth/email-verify.test.ts` PASS
    - `npx vitest run` (suíte completa) PASS — sem regressão nos 16 testes existentes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --reporter=dot</automated>
  </verify>
  <done>Callbacks de email enviam de verdade; verificação expira em 24h (AUTH-04); onPasswordReset audita; suíte completa verde sem regressão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resend -> /api/webhooks/resend | Payload externo não confiável; só svix-signed é aceito |
| Server Action -> Better Auth callback -> Resend API | Saída para serviço externo (EUA) — operador LGPD |
| App -> audit_log | Escrita append-only; IP/UA só como hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Spoofing | app/api/webhooks/resend/route.ts | mitigate | svix verify sobre request.text() cru; 400 em assinatura inválida (block-on:high) |
| T-01-04-02 | Information Disclosure | lib/audit/log.ts + logs | mitigate | sha256 de IP/UA; pino redact (Plan 01); webhook loga só event_type/email_id |
| T-01-04-03 | Repudiation | lib/auth/server.ts onPasswordReset | mitigate | logAudit action=customer_password_reset |
| T-01-04-04 | Denial of Service | callbacks de email | accept | void (não-bloqueante) em v1; pg-boss em Phase 4 |
</threat_model>

<verification>
- Pré-requisito: `docker start doces-pg` e `.env` com DATABASE_URL/BETTER_AUTH_SECRET/RESEND_* presentes.
- `npx vitest run` verde (>= 19 testes; sem regressão dos 16 anteriores).
- `npx tsc --noEmit` exit 0.
- `grep` gates da Task 2/3 confirmam svix verify, hash de IP/UA, expiresIn 24h e remoção dos NOOPs.
</verification>

<success_criteria>
- AUTH-04: cadastro dispara email real via Resend E o link de verificação vale 24h (emailVerification.expiresIn) — copy e backend consistentes.
- AUTH-05: reset dispara email real via Resend (NOOP eliminado).
- AUTH-11 (fundação): `logAudit` persiste em audit_log com hash; reset de senha gera evento.
- Webhook Resend valida assinatura svix (rejeita forjado).
</success_criteria>

<output>
Após completar, criar `.planning/phase-1-foundation/01-04-SUMMARY.md` usando @E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md.
</output>
</content>
</invoke>
