---
phase: 01-foundation
plan: 08
type: execute
gap_closure: true
wave: 5
depends_on: [03, 04, 06, 07]
autonomous: false
files_modified:
  - lib/validation/auth.ts
  - lib/actions/auth.ts
  - app/cadastro/page.tsx
  - app/cadastro/verifique-seu-email/page.tsx
  - app/auth/confirmar-email/[token]/page.tsx
  - tests/auth/signup.test.ts
  - tests/auth/email-first.test.ts
  - tests/auth/anti-enum.test.ts
  - tests/auth/signin.test.ts
  - tests/lgpd/consent.test.ts
  - tests/audit/admin-login.test.ts
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-07, AUTH-08, AUTH-10, AUTH-11, INFRA-04, LGPD-01, LGPD-02]
threat_model:
  - id: T-01-08-01
    severity: high
    category: Information Disclosure
    note: "Enumeração de email em cadastro/login/reset"
    mitigation: "AUTH-07: login e reset retornam mensagem genérica idêntica independentemente de existência; cadastro tem leak CONTROLADO e intencional só na branch AUTH-03 (UI-SPEC) para reduzir duplicatas em clientela 50+"
  - id: T-01-08-02
    severity: high
    category: Denial of Service
    note: "Brute force / flood nas Server Actions de auth"
    mitigation: "INFRA-04 (defesa em profundidade): rateLimitAuth.consume(ip) em signup/signin (10/60s); rateLimitForgotEmail.consume(email) em requestPasswordReset (3/15min); estouro -> 'Muitas tentativas seguidas...'. O limite PRIMÁRIO contra o POST direto no handler catch-all é aplicado no proxy.ts (Plan 06); aqui é a 2ª camada."
  - id: T-01-08-02b
    severity: high
    category: Elevation of Privilege
    note: "Cliente conseguindo papel admin via cadastro"
    mitigation: "signUpEmail força role=customer (Better Auth defaultRole); role=admin só via CLI seed (Plan 09); admin_login audita papel real"
  - id: T-01-08-03
    severity: medium
    category: Tampering
    note: "Cadastro sem validação de +18 ou consentimento (LGPD)"
    mitigation: "LGPD-01: Zod exige isAdult === true (hard block); LGPD-02: grava termsVersion/privacyVersion='v1.0-shell' + termsAcceptedAt/privacyAcceptedAt=now()"
  - id: T-01-08-04
    severity: medium
    category: Spoofing
    note: "CSRF em Server Action de origem maliciosa"
    mitigation: "Next 16 origin check + experimental.serverActions.allowedOrigins (Plan 01); cookie sameSite=lax (Better Auth)"

must_haves:
  truths:
    - "Cliente preenche email+senha+nome+telefone e cria conta (AUTH-01)"
    - "Cadastro é email-first: passo 1 verifica existência antes do passo 2 (AUTH-02)"
    - "Email já existente mostra 'Esse email já tem conta. Talvez você tenha digitado errado...' (AUTH-03)"
    - "Cadastro sem isAdult=true é rejeitado (LGPD-01)"
    - "Cadastro grava termsVersion/privacyVersion + accepted_at (LGPD-02)"
    - "Login e reset retornam mensagem genérica (sem enumeração) (AUTH-07)"
    - "11ª tentativa em /api/auth/* dentro de 60s é bloqueada — defesa em profundidade na action (limite primário no proxy, Plan 06) (INFRA-04)"
    - "Login admin bem-sucedido grava admin_login em audit_log (AUTH-10/AUTH-11)"
    - "Click no link de confirmação verifica o email e grava customer_email_verified (AUTH-04/AUTH-11)"
  artifacts:
    - path: "lib/validation/auth.ts"
      provides: "Zod schemas (signup/signin/forgot/reset) com copy de erro UI-SPEC"
      exports: ["SignupSchema", "SigninSchema", "ForgotSchema", "ResetSchema"]
    - path: "lib/actions/auth.ts"
      provides: "Server Actions de auth"
      exports: ["signupCustomer", "checkEmailExists", "signinUser", "signinAdmin", "requestPasswordReset", "resetPassword"]
    - path: "app/cadastro/page.tsx"
      provides: "Cadastro inteligente 2 passos + AUTH-03"
      contains: "Vamos começar com seu email"
  key_links:
    - from: "lib/actions/auth.ts"
      to: "lib/ratelimit/memory.ts"
      via: "rateLimitAuth.consume / rateLimitForgotEmail.consume"
      pattern: "rateLimit(Auth|ForgotEmail)\\.consume"
    - from: "lib/actions/auth.ts"
      to: "lib/audit/log.ts"
      via: "logAudit (customer_signup, admin_login)"
      pattern: "logAudit"
    - from: "lib/actions/auth.ts"
      to: "lib/auth/server.ts"
      via: "auth.api.signUpEmail / signInEmail / forgetPassword / resetPassword"
      pattern: "auth\\.api\\."
---

<objective>
Construir a camada de Server Actions + validação Zod de auth e as superfícies de cadastro inteligente (email-first, AUTH-03) e confirmação de email. Liga a UI ao Better Auth com rate limit (INFRA-04), anti-enumeração (AUTH-07), captura LGPD (+18/consent) e escrita de audit (customer_signup, admin_login, customer_email_verified).

Purpose: Hoje nenhum usuário consegue de fato cadastrar/logar pela aplicação — só existe o handler de API. Esta é a espinha dorsal funcional do auth de cliente e admin.
Output: lib/validation/auth.ts + lib/actions/auth.ts + páginas de cadastro/confirmação + 6 testes de integração.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-1-foundation/01-UI-SPEC.md
@.planning/phase-1-foundation/01-RESEARCH.md
@.planning/phase-1-foundation/01-PATTERNS.md
@.planning/phase-1-foundation/01-03-SUMMARY.md

<interfaces>
lib/auth/server.ts (Plan 03/04) — auth.api: signUpEmail({ body:{ email, password, name } }), signInEmail({ body:{ email, password } }), forgetPassword({ body:{ email, redirectTo } }), resetPassword({ body:{ token, newPassword } }), verifyEmail({ query:{ token } }). getSession({ headers }). requireEmailVerification=true; revokeSessionsOnPasswordReset=true; verificação de email expira em 24h (Plan 04); reset token 1h.
lib/ratelimit/memory.ts (Plan 06) — rateLimitAuth, rateLimitForgotEmail (.consume(key) rejeita ao estourar). NOTA: o limite PRIMÁRIO contra POST direto no handler /api/auth/[...all] já é aplicado no proxy.ts (Plan 06, Task 3). O consume aqui nas Server Actions é DEFESA EM PROFUNDIDADE (mantido de propósito) — não remover.
lib/audit/log.ts (Plan 04) — logAudit({ actorType, actorId, action, metadata?, rawIp?, rawUa? }).
lib/db/client.ts (Plan 02) — prisma (para checkEmailExists e UPDATE de telefone/LGPD pós-signup).
components/ui/* (Plan 07) — button/input/label/form/checkbox/card/alert.
tests/conftest.ts — createTestUser/truncateAll/signInAsCustomer/signInAsAdmin/generateResetToken.
Next 16: headers async (await nextHeaders()); Server Action pattern em RESEARCH §Architecture Pattern 1 (lines 405-459).

NOTA W3 — copy de erro de login: o literal "Email ou senha não conferem" NÃO consta no UI-SPEC (lacuna). É adotado aqui (e no Plan 10) como copy padrão de credencial inválida: é AUTH-07-appropriado (não-enumerante, idêntico para email inexistente e senha errada). Preencher essa lacuna do UI-SPEC é aceitável; NÃO alterar o UI-SPEC. Manter a string idêntica entre Plan 08 e Plan 10.
</interfaces>

Copy literal: UI-SPEC §Copywriting (cadastro step1/step2, +18, termos, AUTH-03, anti-enum, erros). Fluxos: RESEARCH §Auth Flows F1/F2/F3/F4 (lines 786-858). Schemas: PATTERNS §2.17. Actions: PATTERNS §2.18 (skeleton 7 passos). Páginas: PATTERNS §3.2, §3.6.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/validation/auth.ts + lib/actions/auth.ts (todas as Server Actions) + testes de integração</name>
  <files>lib/validation/auth.ts, lib/actions/auth.ts, tests/auth/signup.test.ts, tests/auth/email-first.test.ts, tests/auth/anti-enum.test.ts, tests/auth/signin.test.ts, tests/lgpd/consent.test.ts, tests/audit/admin-login.test.ts</files>
  <read_first>
    - .planning/phase-1-foundation/01-RESEARCH.md §Architecture Pattern 1 (lines 405-459) — skeleton de Server Action; §Auth Flows F1-F5 (786-887)
    - .planning/phase-1-foundation/01-PATTERNS.md §2.17 (schemas + erros literais), §2.18 (7-step actions)
    - .planning/phase-1-foundation/01-UI-SPEC.md §Copywriting (erros: "Esse email não parece certo...", "A senha precisa ter pelo menos 8 caracteres.", anti-enum, rate limit)
    - lib/auth/server.ts, lib/ratelimit/memory.ts, lib/audit/log.ts
  </read_first>
  <behavior>
    - signupCustomer com isAdult !== true -> erro, NÃO cria user (LGPD-01)
    - signupCustomer válido -> cria User+Account; grava telefone, isAdult=true, termsVersion/privacyVersion='v1.0-shell', terms/privacyAcceptedAt set (LGPD-02); logAudit customer_signup
    - checkEmailExists(email) -> { exists:true } para email cadastrado não-anonimizado; { exists:false } para inexistente OU anonimizado (deletedAt set)
    - signinUser com credenciais erradas -> erro genérico "Email ou senha não conferem" (mesma string p/ email inexistente — AUTH-07; literal compartilhado com Plan 10, ver NOTA W3)
    - signinAdmin de user role=admin com sucesso -> logAudit action='admin_login' (actorId=user.id) (AUTH-10/AUTH-11)
    - requestPasswordReset retorna SEMPRE a mensagem genérica anti-enum, exista ou não o email (AUTH-07)
    - rate limit (defesa em profundidade): a 11ª chamada de signinUser do mesmo IP em 60s retorna o erro de rate limit (INFRA-04)
  </behavior>
  <action>
    Criar `lib/validation/auth.ts` (PATTERNS §2.17): `SignupSchema` (email, senha min 8 com msg "A senha precisa ter pelo menos 8 caracteres.", nome, telefone, isAdult literal true, termsAccepted literal true), `SigninSchema`, `ForgotSchema`, `ResetSchema` (newPassword min 8). Mensagens de erro = literais UI-SPEC.
    Criar `lib/actions/auth.ts` ('use server') com as actions seguindo o skeleton de 7 passos (PATTERNS §2.18): `signupCustomer`, `checkEmailExists`, `signinUser`, `signinAdmin`, `requestPasswordReset`, `resetPassword`. Cada uma: `const h = await nextHeaders()`; deriva ip = x-forwarded-for[0]; `rateLimitAuth.consume(ip).catch(()=>null)` (ou rateLimitForgotEmail por email no forgot) com early-return da copy de rate limit; Zod parse; chamada `auth.api.*` em try/catch com erro genérico (AUTH-07); efeitos colaterais (UPDATE telefone+LGPD no signup; logAudit). signupCustomer roda o UPDATE de telefone/isAdult/terms/privacy via prisma após signUpEmail. signinAdmin grava admin_login. checkEmailExists usa prisma.user.findUnique e trata deletedAt como inexistente (AUTH-02/F1). Erro de credencial inválida em signinUser/signinAdmin = literal "Email ou senha não conferem" (NOTA W3 — mesmo literal do Plan 10).
    NÃO simplificar: implementar TODAS as 6 actions de verdade (sem stub/"v1"). Reusar argon2/Better Auth — não rodar hash manual. NÃO remover o consume do rate limit nas actions (defesa em profundidade; o limite primário fica no proxy.ts/Plan 06).
    Criar os 6 testes de integração descritos em <behavior> (usar test DB + fixtures).
  </action>
  <acceptance_criteria>
    - `grep -E -c "export (async function|const) (signupCustomer|checkEmailExists|signinUser|signinAdmin|requestPasswordReset|resetPassword)" lib/actions/auth.ts` >= 6
    - `grep -c 'isAdult' lib/validation/auth.ts` >= 1
    - `grep -c 'v1.0-shell' lib/actions/auth.ts` >= 1
    - `grep -v '^//' lib/actions/auth.ts | grep -c "rateLimit" ` >= 1
    - `grep -c "Email ou senha não conferem" lib/actions/auth.ts` >= 1 (literal compartilhado com Plan 10)
    - `grep -c "admin_login" lib/actions/auth.ts` == 1
    - `grep -c "customer_signup" lib/actions/auth.ts` == 1
    - `npx vitest run tests/auth/signup.test.ts tests/auth/email-first.test.ts tests/auth/anti-enum.test.ts tests/auth/signin.test.ts tests/lgpd/consent.test.ts tests/audit/admin-login.test.ts` PASS
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/auth tests/lgpd/consent.test.ts tests/audit/admin-login.test.ts --reporter=dot</automated>
  </verify>
  <done>6 Server Actions reais (rate limit defesa-em-profundidade + Zod + Better Auth + audit + LGPD capture); anti-enum no login/reset; 6 testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: páginas cadastro (2 passos + AUTH-03), verifique-seu-email e confirmar-email/[token]</name>
  <files>app/cadastro/page.tsx, app/cadastro/verifique-seu-email/page.tsx, app/auth/confirmar-email/[token]/page.tsx</files>
  <read_first>
    - .planning/phase-1-foundation/01-UI-SPEC.md §Copywriting (cadastro step1/step2 literais, +18, termos, AUTH-03, email confirmation landing) + §Interaction (autocomplete/inputmode, sticky CTA) + §Mobile-First
    - .planning/phase-1-foundation/01-PATTERNS.md §3.2, §3.6
    - .planning/phase-1-foundation/01-RESEARCH.md §Auth Flows F1/F2 (788-835)
    - lib/actions/auth.ts (Task 1) + components/ui/* (Plan 07)
    - node_modules/next/dist/docs — forms.md (useActionState / <form action>), params async em Next 16 (await params)
  </read_first>
  <action>
    Criar `app/cadastro/page.tsx` (cadastro inteligente, 2 passos numa rota): passo 1 só email (heading "Vamos começar com seu email", help literal) -> `checkEmailExists`; se existe -> branch AUTH-03 (copy "Esse email já tem conta. Talvez você tenha digitado errado — quer tentar fazer login?" + botão "Tentar fazer login" -> /entrar + link "Não, é outro email"); se não existe -> passo 2 (heading "Faltam só alguns dados": Senha autocomplete new-password + help, Nome autocomplete name, Telefone autocomplete tel inputmode tel + help, checkbox +18 LGPD-01 literal, checkbox termos LGPD-02 literal com links inline /termos /privacidade) -> `signupCustomer`. CTA "Criar minha conta" sticky no mobile (UI-SPEC). Erros de campo em 14px caption (aria-describedby), não vermelho.
    Criar `app/cadastro/verifique-seu-email/page.tsx`: tela de confirmação genérica pós-signup ("confere seu email pra terminar").
    Criar `app/auth/confirmar-email/[token]/page.tsx` (RSC, `await params`): chama `auth.api.verifyEmail({ query:{ token } })`; sucesso -> "Email confirmado. Bem-vinda(o)!" + CTA "Entrar agora" (/entrar) + logAudit customer_email_verified; token expirado -> "Esse link expirou. Sem problema — pede um novo." + CTA "Reenviar email de confirmação".
  </action>
  <acceptance_criteria>
    - `grep -c 'Vamos começar com seu email' app/cadastro/page.tsx` == 1
    - `grep -c 'Talvez você tenha digitado' app/cadastro/page.tsx` == 1
    - `grep -c 'Tenho 18 anos ou mais' app/cadastro/page.tsx` == 1
    - `grep -c 'autocomplete="new-password"' app/cadastro/page.tsx` >= 1
    - `grep -c 'Email confirmado' "app/auth/confirmar-email/[token]/page.tsx"` == 1
    - `grep -c 'customer_email_verified' "app/auth/confirmar-email/[token]/page.tsx"` == 1
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Cadastro email-first com branch AUTH-03 + LGPD checkboxes; landing de confirmação verifica token e audita; type-check passa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fluxo de cadastro inteligente ponta a ponta + confirmação de email, ligados ao Better Auth com rate limit e captura LGPD.</what-built>
  <how-to-verify>
    `npm run dev` (com doces-pg up e Resend key):
    1. /cadastro: digite um email NOVO, clique Continuar -> aparece passo 2. Preencha tudo, deixe +18 desmarcado -> bloqueia. Marque +18 + termos -> "Criar minha conta" -> redireciona para /cadastro/verifique-seu-email.
    2. Confira no inbox (ou log Resend) o email de confirmação; clique o link -> /auth/confirmar-email/[token] mostra "Email confirmado. Bem-vinda(o)!".
    3. /cadastro com um email JÁ cadastrado no passo 1 -> mostra "Esse email já tem conta. Talvez você tenha digitado errado..." + botões.
    4. Auditoria dos eventos customer_signup e customer_email_verified: como a UI de auditoria (/admin/auditoria) só existe no Plan 10, valide pelo teste automatizado `npx vitest run tests/audit/admin-login.test.ts` (verde) E/OU por inspeção direta no banco — `docker exec doces-pg psql -U postgres -d doces -c "select action, ts from audit_log order by ts desc limit 5;"` deve listar customer_signup e customer_email_verified.
  </how-to-verify>
  <resume-signal>Digite "cadastro ok" ou descreva o que falhou (passo/copy/erro).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form -> Server Action | Entrada não confiável; Zod + rate limit (defesa em profundidade) + origin check |
| Server Action -> Better Auth/Prisma | hash argon2id, sessão DB |
| Link de email -> /auth/confirmar-email | Token Better Auth (hashed, single-use), expira em 24h |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-01 | Information Disclosure | signinUser/requestPasswordReset | mitigate | Mensagem genérica idêntica (AUTH-07); leak só na branch AUTH-03 (intencional, UI-SPEC) |
| T-01-08-02 | Denial of Service | lib/actions/auth.ts | mitigate | rate limit IP/email nas actions = defesa em profundidade; limite primário no proxy.ts (Plan 06) cobre o POST direto (INFRA-04, block-on:high) |
| T-01-08-02b | Elevation of Privilege | signupCustomer | mitigate | role=customer forçado; admin só via CLI (block-on:high) |
| T-01-08-03 | Tampering | SignupSchema | mitigate | isAdult===true hard block; consent versionado |
| T-01-08-04 | Spoofing | Server Actions | mitigate | allowedOrigins + sameSite=lax |
</threat_model>

<verification>
- Pré-requisito: `docker start doces-pg` + `.env` completo (RESEND incluso).
- `npx vitest run` verde (6 novos testes + sem regressão).
- `npx tsc --noEmit` exit 0.
- Checkpoint humano valida o fluxo de cadastro/confirmação (auditoria via teste/DB, não via UI — viewer só no Plan 10).
</verification>

<success_criteria>
- AUTH-01/02/03: cadastro completo, email-first, branch "talvez digitou errado".
- AUTH-07: anti-enumeração em login e reset.
- AUTH-08/10: actions de login cliente e admin; admin_login auditado.
- INFRA-04: rate limit nas actions (defesa em profundidade; limite primário no proxy, Plan 06).
- LGPD-01/02: +18 obrigatório + consentimento versionado capturado.
- AUTH-11: customer_signup, admin_login, customer_email_verified gravados.
</success_criteria>

<output>
Após completar, criar `.planning/phase-1-foundation/01-08-SUMMARY.md` usando @E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md.
</output>
</content>
