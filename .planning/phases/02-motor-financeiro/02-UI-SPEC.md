---
phase: 2
slug: motor-financeiro
status: draft
shadcn_initialized: true
preset: radix-nova (shadcn CLI v3+; Lucide + Geist, baseColor neutral) + Luizinha Confeitaria brand CSS vars (app/globals.css, 2026-07-03)
created: 2026-07-03
---

# Phase 2 — UI Design Contract (Motor Financeiro)

> Visual and interaction contract for Phase 2: admin-only CRUD do motor financeiro (ingredientes, compras, receitas, produtos, lotes, ajustes). Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Scope reminder:** Phase 2 renders ONLY admin surfaces sob `app/(admin)/admin/*`. Nada de vitrine pública (Phase 3), reservas/pontos (Phase 4), relatórios agregados (Phase 7). A única audiência é a mãe, no celular, muitas vezes no meio do mercado ou da produção — mobile-first não é opcional.
>
> **Supersedes note:** o Phase 1 UI-SPEC declarava a paleta provisória "Doces Valentina" (#9D2D7A). O REBRAND Luizinha Confeitaria (2026-07-03, `.planning/BRAND.md`) já foi aplicado nos tokens shadcn de `app/globals.css` — **este spec usa os tokens atuais do código**, que são a fonte de verdade. Contratos estruturais do Phase 1 (spacing 8pt, 4 tamanhos/2 pesos, 44px touch, voz "vizinha") permanecem válidos e são herdados aqui.

---

## Surface Inventory (what Phase 2 actually renders)

| # | Surface | Route (proposed) | REQ-IDs | Focal point (o que o olho acha primeiro) |
|---|---------|------------------|---------|------------------------------------------|
| 1 | Home admin — ações pendentes + atalhos 1-toque | `/admin` (substitui placeholder Phase 1) | discretion (embrião ADM-01), PROD-08 | Os 2 cards de atalho "Fui ao mercado" / "Produzi hoje" |
| 2 | Lista de ingredientes (inclui embalagens) | `/admin/ingredientes` | ING-01, ING-06 | Primeira linha da lista; CTA "Novo ingrediente" no topo |
| 3 | Novo/editar ingrediente (form simples) | `/admin/ingredientes/novo`, `/admin/ingredientes/[id]/editar` | ING-01, ING-06 | Campo "Nome" |
| 4 | Detalhe de ingrediente — histórico de compras | `/admin/ingredientes/[id]` | ING-08, ING-05, D-03 | Última compra (preço corrente) destacada no topo do histórico |
| 5 | Registrar compras — fluxo "ida ao mercado" | `/admin/compras/nova` | ING-02, ING-03, ING-04, D-01, D-02, D-04 | Form do item corrente (ingrediente → marca → qtde × tamanho → preço) |
| 6 | Editar/excluir compra (só pré-lote) | `/admin/ingredientes/[id]` (inline no histórico) | D-03, ING-07 | Linha da compra com ações visíveis apenas quando editável |
| 7 | Lista de receitas | `/admin/receitas` | REC-01..05 | Primeira linha; custo por unidade visível por receita |
| 8 | Nova/editar receita (form com linhas dinâmicas) | `/admin/receitas/nova`, `/admin/receitas/[id]/editar` | REC-01..05, D-08 | Bloco "Ingredientes" (useFieldArray) + resumo de custo ao vivo no rodapé |
| 9 | Lista de produtos (com margem corrente) | `/admin/produtos` | PROD-01, PROD-02, PROD-08 | Coluna de margem — linhas abaixo do mínimo saltam (linha vermelha) |
| 10 | Novo/editar produto (margem em tempo real) | `/admin/produtos/novo`, `/admin/produtos/[id]/editar` | PROD-01, PROD-02, PROD-08, PROD-09, D-09, D-10, D-11 | Bloco "Margem" que reage enquanto digita o preço |
| 11 | Lista de lotes com filtros | `/admin/lotes` | LOTE-05 (schema), LOTE-07 | Filtro ativo (Vigentes default) + primeira linha com validade explícita |
| 12 | Produzir lote (fluxo de produção) | `/admin/lotes/produzir` | LOTE-01..04, D-05, D-06, D-07, D-08 | Seleção de receita → linhas de ingrediente com compra pré-selecionada |
| 13 | Ajustes — margem mínima global | `/admin/ajustes` | D-10 | Campo único "Margem mínima (%)" |
| 14 | Ajuda — aviso ANVISA | `/admin/ajuda` | PROD-10 | Heading + lista de palavras a evitar |

Total surfaces: 14, todas admin. Não há surface pública nova nesta fase.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui — **já inicializado** (`components.json` presente; CLI 4.12.0 local) |
| Preset | style `radix-nova`, baseColor `neutral`, cssVariables true, brand override em `app/globals.css` (Luizinha) — detectado do repo, não re-perguntado |
| Component library | Radix UI (pacote unificado `radix-ui@1.6`) |
| Icon library | lucide-react |
| Toast | sonner (já instalado) |
| Font (sans, UI) | Geist Sans (`--font-sans`) — todo o admin |
| Font (display, wordmark) | Fraunces (`--font-display`) — SÓ wordmark "Luizinha Confeitaria" no header; troca por script fica pra Phase 6 (BRAND.md) |
| Font (mono) | Geist Mono — não usado em Phase 2 |
| Forms | react-hook-form 7.80 + @hookform/resolvers 5.4 (instalar — RESEARCH §Standard Stack) para forms com arrays/preview; `useActionState` puro para forms simples (ingrediente, ajustes) |
| Number/date locale | `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })` para dinheiro; `Intl.DateTimeFormat('pt-BR', { timeZone: 'America/Sao_Paulo' })` + date-fns `ptBR` para datas (INFRA-10) |

---

## Spacing Scale

Herdado integralmente do Phase 1 UI-SPEC (8-point, múltiplos de 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Gap ícone↔texto, spacing inline apertado |
| sm | 8px | Gap label↔input, gap entre chips de multiplicador |
| md | 16px | **Default** entre campos de form, entre cards de lista, padding de célula de tabela |
| lg | 24px | Padding interno de card, gap entre seções do form (ex.: "dados do lote" vs "ingredientes usados") |
| xl | 32px | Padding de página (mobile), gap header↔conteúdo |
| 2xl | 48px | Quebras de seção maiores, padding de página (desktop ≥md) |
| 3xl | 64px | Respiro vertical de empty states |

**Touch target floor:** 44×44px em todo elemento clicável (herdado — locked no nível do design system). Botões primários 48px de altura; inputs 48px no mobile.

**Exceptions (Phase 2):**
- Botões icon-only dentro de linhas densas de tabela/lista admin (ex.: lápis "editar compra", X "remover linha de ingrediente" no useFieldArray): visual 40×40px permitido, **hitbox mantida ≥44px** via padding — é a exceção prevista no Phase 1 spec, agora ativa.
- Chips de multiplicador (1× / 1,5× / 2×): 44px de altura mínima, sem exceção.

---

## Typography

Herdado do Phase 1 UI-SPEC — exatamente **4 tamanhos** e **2 pesos**:

| Role | Size | Weight | Line Height | Family |
|------|------|--------|-------------|--------|
| Caption / help / erro de campo / metadado de linha ("12/06 · Extra") | 14px | 400 | 1.5 | Geist Sans |
| Body / label / input / botão / célula de tabela | 16px | 400 regular OU 600 semibold (botão, label, valor de dinheiro em destaque) | 1.5 | Geist Sans |
| Section heading (h2 — "Ingredientes da receita", "Margem") | 20px | 600 | 1.2 | Geist Sans |
| Page heading (h1 — "Fui ao mercado") | 28px | 600 | 1.2 | Geist Sans |

**Regra numérica (nova nesta fase, não é tamanho novo):** toda coluna/linha de dinheiro e quantidade usa `font-variant-numeric: tabular-nums` (classe `tabular-nums` do Tailwind) para alinhamento vertical em listas e no preview de custo. Dinheiro sempre com 2 casas no display ("R$ 5,80"), custo por unidade-base com até 4 casas significativas ("R$ 0,0147/g").

**Anti-rules (herdadas):** sem weight 300, sem itálico em body/label, sem all-caps, sem 12px em lugar nenhum, sem fonte condensada. Fraunces só no wordmark.

---

## Color

Tokens atuais de `app/globals.css` (Luizinha Confeitaria — fonte de verdade, aplicados 2026-07-03). Pares AA já verificados no código: chocolate/creme 8.2:1, chocolate/rosa 5.3:1, chocolate/rosa-claro 6.8:1, muted-fg/branco 6.9:1.

| Role | Token (shadcn var) | Hex | Usage |
|------|--------------------|-----|-------|
| Dominant 60% | `--background` | `#FFF3E6` (creme) | Fundo de página |
| Secondary 30% | `--card` / `--popover` / `--secondary` / `--sidebar` | `#FFFFFF` | Cards de lista, forms, sheet de navegação, popover do combobox |
| Secondary 30% (bordas) | `--border` / `--input` | `#FFD6E2` (rosa-claro) | Bordas de card, input, divisores de linha |
| Accent 10% | `--primary` | `#F7B6C6` (rosa) com texto `#6B3E26` (chocolate, 5.3:1 AA) | **Ver "Accent reserved for" abaixo** |
| Hover/ghost sutil | `--accent` / `--muted` | `#FFD6E2` | Hover de item de lista/nav, fundo de chip selecionado |
| Foreground | `--foreground` | `#6B3E26` (chocolate) | Todo texto (8.2:1 vs creme, AAA) |
| Foreground muted | `--muted-foreground` | `#7A5138` | Help text, captions, metadados (6.9:1 vs branco AA) |
| Focus ring | `--ring` | `#6B3E26` (chocolate) | 2px outside em todo elemento interativo |
| Destructive | `--destructive` | `#B91C1C` texto/fundo com foreground `#FFFFFF` | **Ver "Destructive reserved for" abaixo** |
| Success (reservado) | `--success` | `#15803D` | SEM uso em Phase 2 (reservado Phase 4). Margem saudável NÃO usa verde — usa foreground normal |
| Caramelo (decorativo) | — (BRAND.md) | `#C49A7A` | NUNCA texto (2.6:1). Sem uso em Phase 2; livre pra charts Phase 7 |

**Accent (`--primary` rosa) reserved for** (lista explícita — nunca "todos os elementos interativos"):
1. CTA primário de cada surface ("Salvar ingrediente", "Adicionar item", "Salvar receita", "Salvar produto", "Registrar produção", "Salvar" em ajustes)
2. Os 2 cards de atalho da home admin ("Fui ao mercado" / "Produzi hoje") — fundo rosa, texto chocolate
3. Item ativo da navegação (sheet) — `--sidebar-primary`
4. Chip de multiplicador selecionado e filtro de lote ativo (Vigentes/Vencidos/Esgotados)
5. Indicador de check em checkbox/radio selecionado

**Accent NOT used for:** botões secundários ("Trocar compra", "Cancelar" — usam `--secondary` branco + borda), links inline (foreground + underline), badges de status de lote, células de margem, ícones informativos, hover de linha (usa `--accent` rosa-claro).

**Destructive reserved for** (lista explícita):
1. **Linha vermelha de margem (PROD-08):** o bloco de margem quando margem < mínimo — borda esquerda 4px `--destructive` + ícone `TriangleAlert` (lucide) + texto 16px em `--destructive`. Nunca cor sozinha (regra color-independence)
2. **Bloqueio preço < custo (PROD-09):** alert destructive acima do CTA + CTA desabilitado no client (cortesia; o bloqueio real é server-side)
3. Item de "ações pendentes" da home admin quando um produto caiu abaixo da margem mínima (mesmo padrão borda+ícone+texto)
4. Badge "Vencido" no filtro/lista de lotes (outline destructive + ícone)
5. Botão "Excluir compra" (só visível pré-lote) e o dialog de confirmação correspondente
6. Erro de form catastrófico (falha total de submit)

**Destructive NOT used for:** erros de validação de campo (foreground + ícone + caption 14px, herdado), badge "Esgotado" (usa `--muted-foreground` + ícone `PackageX`), avisos informativos (ex.: "essa compra já foi usada num lote" quando exibido como estado, não como erro — usa ícone `Lock` + muted).

---

## Copywriting Contract

Voz herdada (locked): **calorosa, direta, "vizinha"** — "a gente", "tá", "você"; linguagem da mãe ("fui ao mercado", "produzi hoje"), NUNCA jargão ("registrar evento de aquisição", "efetivar transação"). Sem all-caps, sem ponto final em CTA, erro sempre orienta a próxima ação.

### Home admin (surface 1)

| Element | Copy |
|---------|------|
| h1 | "Oi! O que você fez hoje?" |
| Atalho 1 (card, → `/admin/compras/nova`) | **"Fui ao mercado"** + caption "Registrar as compras" |
| Atalho 2 (card, → `/admin/lotes/produzir`) | **"Produzi hoje"** + caption "Registrar um lote" |
| Seção ações pendentes — heading (h2) | "Precisa de atenção" |
| Item de margem caída | "{Produto}: a margem caiu pra {X}% — o preço de um ingrediente subiu" + link "Ver produto" |
| Ações pendentes — empty state | "Tudo em dia por aqui 🎉" + body "Quando algum doce ficar com margem baixa, a gente te avisa nesta lista." |

### Ingredientes e compras (surfaces 2–6)

| Element | Copy |
|---------|------|
| `/admin/ingredientes` h1 | "Ingredientes e embalagens" |
| CTA novo | **"Novo ingrediente"** |
| Empty state heading | "Nenhum ingrediente ainda" |
| Empty state body | "Cadastra o primeiro — nome e unidade (g, ml ou unidade) bastam. Forminha e caixa também entram aqui, como embalagem." |
| Form: label nome | "Nome" |
| Form: label unidade-base | "Como você mede" (opções: "gramas (g)" / "mililitros (ml)" / "unidade (un)") |
| Form: label tipo | "É ingrediente ou embalagem?" (opções: "Ingrediente" / "Embalagem (forminha, caixa, fita...)") |
| Form CTA | **"Salvar ingrediente"** |
| Detalhe: h2 histórico | "Compras desse ingrediente" |
| Detalhe: preço corrente | "Última compra: {marca}, {R$ X,XX} ({R$ 0,0000}/{un-base}) em {dd/mm}" |
| Histórico empty state | "Nenhuma compra registrada ainda" + body "Toca em 'Fui ao mercado' na tela inicial pra registrar a primeira." |
| Compra imutável (estado, ícone Lock) | "Usada num lote — não dá mais pra mudar" |
| Compra editável — ações | "Corrigir" / "Excluir" |
| `/admin/compras/nova` h1 | "Fui ao mercado" |
| Header do fluxo: label mercado | "Onde você comprou" (autocomplete que aprende — D-04) |
| Header do fluxo: label data | "Quando" (default hoje) |
| Item: label ingrediente | "O que você comprou" |
| Item: label marca | "Marca" (autocomplete que aprende) |
| Item: labels quantidade (D-02) | "Quantas embalagens" × "Tamanho de cada uma ({un-base})" + "Preço de cada uma (R$)" |
| Derivação ao vivo (D-02/ING-03) | "= {790}{g} por {R$ 11,60} → {R$ 0,0147} por {g}" |
| Item CTA | **"Adicionar item"** |
| Item salvo (linha na lista abaixo) | "{Ingrediente} — {marca} · {2} × {395}{g} · {R$ 11,60}" + toast "Anotado!" |
| Fluxo: CTA de saída | "Terminei as compras" (secundário — volta pra home; cada item já foi salvo na hora, fechar a aba não perde nada) |
| Erro: editar compra usada | "Essa compra já foi usada num lote, então ela não pode mais mudar — é ela que garante o custo verdadeiro do que você já produziu." |

### Receitas (surfaces 7–8)

| Element | Copy |
|---------|------|
| `/admin/receitas` h1 | "Receitas" |
| CTA novo | **"Nova receita"** |
| Empty state heading | "Nenhuma receita ainda" |
| Empty state body | "A receita diz quanto de cada ingrediente vai num lote — é com ela que a gente calcula o custo de cada doce." |
| Form: label nome | "Nome da receita" |
| Form: h2 ingredientes | "Ingredientes (pra um lote)" |
| Linha: labels | "Ingrediente" + "Quantidade ({un-base})" |
| Adicionar linha | "Mais um ingrediente" |
| Form: label rendimento | "Rende quantas unidades" |
| Form: label gás | "Gasto de gás/energia por lote (R$) — se quiser" |
| Form: label validade padrão (D-08) | "Dura quantos dias" |
| Resumo de custo ao vivo (REC-05) | "Custo do lote hoje: {R$ X,XX} · {R$ X,XX} por unidade" + caption "calculado com a última compra de cada ingrediente" |
| Ingrediente sem compra | "{Ingrediente} ainda não tem compra registrada — o custo fica incompleto até você registrar uma." |
| Form CTA | **"Salvar receita"** |

### Produtos e margem (surfaces 9–10)

| Element | Copy |
|---------|------|
| `/admin/produtos` h1 | "Produtos" |
| CTA novo | **"Novo produto"** |
| Empty state heading | "Nenhum produto ainda" |
| Empty state body | "Produto é o que vai pra vitrine: um doce (ligado a uma receita) ou um kit com vários." |
| Form: labels | "Nome" · "Descrição" · "Categoria" (autocomplete que aprende) · "É doce único ou kit?" ("Doce único" / "Kit") · "Receita" (só UNITARIO) · "Itens do kit" (só KIT) · "Preço de venda (R$)" |
| Bloco margem — saudável (D-09) | "Custa {R$ X,XX} pra fazer hoje. Vendendo a {R$ Y,YY}, ficam {R$ Z,ZZ} com você ({W}% de margem)." |
| Bloco margem — abaixo do mínimo (PROD-08, linha vermelha) | "Margem abaixo do mínimo ({30}%): de cada R$ 10 vendidos, menos de R$ 3 ficam com você. Vale subir o preço ou rever a receita." |
| Bloqueio preço < custo (PROD-09) | "Esse preço tá abaixo do custo ({R$ X,XX}) — você pagaria pra vender. Aumenta o preço pra salvar." |
| Form: override de margem (D-10) | "Margem mínima só desse produto (%) — se quiser diferente do padrão" |
| Form CTA | **"Salvar produto"** |
| Nota da definição de margem (caption no bloco, A1) | "Margem = quanto do preço fica com você, já descontado o custo." |

### Lotes (surfaces 11–12)

| Element | Copy |
|---------|------|
| `/admin/lotes` h1 | "Lotes produzidos" |
| Filtros (LOTE-07) | "Vigentes" (default) / "Vencidos" / "Esgotados" |
| Linha de lote | "{Produto} — lote de {dd/mm} · vence {dd/mm} · {N} disponíveis" |
| Badge vencido | "Vencido" (destructive outline + ícone) |
| Badge esgotado | "Esgotado" (muted + ícone) |
| Empty state (Vigentes) | "Nenhum lote na ativa" + body "Quando você produzir, toca em 'Produzi hoje' na tela inicial." |
| `/admin/lotes/produzir` h1 | "Produzi hoje" |
| Passo 1: label receita | "Qual receita você fez" |
| Label multiplicador (D-07) | "Quantas receitas" (chips 1× · 1,5× · 2× + campo livre) |
| h2 ingredientes usados (D-05) | "Ingredientes que você usou" |
| Linha pré-selecionada | "{Ingrediente} · {qtde}{un} — {marca}, compra de {dd/mm} ({R$ 0,0000}/{un})" + ação "Trocar" |
| Trocar compra (combobox) | placeholder "Qual compra você usou?" — lista as compras desse ingrediente, mais recente primeiro |
| Label rendimento real (D-06) | "Quantas unidades saíram de verdade" + caption "a receita diz {N}, mas vale o que saiu" |
| Label validade (D-08) | "Vence em" (data pré-preenchida: produção + {X} dias) + caption "confere com a etiqueta que você cola no doce" |
| Resumo antes de confirmar | "Esse lote custou {R$ X,XX} — {R$ X,XX} por unidade." |
| CTA | **"Registrar produção"** |
| Sucesso (toast) | "Lote registrado! O custo ficou guardado do jeitinho que foi hoje." |

### Ajustes e ajuda (surfaces 13–14)

| Element | Copy |
|---------|------|
| `/admin/ajustes` h1 | "Ajustes" |
| Label margem global (D-10) | "Margem mínima padrão (%)" + caption "Abaixo disso, o produto ganha um aviso vermelho. Hoje: {30}%." |
| CTA | **"Salvar"** + toast "Salvo!" |
| `/admin/ajuda` h1 | "Cuidados com o que escrever" |
| Intro ANVISA (PROD-10) | "Na descrição dos doces, evita prometer saúde — a ANVISA não permite alegação de benefício em alimento. Escreve o que ele É, não o que ele CURA." |
| Lista de palavras a evitar | "emagrece", "detox", "fortalece a imunidade", "cura", "trata", "previne doença", "medicinal", "terapêutico", "zero risco", "faz bem pra saúde" |
| Exemplo bom/ruim | "✗ 'Brigadeiro fit que fortalece a imunidade' → ✓ 'Brigadeiro com cacau 70%, menos doce'" |

### Error / fallback (herdados do Phase 1 + novos da fase)

| Element | Copy |
|---------|------|
| Erro genérico de servidor | herdado Phase 1: "Algo não deu certo do nosso lado. Tente de novo em alguns segundos." |
| Valor monetário/quantidade inválido | "Esse número não parece certo. Usa vírgula pros centavos: 5,80" |
| Campo obrigatório | herdado: "Esse campo é obrigatório." |
| Nome de ingrediente duplicado | "Já existe um ingrediente com esse nome — abre ele pra ver o histórico." |
| Loading de submit | botão vira "Salvando..." (largura preservada — sem layout shift); fluxo de compras vira "Anotando..." |
| Loading de página | skeleton no formato do conteúdo final (lista de 3 linhas), nunca spinner genérico |

### Destructive actions inventory (Phase 2)

| Action | Surface | Confirmation pattern |
|--------|---------|---------------------|
| Excluir compra (só pré-lote, D-03) | `/admin/ingredientes/[id]` | `dialog` (agora permitido — baixo risco, reversível re-registrando): título "Excluir essa compra?", body "Ela ainda não foi usada em nenhum lote, então pode sair sem dó.", CTA destructive **"Excluir compra"** + secundário "Deixa quieto" |
| Editar compra (pré-lote) | idem | Sem confirmação — edição não é destrutiva; após lote referenciar, botões somem e a linha exibe o estado "Usada num lote — não dá mais pra mudar" (Pitfall 7: check `EXISTS` preventivo; trigger é última defesa) |
| Remover linha de ingrediente no form de receita/lote | forms 8 e 12 | Sem dialog — remoção in-form desfazível antes do submit; botão X icon-only 44px hitbox |

Nenhuma exclusão de ingrediente, receita, produto ou lote entra na v1 desta fase (não há REQ; lote e compra referenciada são imutáveis por design).

---

## Interaction & Accessibility Contract

Herdado integralmente do Phase 1 UI-SPEC (44px touch, focus ring 2px `--ring`, sem hover-only, labels explícitos, `aria-describedby` em erros, sticky CTA em form longo, sonner top-center mobile). Adições específicas da fase:

| Rule | Specifics |
|------|-----------|
| Inputs de dinheiro/quantidade | `inputMode="decimal"`, aceita vírgula E ponto ("5,80"/"5.80"); NUNCA `type="number"` (spinner + parsing de vírgula quebrado no mobile). Validação via `zDecimalBRL` server-side |
| Preview de custo/margem ao vivo | decimal.js no client (custo corrente chega serializado como string do server — Pitfall 3); recálculo em `onChange` com debounce ≤300ms; o server SEMPRE recomputa e é quem bloqueia (PROD-09) |
| Status nunca só por cor | Margem baixa = borda + ícone `TriangleAlert` + texto; vencido = badge + ícone; esgotado = badge + ícone. Locked (color-independence) |
| Fluxo "ida ao mercado" (D-01) | Header (mercado+data) fica fixo no topo enquanto rola; cada "Adicionar item" persiste NA HORA (uma Server Action por item) e limpa só os campos do item (mercado/data/ingrediente ficam); lista de itens salvos cresce abaixo; foco volta pro campo "O que você comprou" após adicionar |
| Autocomplete (mercado, marca, categoria — D-04) | shadcn `combobox` (popover+command) com entrada livre: sugere valores existentes (DISTINCT), aceita valor novo; match case-insensitive reutiliza grafia canônica ao salvar |
| Trocar compra (D-05) | Cada linha mostra a última compra pré-selecionada em texto pleno (marca + data + custo); "Trocar" abre combobox listando compras do ingrediente (mais recente primeiro, com marca/data/custo visíveis) — nunca dropdown de IDs |
| Multiplicador (D-07) | Chips 1× / 1,5× / 2× (toggle, 44px) + campo numérico livre sincronizado; mudar o multiplicador reescala as quantidades exibidas na hora |
| Validade (D-08) | Input de data nativo (`type="date"`) pré-preenchido; a data resolvida SEMPRE renderizada por extenso ao lado ("vence 22/04") — precisa bater com a etiqueta impressa (Pitfall 1.5) |
| Sticky CTA | Forms longos (produzir lote, nova receita, novo produto): CTA primário sticky no rodapé do viewport no mobile, in-flow ≥md (padrão Phase 1) |
| Forms com useFieldArray | Ao adicionar linha, foco move pro primeiro campo da nova linha; ao remover, foco vai pra linha anterior (nunca perde o foco pro body) |
| Erro do trigger (Pitfall 7) | A mãe NUNCA vê mensagem crua do Postgres: action checa `EXISTS` antes e devolve a copy amigável; catch de `imutavel` como fallback com a mesma copy |

---

## Mobile-First Layout & Admin Navigation Contract

**Decisão de navegação** (Claude's Discretion do CONTEXT — recomendação registrada, defaults aplicados):

| Aspect | Mobile (<md) | Desktop (≥md, 768px) |
|--------|--------------|---------------------|
| Navegação | Header 56px: hamburger (`Menu` lucide, 44px) abre `sheet` lateral com: Início, Ingredientes, Receitas, Produtos, Lotes, Auditoria, Ajustes, Ajuda. Item ativo em `--sidebar-primary` | Sidebar fixa 240px (`--sidebar` branco, borda rosa-claro), mesmos itens; conteúdo max-width 1200px |
| Atalhos 1-toque | Home admin: 2 cards grandes lado a lado (grid 2 col, min 44px... na prática ~96px de altura) "Fui ao mercado" / "Produzi hoje" — SEMPRE acima da lista "Precisa de atenção" | Idem, largura contida |
| Header | Wordmark "Luizinha" (Fraunces) à esquerda, "Sair" à direita (padrão Phase 1) | Idem, 64px |
| Page padding | `px-md` (16px) — admin é denso, difere do `px-xl` das páginas públicas | `px-2xl` dentro do content |
| Listas | Cards empilhados (1 col) com metadado em caption 14px; SEM `table` horizontal no mobile — tabela só ≥md quando agregar (produtos com colunas de margem) | `table` shadcn permitida |
| Forms | 1 coluna sempre; trio do D-02 (qtde × tamanho × preço) pode ser grid de 3 no ≥sm, empilha no mobile estreito | 1 coluna, max-width 560px |

Racional: a mãe opera com uma mão, no mercado ou com luva de cozinha. Os dois fluxos quentes (compras, produção) precisam estar a 1 toque da home — tudo o resto é gestão ocasional e vive no menu.

---

## Component Inventory

**Já instalados (Phase 1 — não reinstalar):** button, input, label, checkbox, card, alert, sonner, separator, skeleton, table.

**Adicionar nesta fase** (registry oficial shadcn; invocar binário por caminho absoluto — hook rtk intercepta `npx`, learning 01-07):

| Component | Used by | Nota de instalação |
|-----------|---------|--------------------|
| `form` | Todos os forms RHF (receita, produto, lote, compras) | ⚠️ STUB VAZIO no style radix-nova (RESEARCH Pitfall 6) — instalar da URL: `node node_modules/shadcn/dist/index.js add https://ui.shadcn.com/r/styles/new-york-v4/form.json` |
| `select` | Unidade-base, tipo de ingrediente, tipo de produto, receita do produto | `add` normal |
| `textarea` | Descrição do produto | `add` normal |
| `dialog` | Confirmação "Excluir compra" | `add` normal (o veto do Phase 1 a dialog era específico do fluxo de conta) |
| `badge` | Status de lote (Vencido/Esgotado), tipo EMBALAGEM na lista de ingredientes | `add` normal |
| `combobox` | Autocomplete mercado/marca/categoria (D-04), trocar compra (D-05) | `add` normal — puxa `popover` + `command` como registryDependencies |
| `sheet` | Navegação mobile do admin | `add` normal |

**Dependências npm novas:** `react-hook-form@7.80` + `@hookform/resolvers@5.4` (RESEARCH §Installation).

**NÃO adicionar nesta fase:** `dropdown-menu`, `tabs`, `calendar`/`date-picker` (input date nativo basta), `chart` (Phase 7), `data-table`/tanstack (listas simples RSC bastam — anti scope creep).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official (`https://ui.shadcn.com` — styles radix-nova + new-york-v4) | form (via URL new-york-v4), select, textarea, dialog, badge, popover, command, combobox, sheet | not required (registry oficial; a URL do `form.json` permanece no domínio oficial ui.shadcn.com — verificada no RESEARCH 2026-07-03: deps radix-ui unificado + RHF + resolvers, sem padrões suspeitos) |
| Third-party | none declared | not applicable — `components.json` tem `"registries": {}` (verificado nesta sessão) |

---

## Brand & Voice Guardrails (herdados + atualizados pós-rebrand)

1. Voz da vizinha: "a gente", "tá", "você" — nunca "o usuário"/"o administrador".
2. Linguagem da mãe no admin: "Fui ao mercado", "Produzi hoje", "Quantas unidades saíram de verdade" — nunca "registrar aquisição", "efetivar lote".
3. Sem inglês, sem all-caps, sem ponto final em CTA.
4. Erro orienta ação ("Aumenta o preço pra salvar"), nunca só constata.
5. **Wordmark: "Luizinha Confeitaria"** (não "Doces Valentina" — rebrand 2026-07-03), Fraunces via `--font-display` até a Phase 6 trocar pela script do kit.
6. Números sempre pt-BR: vírgula decimal no display E no input; datas dd/mm.
7. Emojis: permitidos com MUITA parcimônia só em empty states positivos do admin (ex.: "Tudo em dia por aqui 🎉"); nunca em erro, nunca em CTA, nunca em dado financeiro.

---

## Out of Scope (Phase 2 explicitly does NOT render)

- Vitrine pública, cards de produto com foto, alergênicos, ativação de produto (PROD-03..07 → Phase 3)
- Upload/pipeline de fotos (Phase 3)
- Reserva, carrinho, pontos, painel do dia (Phase 4)
- Cron de lote vencido e sua UI de resultado (Phase 5 — Phase 2 só entrega o filtro "Vencidos")
- Relatórios agregados, gráficos, análise por marca (Phase 7 — a home "Precisa de atenção" é só o embrião do ADM-01)
- Linter ANVISA no campo de descrição (SEC-02, deferido v1.x — Phase 2 só a página de ajuda)
- Dark mode (reavaliar Phase 6)

---

## Pre-Population Audit

| Source | Count | Decisions used |
|--------|-------|---------------|
| 02-CONTEXT.md | 11 locked (D-01..D-11) + 2 discretion + specifics | Fluxo ida-ao-mercado, qtde×tamanho, edição pré-lote, autocomplete que aprende, pré-seleção de compra trocável, rendimento real, multiplicador, validade sugerida, margem corrente, config global+override, kit; voz da mãe; nav admin delegada a este spec |
| 02-RESEARCH.md | ~15 | RHF+resolvers, componentes shadcn verificados no registry (form stub!), Decimal na fronteira como string, zDecimalBRL, checagem EXISTS antes do trigger, definição de margem A1 exposta na copy, batch anti-N+1 na lista de produtos |
| Phase 1 UI-SPEC (01-UI-SPEC.md) | herdado | Spacing 8pt, 4 tamanhos/2 pesos, 44px, interaction contract, voz, error copy base, exceção icon-only 40px agora ativa |
| BRAND.md + app/globals.css (codebase scout) | tokens atuais | Paleta Luizinha completa com pares AA verificados; sidebar tokens já existentes |
| components.json + components/ui/ (codebase scout) | yes | shadcn inicializado (radix-nova), 10 componentes existentes, registries vazio |
| ROADMAP.md Phase 2 + REQUIREMENTS mapping | 25 REQ-IDs | Surface inventory, success criteria → estados exigidos |
| User input (this session) | 0 | Discretion areas resolvidas com defaults registrados (nav sheet+atalhos, alerta de margem na home); usuária estava AFK no discuss — nada re-perguntado |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*UI-SPEC drafted: 2026-07-03 by gsd-ui-researcher*
*Phase: 2 — Motor Financeiro*
*Consumed by: gsd-ui-checker (validation), gsd-planner (task generation), gsd-executor (implementation), gsd-ui-auditor (retro audit)*
