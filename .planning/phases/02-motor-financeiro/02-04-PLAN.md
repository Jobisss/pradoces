---
phase: 02-motor-financeiro
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - lib/validation/ingredientes.ts
  - lib/validation/compras.ts
  - lib/actions/ingredientes.ts
  - lib/actions/compras.ts
  - tests/financeiro/compras.test.ts
autonomous: true
requirements: [ING-01, ING-02, ING-03, ING-04, ING-05, ING-06, ING-07]
must_haves:
  truths:
    - "Registrar compra grava a derivação ING-03 (qtde total na unidade-base + R$/unidade-base) calculada server-side com Decimal"
    - "Compra é editável/excluível ANTES do primeiro lote referenciá-la e imutável DEPOIS, com copy amigável (nunca erro cru do Postgres)"
    - "Marca e mercado são canonicalizados na escrita ('moça' reutiliza a grafia 'Moça' existente)"
    - "Cliente autenticado não-admin NÃO consegue invocar nenhuma mutação (requireAdmin)"
  artifacts:
    - path: "lib/actions/ingredientes.ts"
      provides: "criarIngrediente, editarIngrediente (ING-01, ING-06)"
    - path: "lib/actions/compras.ts"
      provides: "registrarCompra (D-01/D-02/ING-03), corrigirCompra, excluirCompra (D-03), sugestoes (D-04)"
    - path: "tests/financeiro/compras.test.ts"
      provides: "Testes de derivação, trigger, canonicalização e EoP"
  key_links:
    - from: "lib/actions/compras.ts"
      to: "lib/auth/require-admin.ts"
      via: "primeira linha de toda mutação"
      pattern: "requireAdmin\\(\\)"
    - from: "lib/actions/compras.ts"
      to: "lib/validation/decimal.ts"
      via: "zDecimalBRL nos campos monetários"
      pattern: "zDecimalBRL"
    - from: "lib/actions/compras.ts"
      to: "lib/audit/log.ts"
      via: "logAudit compra_registrada após mutação"
      pattern: "compra_registrada"
---

<objective>
Backend completo de ingredientes e compras: schemas Zod, Server Actions admin-only (CRUD de ingrediente; registro de compra individual imutável com derivação ING-03; correção/exclusão guardada por D-03; autocomplete que aprende D-04), e a suite de testes de integração.

Purpose: é o primeiro elo da cadeia do custo congelado — cada compra é o evento que os lotes referenciam. A UI (plano 02-08) só chama estas actions.
Output: 2 arquivos de validação + 2 de actions + compras.test.ts verde.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-financeiro/02-RESEARCH.md
@.planning/phases/02-motor-financeiro/02-PATTERNS.md

⚠️ Test DB == dev DB: rodar vitest apaga dados do dev.
</context>

<interfaces>
Skeleton de mutação (lib/actions/auth.ts:57-116 — copiar a ordem, trocando o passo 2.5 por requireAdmin):
```typescript
'use server'
// 1. clientContext() (ip/ua p/ audit)  2. rate limit (defesa em profundidade)
// 2.5 await requireAdmin() — DENTRO de try/catch que retorna erro genérico
// 3. Zod safeParse  4. mutação em try/catch  5. logAudit  6. revalidatePath / return {ok:true}
export type AuthActionState = { error?: string; fieldErrors?: Record<string, string[] | undefined>; message?: string; ok?: boolean }
```
`clientContext()` (auth.ts:44-51): `const h = await nextHeaders(); const ip = clientIp(h); const ua = h.get('user-agent') ?? undefined`.
Rate limit (auth.ts:62-63): `const rl = await rateLimitAuth.consume(ip).catch(() => null); if (rl === null) return { error: RATE_LIMIT_COPY }`.
logAudit (lib/audit/log.ts:26-41): `logAudit({ actorType: 'admin', actorId, action, entityType?, entityId?, metadata?, rawIp?, rawUa? })`.
Helper action de args simples (auth.ts:125-131, analog p/ sugestões): função async exportada do mesmo arquivo 'use server', chamada direto do client.

Canonicalização D-04 (RESEARCH Pattern 5, verbatim — allowlist obrigatória):
```typescript
function normalizeLabel(raw: string): string { return raw.trim().replace(/\s+/g, ' ') }
async function canonicalize(campo: 'marca' | 'mercado', valor: string) {
  const v = normalizeLabel(valor)
  const [existing] = await prisma.$queryRaw<{ v: string }[]>`
    SELECT DISTINCT ${Prisma.raw(campo)} AS v FROM ingrediente_compras
    WHERE lower(${Prisma.raw(campo)}) = lower(${v}) LIMIT 1`
  return existing?.v ?? v
}
// ⚠️ Prisma.raw SÓ com o identificador da allowlist literal 'marca'|'mercado' — nunca input do user.
```

Do plano 02-03: `requireAdmin()` de `@/lib/auth/require-admin`, `zDecimalBRL`/`zQtdeBRL` de `@/lib/validation/decimal`, factories em `tests/financeiro/fixtures.ts`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Validação + actions de ingredientes (ING-01, ING-06)</name>
  <files>lib/validation/ingredientes.ts, lib/actions/ingredientes.ts</files>
  <read_first>
    - lib/actions/auth.ts (skeleton completo de action — ordem dos passos, consts de copy, ActionState)
    - lib/validation/auth.ts (convenção de schemas + copy literals)
    - lib/auth/require-admin.ts (guard do 02-03)
  </read_first>
  <action>
1. `lib/validation/ingredientes.ts`: `IngredienteSchema = z.object({ nome: z.string().trim().min(1, OBRIGATORIO), unidadeBase: z.enum(['g','ml','un']), tipo: z.enum(['INGREDIENTE','EMBALAGEM']) })` + `export type IngredienteInput = z.infer<...>`. Const `OBRIGATORIO = 'Esse campo é obrigatório.'` (mesma copy da Phase 1).
2. `lib/actions/ingredientes.ts` (`'use server'`), state type igual ao AuthActionState. Actions:
   - `criarIngrediente(_prev, formData)`: passos do skeleton com `await requireAdmin()` dentro do try (catch → `{ error: GENERIC_SERVER_ERROR }`). Nome duplicado (P2002 no unique `nome`) → copy EXATA do UI-SPEC: "Já existe um ingrediente com esse nome — abre ele pra ver o histórico." Após criar: `revalidatePath('/admin/ingredientes')`, `redirect('/admin/ingredientes')`.
   - `editarIngrediente(id, _prev, formData)` (bind do id no form): valida uuid (`z.string().uuid()`), mesmo fluxo. Editar unidadeBase é permitido apenas se o ingrediente NÃO tem compras (mudar a unidade quebraria a semântica das compras históricas) — checar `count` de compras e retornar erro amigável: "Esse ingrediente já tem compras registradas em {un} — a unidade não pode mais mudar."
   - Embalagens (ING-06) entram pelo MESMO fluxo com tipo='EMBALAGEM' — nenhuma action separada.
   Consts de copy no topo do módulo. `GENERIC_SERVER_ERROR = 'Algo não deu certo do nosso lado. Tente de novo em alguns segundos.'`
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "requireAdmin" lib/actions/ingredientes.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'use server'" lib/actions/ingredientes.ts` == 1
    - `grep -c "requireAdmin()" lib/actions/ingredientes.ts` ≥ 2 (uma por mutação)
    - `grep -c "Já existe um ingrediente com esse nome" lib/actions/ingredientes.ts` == 1
    - `grep -cE "z\.number\(\)|parseFloat" lib/validation/ingredientes.ts lib/actions/ingredientes.ts` == 0
  </acceptance_criteria>
  <done>CRUD de ingrediente admin-only com copy pt-BR e proteção de unidade-base.</done>
</task>

<task type="auto">
  <name>Task 2: Validação + actions de compras (D-01/D-02/D-03/D-04, ING-02/03/04/05)</name>
  <files>lib/validation/compras.ts, lib/actions/compras.ts</files>
  <read_first>
    - lib/actions/auth.ts (skeleton + helper action de args simples checkEmailExists)
    - lib/custo/corrente.ts (ultimasCompras — para não duplicar lógica)
    - .planning/phases/02-motor-financeiro/02-RESEARCH.md §Pattern 5 (canonicalize) e §Pitfall 7 (trigger → copy amigável)
  </read_first>
  <action>
1. `lib/validation/compras.ts`: `CompraSchema = z.object({ ingredienteId: z.string().uuid(), dataCompra: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, OBRIGATORIO), mercado: z.string().trim().min(1, OBRIGATORIO), marca: z.string().trim().min(1, OBRIGATORIO), qtdeEmbalagens: zQtdeBRL, tamanhoEmbalagem: zQtdeBRL, precoPorEmbalagem: zDecimalBRL })`.
2. `lib/actions/compras.ts` (`'use server'`):
   - `registrarCompra(_prev, formData)`: skeleton completo (context → rate limit → requireAdmin → Zod). Derivação ING-03 server-side com Decimal: `qtdeTotalBase = qtdeEmbalagens.mul(tamanhoEmbalagem)` (`.toFixed(3)`), `precoTotal = qtdeEmbalagens.mul(precoPorEmbalagem)` (`.toFixed(4)`), `custoPorUnidadeBase = precoTotal.div(qtdeTotalBase)` (`.toFixed(6)`); rejeitar `qtdeTotalBase == 0` com copy de número inválido. `marca` e `mercado` passam por `canonicalize` (verbatim do bloco interfaces). INSERT único (D-01: o fluxo "ida ao mercado" chama esta action UMA vez por item — persistência imediata, nada de batch). `logAudit({ actorType: 'admin', actorId, action: 'compra_registrada', entityType: 'ingrediente_compra', entityId, rawIp, rawUa })`. `revalidatePath('/admin/ingredientes')`. Retorna `{ ok: true, message: 'Anotado!' , item: { id, resumo } }` (SEM Decimal no retorno — strings; Pitfall 3).
   - `corrigirCompra(id, _prev, formData)` e `excluirCompra(id)`: requireAdmin; checagem PREVENTIVA `const usada = await prisma.loteUsoIngrediente.count({ where: { ingredienteCompraId: id } })` — se > 0, retornar copy EXATA do UI-SPEC: "Essa compra já foi usada num lote, então ela não pode mais mudar — é ela que garante o custo verdadeiro do que você já produziu." Se 0, prosseguir (corrigir re-deriva ING-03; excluir deleta). Catch com match `/imutavel/` devolve a MESMA copy (trigger é última defesa — Pitfall 7). Audit em corrigir/excluir com metadata `{ antes, depois }` para corrigir.
   - `sugestoesMarca(prefix: string)`, `sugestoesMercado(prefix: string)`, `sugestoesCategoria` NÃO aqui (categoria é do produto — plano 02-06). Helpers de args simples (analog checkEmailExists): `SELECT DISTINCT marca FROM ingrediente_compras WHERE marca ILIKE ${prefix + '%'} ORDER BY marca LIMIT 10` via $queryRaw tagged template (coluna fixa no template, sem Prisma.raw). Sem requireAdmin? SIM requireAdmin também (dados de compra são internos do negócio) — mas retornando `[]` no catch em vez de erro.
   - `historicoCompras` não é action — a página RSC (02-08) consulta prisma direto.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "compra_registrada" lib/actions/compras.ts && grep -c "loteUsoIngrediente.count" lib/actions/compras.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requireAdmin()" lib/actions/compras.ts` ≥ 5 (registrar, corrigir, excluir, 2 sugestões)
    - `grep -c "Essa compra já foi usada num lote" lib/actions/compras.ts` ≥ 1
    - `grep -c "canonicalize" lib/actions/compras.ts` ≥ 2 (marca e mercado)
    - `grep -c "queryRawUnsafe" lib/actions/compras.ts` == 0; `Prisma.raw` aparece apenas dentro de canonicalize com union literal 'marca' | 'mercado'
    - `grep -cE "\.toFixed\(6\)" lib/actions/compras.ts` ≥ 1 (custoPorUnidadeBase em 19,6)
  </acceptance_criteria>
  <done>Compras: registro imutável com derivação exata, edição guardada D-03, autocomplete D-04 — tudo admin-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes de integração de compras (trigger, derivação, canonicalização, EoP)</name>
  <files>tests/financeiro/compras.test.ts</files>
  <read_first>
    - tests/auth/password-reset.test.ts (padrão 01-08: vi.hoisted ctx + mock next/headers e next/navigation + form() helper)
    - tests/conftest.ts (createTestUser({role:'admin'}), signInAsAdmin, truncateAll)
    - tests/financeiro/fixtures.ts (factories do 02-03)
    - lib/actions/compras.ts (assinaturas reais recém-criadas)
  </read_first>
  <behavior>
    - ING-03: registrarCompra com qtdeEmbalagens='2', tamanhoEmbalagem='395', precoPorEmbalagem='5,80' → row com qtdeTotalBase '790.000', precoTotal '11.6000', custoPorUnidadeBase '0.014684' (11.60 ÷ 790 = 0.014683544... → toFixed(6) = '0.014684')
    - ING-06: ingrediente tipo EMBALAGEM aceita compra pelo mesmo fluxo
    - D-04: registrar com marca 'Moça' e depois 'moça' → segunda row gravada com 'Moça' (canonicalização); sugestoesMarca('mo') retorna ['Moça'] uma vez só
    - D-03: corrigirCompra em compra sem lote funciona (re-deriva); excluirCompra idem
    - ING-04/ING-07: após produzirLote (fixture) referenciar a compra, corrigirCompra e excluirCompra retornam a copy amigável (e NÃO estouram erro cru); update direto via prisma rejeita com /imutavel/
    - ING-05: fixture com 2 compras → ultimasCompras retorna a mais recente
    - EoP (T-02-06): sessão de customer (createTestUser sem role admin + signInAsCustomer, cookie no mock de headers) → registrarCompra retorna { error } genérico e NÃO insere row
  </behavior>
  <action>
Criar `tests/financeiro/compras.test.ts` com o padrão 01-08: `vi.hoisted` ctx com ip mutável + cookie mutável; `vi.mock('next/headers')` retornando Headers com `x-forwarded-for`, `user-agent` e `cookie` do ctx; `vi.mock('next/navigation', () => ({ redirect: vi.fn(), }))`. Helper `form(fields)` para FormData. Sessão admin: `const admin = await createTestUser({ role: 'admin' }); const { cookie } = await signInAsAdmin(admin.id)` e injetar no ctx. IP randomizado por teste (rate limiter in-process). `beforeEach(truncateAll)`. Implementar TODOS os casos do behavior. Decimal via `.toFixed(n)` strings.
  </action>
  <verify>
    <automated>rtk npx vitest run tests/financeiro/compras.test.ts --reporter=dot && rtk npm test</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo contém as asserções literais `'0.014684'`, `'790.000'` e `/imutavel/`
    - Contém um caso com sessão customer esperando `{ error }` (grep "signInAsCustomer")
    - Suite completa verde
  </acceptance_criteria>
  <done>Cadeia compra→derivação→imutabilidade provada por integração, incluindo EoP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (qualquer usuário autenticado) → Server Actions de compras | Actions são endpoints POST públicos; layout não as protege |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege | todas as actions deste plano | mitigate | requireAdmin() primeira linha; teste com sessão customer esperando erro (Task 3) |
| T-02-07 | Tampering (SQLi) | canonicalize/sugestões ($queryRaw) | mitigate | Tagged templates; Prisma.raw só com allowlist literal 'marca'\|'mercado'; gate por grep |
| T-02-09 | Tampering | derivação ING-03 | mitigate | qtdeTotalBase/precoTotal/custoPorUnidadeBase recomputados server-side; client nunca envia valores derivados |
| T-02-01 | Tampering/Repudiation | histórico de compras | mitigate | Checagem EXISTS preventiva + trigger (02-01) + audit compra_registrada com IP/UA hasheados |
| T-02-10 | DoS | actions admin | mitigate | rateLimitAuth.consume(ip) (defesa em profundidade, mesmas instâncias in-process do 01-08) |
</threat_model>

<verification>
- `rtk npx vitest run tests/financeiro --reporter=dot` — verde
- `rtk npm test` — suite completa verde
- `rtk npx tsc --noEmit` — 0 erros
</verification>

<success_criteria>
- Toda mutação: requireAdmin + Zod + audit + revalidatePath, na ordem do skeleton 01-08
- Derivação ING-03 exata em Decimal (6 casas no custo unitário)
- D-03 com UX primeiro (EXISTS) e trigger como última defesa
- Nenhuma mensagem crua de Postgres/Prisma alcançável pela mãe
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-financeiro/02-04-SUMMARY.md`
</output>
