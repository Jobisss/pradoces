---
phase: 02-motor-financeiro
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - components/ui/form.tsx
  - components/ui/select.tsx
  - components/ui/textarea.tsx
  - components/ui/dialog.tsx
  - components/ui/badge.tsx
  - components/ui/popover.tsx
  - components/ui/command.tsx
  - components/ui/combobox.tsx
  - components/ui/sheet.tsx
  - app/(admin)/admin/layout.tsx
  - components/admin/admin-nav.tsx
  - app/(admin)/admin/ajuda/page.tsx
autonomous: true
requirements: [PROD-10]
must_haves:
  truths:
    - "Admin navega entre as 8 áreas (Início, Ingredientes, Receitas, Produtos, Lotes, Auditoria, Ajustes, Ajuda) por sheet no mobile e sidebar no desktop"
    - "components/ui/form.tsx existe com conteúdo real (não é o stub vazio do radix-nova)"
    - "Página de ajuda exibe o aviso ANVISA com a lista de palavras a evitar"
  artifacts:
    - path: "components/ui/form.tsx"
      provides: "shadcn form (RHF bridge) via workaround new-york-v4"
      min_lines: 100
    - path: "components/ui/combobox.tsx"
      provides: "Autocomplete base (D-04, D-05)"
    - path: "components/admin/admin-nav.tsx"
      provides: "Navegação admin mobile (sheet) + desktop (sidebar)"
    - path: "app/(admin)/admin/ajuda/page.tsx"
      provides: "Aviso ANVISA (PROD-10)"
      contains: "ANVISA"
  key_links:
    - from: "app/(admin)/admin/layout.tsx"
      to: "components/admin/admin-nav.tsx"
      via: "import + render no shell admin"
      pattern: "AdminNav"
    - from: "package.json"
      to: "react-hook-form"
      via: "dependencies"
      pattern: "react-hook-form"
---

<objective>
Preparar a infraestrutura de UI da fase: instalar react-hook-form + resolvers, adicionar os componentes shadcn que os forms da fase exigem (com o workaround do stub `form` do radix-nova), construir a navegação do admin (sheet mobile + sidebar desktop, decisão de discretion registrada no UI-SPEC), e entregar a página estática de ajuda ANVISA (PROD-10).

Purpose: os 3 planos de UI (waves 4) e o form de receitas (wave 3) dependem destes componentes e da navegação. Zero dependência do schema — roda em paralelo com o plano 01.
Output: deps instaladas, 9 componentes shadcn novos, nav admin funcional, /admin/ajuda no ar.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-financeiro/02-UI-SPEC.md
@.planning/phases/02-motor-financeiro/02-RESEARCH.md

⚠️ O hook rtk intercepta `npx shadcn` — invocar o binário por caminho: `node node_modules/shadcn/dist/index.js add ...` (learning 01-07).
⚠️ Pitfall 6 (RESEARCH): `shadcn add form` no style radix-nova completa "com sucesso" sem criar arquivo (stub de 104 bytes no registry). Instalar da URL new-york-v4.
</context>

<interfaces>
Shell admin atual: `app/(admin)/admin/layout.tsx` — session fetch + role gate (linhas 14–16):
```typescript
const session = await auth.api.getSession({ headers: await nextHeaders() })
if (!session) redirect('/admin/entrar')
if (session.user.role !== 'admin') { /* 403 copy */ }
```
Manter o gate intacto; a nav é ADIÇÃO ao shell, não substituição.

Tokens de marca (app/globals.css, Luizinha): `--sidebar` branco, `--border` #FFD6E2, `--sidebar-primary` para item ativo, `--font-display` (Fraunces) só no wordmark.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Instalar react-hook-form + resolvers e os componentes shadcn (com workaround do form)</name>
  <files>package.json, package-lock.json, components/ui/form.tsx, components/ui/select.tsx, components/ui/textarea.tsx, components/ui/dialog.tsx, components/ui/badge.tsx, components/ui/popover.tsx, components/ui/command.tsx, components/ui/combobox.tsx, components/ui/sheet.tsx</files>
  <read_first>
    - components.json (config shadcn: style radix-nova, aliases)
    - components/ui/button.tsx (formato dos componentes já vendorados — conferir convenção de import `radix-ui`)
  </read_first>
  <action>
Executar exatamente (RESEARCH §Installation, verificado no registry ao vivo em 2026-07-03):

```bash
rtk npm install react-hook-form @hookform/resolvers
node node_modules/shadcn/dist/index.js add https://ui.shadcn.com/r/styles/new-york-v4/form.json
node node_modules/shadcn/dist/index.js add select textarea dialog badge combobox sheet
```

Notas:
- `combobox` puxa `popover` + `command` como registryDependencies — não adicionar separadamente a menos que o CLI não os traga.
- O `form.json` da URL new-york-v4 depende do pacote unificado `radix-ui` (já instalado @1.6) + RHF + resolvers — compatível.
- Após o add, CONFERIR que `components/ui/form.tsx` tem conteúdo real (>100 linhas, exporta `Form`, `FormField`, `FormItem`, `FormLabel`, `FormControl`, `FormMessage`). Se vier vazio/ausente, vendorar manualmente o conteúdo de https://ui.shadcn.com/r/styles/new-york-v4/form.json.
- Se algum componente importar de `@radix-ui/react-*` (pacotes antigos) em vez do unificado `radix-ui`, ajustar o import para o padrão dos componentes existentes.
  </action>
  <verify>
    <automated>test -s components/ui/form.tsx && grep -c "FormField" components/ui/form.tsx && test -s components/ui/combobox.tsx && test -s components/ui/sheet.tsx && rtk npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"react-hook-form"' package.json` e `grep '"@hookform/resolvers"' package.json` retornam match
    - Os 9 arquivos existem em components/ui/ e nenhum está vazio (`test -s` em cada)
    - `grep -c "export" components/ui/form.tsx` ≥ 5 (Form, FormField, FormItem, FormLabel, FormControl/FormMessage)
    - `rtk npx tsc --noEmit` sai 0
  </acceptance_criteria>
  <done>Deps instaladas e 9 componentes shadcn reais no repo, tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Navegação admin — sheet mobile + sidebar desktop</name>
  <files>components/admin/admin-nav.tsx, app/(admin)/admin/layout.tsx</files>
  <read_first>
    - app/(admin)/admin/layout.tsx (gate de role atual — NÃO quebrar)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §"Mobile-First Layout & Admin Navigation Contract"
    - components/ui/sheet.tsx (API do componente recém-adicionado)
  </read_first>
  <action>
Criar `components/admin/admin-nav.tsx` (`'use client'` — sheet precisa de estado) implementando o contrato do UI-SPEC:

**Mobile (<md):** header 56px com hamburger (ícone `Menu` do lucide-react, hitbox 44×44px, `aria-label="Abrir menu"`) que abre `Sheet` lateral com os itens, nesta ordem e com estes labels exatos: "Início" (/admin), "Ingredientes" (/admin/ingredientes), "Receitas" (/admin/receitas), "Produtos" (/admin/produtos), "Lotes" (/admin/lotes), "Auditoria" (/admin/auditoria), "Ajustes" (/admin/ajustes), "Ajuda" (/admin/ajuda). Item ativo (via `usePathname()` — match por prefixo, /admin só exato) com fundo `--sidebar-primary`. Cada item ≥44px de altura.

**Desktop (≥md, 768px):** sidebar fixa 240px (`bg-sidebar`, borda `border-border`), mesmos itens; conteúdo com max-width 1200px.

**Header:** wordmark "Luizinha" em `font-display` à esquerda (link para /admin), botão/link "Sair" à direita (preservar o mecanismo de signout existente no layout atual, se houver; senão link para a action de signout do Phase 1).

Modificar `app/(admin)/admin/layout.tsx` para renderizar `<AdminNav />` + área de conteúdo. O role-gate existente permanece EXATAMENTE como está (session fetch + redirect + 403). Page padding do conteúdo: `px-4` mobile (16px), mais largo ≥md, per UI-SPEC.

Sem accent rosa em itens de nav não-ativos; hover usa `--accent` (rosa-claro). Sem dark mode.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "Abrir menu" components/admin/admin-nav.tsx && grep -c "AdminNav" "app/(admin)/admin/layout.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - components/admin/admin-nav.tsx contém as 8 strings de label exatas: Início, Ingredientes, Receitas, Produtos, Lotes, Auditoria, Ajustes, Ajuda
    - `grep -c 'aria-label="Abrir menu"' components/admin/admin-nav.tsx` == 1
    - layout.tsx ainda contém o gate (`grep "redirect('/admin/entrar')" "app/(admin)/admin/layout.tsx"` retorna match) e importa AdminNav
    - `rtk npm test` continua verde (nenhum teste de layout quebra)
  </acceptance_criteria>
  <done>Admin tem navegação completa mobile+desktop com item ativo destacado; gate de role intacto.</done>
</task>

<task type="auto">
  <name>Task 3: Página de ajuda — aviso ANVISA (PROD-10)</name>
  <files>app/(admin)/admin/ajuda/page.tsx</files>
  <read_first>
    - app/(public)/termos/page.tsx (analog de página estática de conteúdo)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §"Ajustes e ajuda (surfaces 13–14)"
  </read_first>
  <action>
Criar RSC estática `app/(admin)/admin/ajuda/page.tsx` com o copy EXATO do UI-SPEC:

- h1 (28px/600): "Cuidados com o que escrever"
- Intro: "Na descrição dos doces, evita prometer saúde — a ANVISA não permite alegação de benefício em alimento. Escreve o que ele É, não o que ele CURA."
- Lista de palavras a evitar (uma por item): "emagrece", "detox", "fortalece a imunidade", "cura", "trata", "previne doença", "medicinal", "terapêutico", "zero risco", "faz bem pra saúde"
- Exemplo bom/ruim: "✗ 'Brigadeiro fit que fortalece a imunidade' → ✓ 'Brigadeiro com cacau 70%, menos doce'"

Tipografia do UI-SPEC (16px body, 20px section heading), sem componentes novos. É a versão v1 completa do PROD-10 — NÃO implementar linter no campo de descrição (SEC-02 deferido v1.x).
  </action>
  <verify>
    <automated>grep -c "ANVISA" "app/(admin)/admin/ajuda/page.tsx" && grep -c "fortalece a imunidade" "app/(admin)/admin/ajuda/page.tsx" && rtk npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Página contém as strings exatas "Cuidados com o que escrever", "ANVISA" e as 10 palavras da lista
    - Nenhum código de linter/validação de descrição foi adicionado (grep -ri "linter" app/ == 0 matches novos)
  </acceptance_criteria>
  <done>/admin/ajuda renderiza o aviso ANVISA completo per UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry externo → repo | Código de terceiros (shadcn registry) vendorado no repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | componentes shadcn vendorados | mitigate | Só registry oficial ui.shadcn.com (styles radix-nova e new-york-v4 — URLs verificadas no RESEARCH); `components.json` registries permanece `{}`; revisar diff dos arquivos adicionados antes do commit |
| T-02-05 | Information Disclosure | nav admin | accept | Nav só renderiza dentro do layout gateado por role; rotas listadas não são segredo (proteção real é o gate + requireAdmin nas actions) |
</threat_model>

<verification>
- `rtk npx tsc --noEmit` — 0 erros
- `rtk npm test` — suite Phase 1 continua verde
- 9 arquivos novos em components/ui/, todos não-vazios
- /admin navegável com 8 itens (verificação visual final no plano 02-11)
</verification>

<success_criteria>
- react-hook-form@7.x + @hookform/resolvers@5.x em package.json
- form.tsx real (workaround do stub aplicado)
- Nav admin mobile-first per UI-SPEC (44px targets, aria-labels, item ativo)
- PROD-10 entregue como página de ajuda estática
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-financeiro/02-02-SUMMARY.md`
</output>
