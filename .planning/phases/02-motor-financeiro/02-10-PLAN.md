---
phase: 02-motor-financeiro
plan: 10
type: execute
wave: 4
depends_on: ["02-02", "02-07"]
files_modified:
  - app/(admin)/admin/lotes/page.tsx
  - app/(admin)/admin/lotes/produzir/page.tsx
  - components/admin/produzir-lote-form.tsx
autonomous: true
requirements: [LOTE-01, LOTE-07]
must_haves:
  truths:
    - "Mãe registra produção: escolhe receita, vê linhas com a última compra pré-selecionada (trocável — D-05), multiplica (D-07), informa rendimento real (D-06) e validade sugerida (D-08), e confirma"
    - "Lista de lotes filtra Vigentes (default) / Vencidos / Esgotados com validade explícita por linha"
    - "Resumo antes de confirmar mostra o custo que será congelado; toast de sucesso confirma o congelamento"
  artifacts:
    - path: "components/admin/produzir-lote-form.tsx"
      provides: "Fluxo de produção completo (D-05/06/07/08)"
      min_lines: 150
    - path: "app/(admin)/admin/lotes/page.tsx"
      provides: "Lista com filtros LOTE-07 e badges"
  key_links:
    - from: "components/admin/produzir-lote-form.tsx"
      to: "lib/actions/lotes.ts"
      via: "dadosProducao + comprasDoIngrediente + produzirLote"
      pattern: "produzirLote"
    - from: "app/(admin)/admin/lotes/page.tsx"
      to: "lib/lotes/queries.ts"
      via: "listarLotes(filtro) com hoje BRT"
      pattern: "listarLotes"
---

<objective>
UI de lotes (surfaces 11–12): lista com filtros LOTE-07 e o fluxo "Produzi hoje" — o formulário mais importante do motor financeiro, onde o custo é congelado.

Purpose: fecha o ciclo compra → receita → lote na mão da mãe. Toda a lógica pesada já existe (02-07); aqui é composição fiel ao UI-SPEC.
Output: /admin/lotes e /admin/lotes/produzir funcionais.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-financeiro/02-UI-SPEC.md
@.planning/phases/02-motor-financeiro/02-PATTERNS.md

⚠️ D-08 / Pitfall 1.5: a data de validade resolvida SEMPRE renderizada por extenso ao lado do input ("vence 22/04") — precisa bater com a etiqueta impressa.
⚠️ Pitfall 3: strings na fronteira; decimal.js no client pro resumo.
</context>

<interfaces>
Do 02-07 (lib/actions/lotes.ts):
- `dadosProducao(receitaId)` → `{ receita: { id, nome, rendimentoPadrao, validadeDias, custoGas: string|null }, linhas: [{ ingredienteId, nome, unidadeBase, qtdeBase: string, compraSelecionada: { id, marca, dataCompra: string, custoPorUnidadeBase: string } | null }] }`
- `comprasDoIngrediente(ingredienteId)` → `[{ id, marca, dataCompra: string, custoPorUnidadeBase: string }]` (mais recente primeiro)
- `produzirLote(input)` → `{ ok, id, custoTotal: string, custoPorUnidade: string } | { error, fieldErrors }`
Do 02-07 (lib/lotes/queries.ts): `listarLotes(filtro: 'vigentes'|'vencidos'|'esgotados')`, `hojeSaoPaulo()`.
Copy (UI-SPEC §surfaces 11–12) — strings-chave embutidas nas tasks.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Lista de lotes com filtros e badges</name>
  <files>app/(admin)/admin/lotes/page.tsx</files>
  <read_first>
    - lib/lotes/queries.ts (assinatura listarLotes)
    - app/(admin)/admin/auditoria/page.tsx (analog lista RSC)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §surface 11 + §Color (badges)
  </read_first>
  <action>
RSC com searchParam `?filtro=` (default 'vigentes'): h1 "Lotes produzidos". Filtros como chips/links de 44px: "Vigentes" (default) / "Vencidos" / "Esgotados" — ativo com fundo accent (`--primary`).
Linha (card mobile): "{Produto} — lote de {dd/mm} · vence {dd/mm} · {N} disponíveis" (datas via Intl pt-BR America/Sao_Paulo; tabular-nums). Badge "Vencido" (outline destructive + ícone TriangleAlert) no filtro vencidos; badge "Esgotado" (muted-foreground + ícone PackageX) quando qtdeDisponivel=0. Custo congelado por unidade visível como caption ("custou R$ X,XX/un").
Empty state (Vigentes): "Nenhum lote na ativa" + "Quando você produzir, toca em 'Produzi hoje' na tela inicial." CTA topo: "Produzi hoje" → /admin/lotes/produzir.
Sem N+1: listarLotes já faz include de produto.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "Lotes produzidos" "app/(admin)/admin/lotes/page.tsx" && grep -c "listarLotes" "app/(admin)/admin/lotes/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Os 3 filtros presentes com labels exatos "Vigentes"/"Vencidos"/"Esgotados"; default vigentes
    - Badges com ícone + texto (nunca cor sozinha): grep PackageX e TriangleAlert
    - Copy exata do empty state
  </acceptance_criteria>
  <done>Lista LOTE-07 completa com filtros BRT-corretos e badges multi-canal.</done>
</task>

<task type="auto">
  <name>Task 2: Fluxo "Produzi hoje" (D-05/D-06/D-07/D-08)</name>
  <files>app/(admin)/admin/lotes/produzir/page.tsx, components/admin/produzir-lote-form.tsx</files>
  <read_first>
    - lib/actions/lotes.ts (assinaturas reais das 3 funções)
    - components/admin/receita-form.tsx (padrão RHF/estado estabelecido)
    - components/ui/combobox.tsx (API)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §surface 12 + §Interaction (multiplicador, trocar compra, validade)
  </read_first>
  <action>
1. Page (RSC): h1 "Produzi hoje"; carrega receitas COM produto vinculado (`prisma.receita.findMany({ where: { produto: { isNot: null } }, include: { produto: { select: { id: true, nome: true } } } })`) — lote referencia produto+receita; receita sem produto não pode produzir (exibir aviso "Essa receita ainda não tem produto — cadastra o produto primeiro" se a lista vier vazia com receitas existentes). Passa `[{ receitaId, produtoId, nome }]` ao form.
2. `components/admin/produzir-lote-form.tsx` (`'use client'`):
   - **Passo 1**: "Qual receita você fez" (select/combobox). Ao selecionar → `dadosProducao(receitaId)` via useTransition → popula linhas.
   - **Multiplicador (D-07)**: label "Quantas receitas" — chips toggle 1× / 1,5× / 2× (44px, selecionado com fundo accent) + campo numérico livre sincronizado (`inputMode="decimal"`); mudar reescala as quantidades exibidas NA HORA (qtdeBase × multiplicador via decimal.js).
   - **h2 "Ingredientes que você usou" (D-05)**: cada linha exibe "{Ingrediente} · {qtde}{un} — {marca}, compra de {dd/mm} ({R$ 0,0000}/{un})" com a compra pré-selecionada; ação "Trocar compra" abre combobox (placeholder "Qual compra você usou?") populado por `comprasDoIngrediente` — cada opção mostra marca + data + custo, NUNCA IDs. Linha sem compra (compraSelecionada null): aviso "ainda não tem compra registrada" e CTA desabilitado até resolver (todas as linhas precisam de compra — LOTE-02 FK NOT NULL).
   - **Rendimento real (D-06)**: label "Quantas unidades saíram de verdade" + caption "a receita diz {N}, mas vale o que saiu" (N = rendimentoPadrao × multiplicador, arredondado pra exibição).
   - **Validade (D-08)**: label "Vence em" — `type="date"` pré-preenchido com hoje + validadeDias (recalcula se validadeDias existir; senão vazio obrigatório) + AO LADO a data por extenso: caption "vence {dd/mm}" + "confere com a etiqueta que você cola no doce".
   - **Resumo antes de confirmar**: "Esse lote custou {R$ X,XX} — {R$ X,XX} por unidade." — calculado no client via decimal.js (Σ qtde escalada × custoPorUnidadeBase + gás, ÷ rendimento real digitado); é preview — o server recomputa.
   - **CTA sticky "Registrar produção"** → `produzirLote({ produtoId, receitaId, multiplicador, rendimentoReal, validade, linhas: [{ ingredienteCompraId, qtde: qtdeEscalada }] })`. Sucesso: toast "Lote registrado! O custo ficou guardado do jeitinho que foi hoje." + router.push('/admin/lotes'). Erro: Alert com `error` (inclui o caso "Os dados mudaram — recarrega a página e tenta de novo.").
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "Quantas unidades saíram de verdade" components/admin/produzir-lote-form.tsx && grep -c "Registrar produção" components/admin/produzir-lote-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Copy exata: "Qual receita você fez", "Quantas receitas", "Ingredientes que você usou", "Trocar compra", "Qual compra você usou?", "confere com a etiqueta que você cola no doce", "O custo ficou guardado do jeitinho que foi hoje."
    - `grep -c "new Decimal" components/admin/produzir-lote-form.tsx` ≥ 1 e `grep -c "type=\"number\"" ...` == 0
    - Payload de produzirLote contém só IDs + quantidades (grep confirma ausência de campo custo no objeto enviado)
    - Data resolvida por extenso ao lado do input (grep "vence")
    - `rtk npm test` continua verde
  </acceptance_criteria>
  <done>Fluxo de produção completo: 1 toque no caso comum, fidelidade quando importa (troca de compra).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → produzirLote | Client envia IDs+quantidades; custo é preview local descartado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | resumo de custo no client | mitigate | Server recomputa dentro da transação (02-07, testado por LOTE-08); preview nunca enviado |
| T-02-18 | Information Disclosure | custos nas páginas de lote | mitigate | Rotas sob (admin) com role-gate |
</threat_model>

<verification>
- `rtk npx tsc --noEmit` — 0 erros
- `rtk npm test` — suite completa verde (incl. LOTE-08)
- Verificação visual do fluxo no checkpoint do 02-11
</verification>

<success_criteria>
- Surface 12 fiel ao UI-SPEC: pré-seleção D-05, multiplicador D-07 reescalando ao vivo, rendimento real D-06, validade sugerida D-08 com data por extenso
- Lista com 3 filtros e badges corretos
- Nenhum custo confiado do client
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-financeiro/02-10-SUMMARY.md`
</output>
