---
phase: 02-motor-financeiro
plan: 07
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - lib/validation/lotes.ts
  - lib/actions/lotes.ts
  - lib/lotes/queries.ts
  - tests/financeiro/custo-congelado.test.ts
  - tests/financeiro/lotes.test.ts
autonomous: true
requirements: [LOTE-01, LOTE-02, LOTE-03, LOTE-04, LOTE-07, LOTE-08]
must_haves:
  truths:
    - "Produzir lote grava lote_uso_ingredientes com FK pra COMPRA específica + marca_snapshot + custo_congelado em UMA transação"
    - "LOTE-08: mudar o preço corrente do ingrediente DEPOIS não altera o custo congelado do lote antigo (teste de regressão — success criterion 5 do roadmap)"
    - "Custo por unidade do lote usa o rendimento REAL informado (D-06), quantidades escaladas pelo multiplicador (D-07)"
    - "Filtros vigentes/vencidos/esgotados comparam validade com 'hoje' em America/Sao_Paulo, não UTC"
    - "A action recomputa TUDO server-side — o client manda só {ingredienteCompraId, qtde}, nunca custos"
  artifacts:
    - path: "lib/actions/lotes.ts"
      provides: "produzirLote (transação nested create) + reads p/ o form de produção (D-05)"
    - path: "lib/lotes/queries.ts"
      provides: "hojeSaoPaulo(), filtroLotes (vigentes/vencidos/esgotados)"
    - path: "tests/financeiro/custo-congelado.test.ts"
      provides: "Teste de regressão LOTE-08 (MANDATÓRIO)"
      contains: "LOTE-08"
  key_links:
    - from: "lib/actions/lotes.ts"
      to: "lib/custo/congelado.ts"
      via: "computeLoteSnapshot dentro da transação"
      pattern: "computeLoteSnapshot"
    - from: "lote_uso_ingredientes"
      to: "ingrediente_compras"
      via: "ingredienteCompraId (nunca ingredienteId)"
      pattern: "ingredienteCompraId"
---

<objective>
O coração da fase: a action transacional de produção de lote que congela o custo (LOTE-01..04), o teste de regressão LOTE-08 (escrito PRIMEIRO — success criterion 5), e as queries de filtro com timezone correto (LOTE-07).

Purpose: é aqui que o Pitfall 5.5 morre de vez — custo congelado por FK à compra específica, recomputado server-side, imutável após INSERT.
Output: action + queries + 2 suites de teste verdes (custo-congelado e lotes).
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-financeiro/02-RESEARCH.md
@.planning/phases/02-motor-financeiro/02-PATTERNS.md

⚠️ Pitfall 2: depois do INSERT o lote NUNCA lê a receita para nada financeiro — tudo congelado em colunas próprias (qtde_usada já escalada, custo_gas_congelado, rendimento_real).
⚠️ Pitfall 10: `validade` é `@db.Date`; "hoje" derivado explicitamente em America/Sao_Paulo — nunca `new Date()` UTC cru na comparação.
⚠️ Test DB == dev DB: rodar vitest apaga dados do dev.
</context>

<interfaces>
Do 02-03: `computeLoteSnapshot({ linhas, custoGas, rendimentoReal })` → `{ usos[], custoTotalCongelado, custoPorUnidadeCongelado, custoGasCongelado }` (strings prontas pro create); `ultimasCompras(ids)`; `requireAdmin()`; `zQtdeBRL`; fixtures.

Nested create alvo (RESEARCH Pattern 4 — a action implementa exatamente isto):
```typescript
const lote = await prisma.lote.create({
  data: {
    produtoId, receitaId, multiplicador, rendimentoReal, validade,
    qtdeDisponivel: rendimentoReal, qtdeReservada: 0,
    custoGasCongelado: snapshot.custoGasCongelado,      // snapshot! receita é mutável
    custoTotalCongelado: snapshot.custoTotalCongelado,
    custoPorUnidadeCongelado: snapshot.custoPorUnidadeCongelado, // D-06: rendimento REAL
    usos: { create: snapshot.usos },                     // LOTE-02: FK pra COMPRA
  },
})
await logAudit({ actorType: 'admin', actorId, action: 'lote_criado', entityType: 'lote', entityId: lote.id, rawIp, rawUa })
```

Esqueleto LOTE-08 (RESEARCH §Code Examples — implementar com as fixtures reais):
```typescript
it('LOTE-08: custo congelado sobrevive a mudança de preço corrente', async () => {
  // compra A: Moça 1×395g a R$3,95 → 0.010000/g; receita 395g rend 20; produzirLote
  // nova compra mais cara (Italac 7,90) muda o preço CORRENTE
  // rows de lote_uso_ingredientes: custoUnitarioCongelado.toFixed(6) === '0.010000'; marcaSnapshot === 'Moça'
  // UPDATE na compra A rejeita /imutavel/; compra NÃO referenciada continua editável (D-03)
})
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LOTE-08 primeiro + action produzirLote (transação com snapshot server-side)</name>
  <files>tests/financeiro/custo-congelado.test.ts, lib/validation/lotes.ts, lib/actions/lotes.ts</files>
  <read_first>
    - lib/custo/congelado.ts e lib/custo/corrente.ts (assinaturas reais)
    - tests/financeiro/fixtures.ts (produzirLote da fixture — a action é a versão com requireAdmin/audit do mesmo fluxo)
    - tests/auth/password-reset.test.ts (mocks 01-08)
    - lib/actions/auth.ts (skeleton)
  </read_first>
  <behavior>
    - RED primeiro: escrever tests/financeiro/custo-congelado.test.ts com o caso LOTE-08 do bloco interfaces CHAMANDO A ACTION produzirLote (não a fixture) — commit de teste falhando, depois implementar
    - LOTE-08 completo: custo congelado intacto após nova compra mais cara; marcaSnapshot preservada; UPDATE na compra referenciada rejeita /imutavel/; compra não referenciada segue editável
    - Snapshot é recomputado server-side: chamar produzirLote com um custo forjado no payload não muda nada (o input nem tem campo de custo)
    - D-06: rendimentoReal 18 com receita de rendimento 20 → custoPorUnidadeCongelado = total ÷ 18
    - D-07: multiplicador '1,5' escala qtdeUsada das linhas (395 → 592.500)
    - D-05: linha com compra trocada (não a última) grava a compra escolhida e a marca dela
    - EoP: sessão customer → { error }, nenhum lote criado
  </behavior>
  <action>
1. `lib/validation/lotes.ts`:
```typescript
export const ProduzirLoteSchema = z.object({
  produtoId: z.string().uuid(),
  receitaId: z.string().uuid(),
  multiplicador: zQtdeBRL.refine(v => v.gt(0), 'O multiplicador precisa ser maior que zero.'),
  rendimentoReal: z.coerce.number().int().min(1, 'Precisa ter saído pelo menos 1 unidade.'),
  validade: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, OBRIGATORIO),
  linhas: z.array(z.object({ ingredienteCompraId: z.string().uuid(), qtde: zQtdeBRL })).min(1),
})
```
NOTA: `linhas[].qtde` é a quantidade JÁ ESCALADA exibida no form (o client escala pelo multiplicador para a mãe conferir); a action NÃO confia nela para custo — ver passo 2.
2. `lib/actions/lotes.ts` (`'use server'`):
   - `produzirLote(input: unknown)`: skeleton (context → rate limit → requireAdmin → safeParse). DENTRO de `prisma.$transaction`: (a) carregar as compras referenciadas (`ingredienteCompra.findMany({ where: { id: { in: linhas.map(l => l.ingredienteCompraId) } } })`) e validar que cobrem exatamente os ingredientes da receita (cada compra pertence a um ingrediente da receita; sem ingrediente faltando — comparar sets); (b) recomputar qtdeUsada server-side: `qtde da receita × multiplicador` (Decimal) — usar a qtde do client apenas para validação de igualdade (se divergir, retornar erro genérico de dados desatualizados: "Os dados mudaram — recarrega a página e tenta de novo."); (c) `computeLoteSnapshot({ linhas: [{compra, qtdeUsada}], custoGas: receita.custoGas ?? 0, rendimentoReal })`; (d) nested create verbatim do bloco interfaces; validade como Date do string `yyyy-mm-dd`. Depois do commit: `logAudit lote_criado` + `revalidatePath('/admin/lotes')`. Retorno `{ ok: true, id, custoTotal: string, custoPorUnidade: string }` (strings — Pitfall 3).
   - Reads para o form de produção (D-05), funções de args simples no mesmo arquivo: `dadosProducao(receitaId: string)` → receita com itens + `ultimasCompras` pré-selecionadas, serializado (Decimal→string): `{ receita: { id, nome, rendimentoPadrao, validadeDias, custoGas: string|null }, linhas: [{ ingredienteId, nome, unidadeBase, qtdeBase: string, compraSelecionada: { id, marca, dataCompra: string, custoPorUnidadeBase: string } | null }] }`; e `comprasDoIngrediente(ingredienteId: string)` → últimas 20 compras (id, marca, dataCompra, custoPorUnidadeBase como string) mais recente primeiro, para o combobox "Trocar compra". Ambas com requireAdmin (catch → null/[]).
3. Escrever `tests/financeiro/custo-congelado.test.ts` ANTES da action (RED→GREEN), padrão 01-08 com sessão admin mockada. Implementar todos os casos do behavior.
  </action>
  <verify>
    <automated>rtk npx vitest run tests/financeiro/custo-congelado.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LOTE-08" tests/financeiro/custo-congelado.test.ts` ≥ 1 e contém `'0.010000'`, `'Moça'` e `/imutavel/`
    - `grep -c "computeLoteSnapshot" lib/actions/lotes.ts` == 1 e `grep -c "\$transaction" lib/actions/lotes.ts` ≥ 1
    - `grep -c "ingredienteId" lib/actions/lotes.ts` — o create de usos NÃO contém ingredienteId (FK é ingredienteCompraId; grep "usos" -A3 mostra ingredienteCompraId)
    - Histórico git mostra commit de teste antes do commit de implementação (test(02-07) → feat(02-07))
  </acceptance_criteria>
  <done>LOTE-08 verde chamando a action real; lote congela custo em transação com recomputação server-side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Queries de filtro com TZ (LOTE-07) + testes de lote (CHECK, filtros, multiplicador)</name>
  <files>lib/lotes/queries.ts, tests/financeiro/lotes.test.ts</files>
  <read_first>
    - lib/actions/lotes.ts (recém-criado)
    - tests/financeiro/fixtures.ts
    - .planning/phases/02-motor-financeiro/02-RESEARCH.md §Pitfall 10 (validade UTC)
  </read_first>
  <behavior>
    - hojeSaoPaulo() retorna a data civil corrente em America/Sao_Paulo como 'yyyy-mm-dd' (via Intl.DateTimeFormat('en-CA', { timeZone: 'America/Sao_Paulo' }) ou date-fns equivalente)
    - Filtro vigentes: validade ≥ hoje E qtdeDisponivel > 0; vencidos: validade < hoje; esgotados: qtdeDisponivel == 0 E validade ≥ hoje
    - Lote com validade == hoje é VIGENTE (vence hoje, ainda vende)
    - LOTE-04: `$executeRaw` UPDATE qtde_disponivel = -1 rejeita com CHECK violation
    - Multiplicador: produzirLote com multiplicador '1,5' → usos com qtdeUsada '592.500' (395 × 1.5)
    - LOTE-02/03: usos gravados com marcaSnapshot e custoUnitarioCongelado corretos
  </behavior>
  <action>
1. Criar `lib/lotes/queries.ts` (server module):
   - `hojeSaoPaulo(): string` — data civil BRT em 'yyyy-mm-dd' (documentar Pitfall 10 no doc-comment).
   - `type FiltroLote = 'vigentes' | 'vencidos' | 'esgotados'`
   - `listarLotes(filtro: FiltroLote)` — `prisma.lote.findMany` com `include: { produto: { select: { nome: true } } }` (anti N+1), where derivado do behavior acima, `orderBy: { validade: 'asc' }` para vigentes / `{ validade: 'desc' }` para vencidos. Retorna rows (a page do 02-10 serializa).
2. Criar `tests/financeiro/lotes.test.ts` (integração, beforeEach truncateAll, fixtures + action) cobrindo todos os casos do behavior. Para testar filtros de data sem viajar no tempo: inserir lotes com validade ontem/hoje/amanhã relativas a `hojeSaoPaulo()`.
  </action>
  <verify>
    <automated>rtk npx vitest run tests/financeiro/lotes.test.ts --reporter=dot && rtk npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "America/Sao_Paulo" lib/lotes/queries.ts` ≥ 1
    - lotes.test.ts contém `'592.500'` e um caso de CHECK (`rejects` em qtde negativa)
    - `grep -c "include" lib/lotes/queries.ts` ≥ 1
    - Suite completa verde
  </acceptance_criteria>
  <done>Filtros LOTE-07 corretos em BRT e provados; CHECK LOTE-04 provado via caminho real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → produzirLote | Client envia IDs + quantidades para conferência; custos NUNCA cruzam a fronteira para dentro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege | produzirLote, dadosProducao, comprasDoIngrediente | mitigate | requireAdmin() em todas; teste EoP na Task 1 |
| T-02-13 | Tampering | custo forjado pelo client | mitigate | Input não tem campo de custo; snapshot recomputado das rows FK'adas dentro da transação; qtde do client só validada por igualdade contra receita×multiplicador |
| T-02-01 | Tampering | histórico congelado | mitigate | LOTE-08 como regressão permanente na suite; trigger (02-01) cobre acesso direto |
| T-02-16 | Repudiation | criação de lote | mitigate | logAudit lote_criado com entityId + IP/UA hasheados (AUTH-11) |
</threat_model>

<verification>
- `rtk npx vitest run tests/financeiro --reporter=dot` — verde (custo-congelado + lotes + anteriores)
- `rtk npm test` — suite completa verde
- `rtk npx tsc --noEmit` — 0 erros
</verification>

<success_criteria>
- LOTE-08 passa chamando a Server Action real (não só a fixture) — success criterion 5 do roadmap
- Lote + usos numa única transação; FK pra compra; snapshot em colunas tipadas
- D-05/D-06/D-07 implementados exatamente (troca de compra, rendimento real, multiplicador)
- Filtros LOTE-07 com data civil BRT
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-financeiro/02-07-SUMMARY.md`
Anotar handoff: LOTE-05 (cron de vencidos) usa `listarLotes('vencidos')`/hojeSaoPaulo na Phase 5.
</output>
