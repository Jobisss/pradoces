---
phase: 02-motor-financeiro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_motor_financeiro/migration.sql
  - tests/setup.ts
  - tests/conftest.ts
  - tests/financeiro/schema.test.ts
autonomous: true
requirements: [ING-04, ING-07, LOTE-02, LOTE-04, LOTE-05, LOTE-06]
must_haves:
  truths:
    - "Os 9 models de domínio existem no banco de dev (migration aplicada, não só no schema file)"
    - "UPDATE/DELETE numa compra referenciada por lote é bloqueado pelo Postgres (trigger), não por convenção"
    - "UPDATE que negativa qtde_disponivel ou qtde_reservada aborta (CHECK)"
    - "Rodar a suite vitest não deixa rows órfãs dos models novos (harness truncates children-first)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "9 models (Ingrediente, IngredienteCompra, Receita, ReceitaIngrediente, Produto, ProdutoKitItem, Lote, LoteUsoIngrediente, Configuracao) + 3 enums"
      contains: "model LoteUsoIngrediente"
    - path: "prisma/migrations"
      provides: "Migration com SQL custom (trigger trg_compra_imutavel, CHECKs LOTE-04, singleton configuracoes)"
    - path: "tests/financeiro/schema.test.ts"
      provides: "Smoke test do trigger e dos CHECKs contra o Postgres real"
    - path: "tests/setup.ts"
      provides: "deleteMany dos 9 models novos children-first"
  key_links:
    - from: "prisma/migrations/*_motor_financeiro/migration.sql"
      to: "banco de dev"
      via: "prisma migrate dev (aplicada de verdade, não db push)"
      pattern: "CREATE TRIGGER trg_compra_imutavel"
    - from: "lotes.qtde_disponivel"
      to: "CHECK constraint"
      via: "ALTER TABLE lotes ADD CONSTRAINT"
      pattern: "lotes_qtde_disponivel_nao_negativa"
---

<objective>
Criar a fundação de dados do motor financeiro: 9 models Prisma + 3 enums, migration com SQL custom (trigger de imutabilidade de compra ING-07, CHECKs de estoque LOTE-04, singleton de configuração), aplicação REAL da migration no banco de dev, e atualização do harness de testes para os models novos.

Purpose: todo o resto da fase (actions, lib/custo, UI) compila e testa contra este schema. O invariante central do produto — custo congelado enforçado no schema, não em convenção (Pitfall 5.5) — nasce aqui.
Output: schema migrado + trigger/CHECKs vivos no Postgres + harness de teste isolando os models novos + smoke test verde.

**LOTE-06 (validade imutável após primeira reserva ativa):** satisfeito vacuamente nesta fase — reservas só existem na Phase 4; não há surface de edição de validade de lote na v1 desta fase. Registrar no SUMMARY como handoff explícito: "Phase 4 adiciona trigger/check de validade junto com a tabela de reservas".
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-motor-financeiro/02-RESEARCH.md
@.planning/phases/02-motor-financeiro/02-PATTERNS.md

⚠️ CRITICAL (memória do projeto): **o banco de teste É o banco de dev** (mesmo DATABASE_URL). Rodar vitest apaga dados manuais do dev. Não manter dados de exercício no dev DB durante a execução.

⚠️ **NUNCA usar `prisma db push`** — ele diffa direto do schema e NÃO executa o SQL custom (trigger/CHECKs somem silenciosamente). Fluxo obrigatório: `migrate dev --create-only` → anexar SQL → `migrate dev`.

⚠️ O hook rtk intercepta `npx prisma` com wrapper próprio — para flags como `--create-only`, invocar via npm script (`rtk npm run db:migrate:dev -- --create-only --name motor_financeiro`) ou binário direto (`node node_modules/prisma/build/index.js ...`).
</context>

<interfaces>
Convenções existentes em prisma/schema.prisma (header, linhas 1–12) — repetir nos models novos:
- PK: `String @id @default(uuid()) @db.Uuid`
- snake_case via `@@map`/`@map`
- Timestamps: `criadoEm DateTime @default(now()) @db.Timestamptz(6) @map("criado_em")` / `atualizadoEm DateTime @updatedAt @db.Timestamptz(6) @map("atualizado_em")`
- Money: `Decimal @db.Decimal(19, 4)`; custo por unidade-base: `@db.Decimal(19, 6)`
- Índice descendente (analog AuditLog): `@@index([campo(sort: Desc)])`

Harness atual (tests/setup.ts linhas 19–27 e tests/conftest.ts#truncateAll) — MESMA lista nos dois arquivos:
```typescript
await prisma.$transaction([
  prisma.session.deleteMany(),
  prisma.account.deleteMany(),
  prisma.verification.deleteMany(),
  prisma.auditLog.deleteMany(),
  prisma.user.deleteMany(),
])
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar 9 models + 3 enums ao schema e gerar migration com SQL custom (sem aplicar)</name>
  <files>prisma/schema.prisma, prisma/migrations/*_motor_financeiro/migration.sql</files>
  <read_first>
    - prisma/schema.prisma (convenções do header + models User/Session/AuditLog como analog)
    - prisma/migrations/20260629133830_init/migration.sql (layout do SQL gerado)
    - .planning/phases/02-motor-financeiro/02-RESEARCH.md §"Shape de schema recomendado" e §"Migration SQL custom"
  </read_first>
  <action>
Adicionar ao `prisma/schema.prisma` (mantendo as convenções do header do arquivo) os enums e models abaixo. Este shape vem do RESEARCH (Claude's Discretion refinada — pode ajustar nomes de relação/back-relations para o Prisma validar, mas NÃO mudar: tipos de coluna, precisões Decimal, @map/@@map, índices, onDelete):

```prisma
enum UnidadeBase { g  ml  un }
enum TipoIngrediente { INGREDIENTE  EMBALAGEM }   // ING-06
enum TipoProduto { UNITARIO  KIT }                 // PROD-01

model Ingrediente {
  id           String            @id @default(uuid()) @db.Uuid
  nome         String            @unique
  unidadeBase  UnidadeBase       @map("unidade_base")
  tipo         TipoIngrediente   @default(INGREDIENTE)
  criadoEm     DateTime          @default(now()) @db.Timestamptz(6) @map("criado_em")
  atualizadoEm DateTime          @updatedAt @db.Timestamptz(6) @map("atualizado_em")
  compras      IngredienteCompra[]
  receitaItens ReceitaIngrediente[]
  @@map("ingredientes")
}

model IngredienteCompra {
  id                  String   @id @default(uuid()) @db.Uuid
  ingredienteId       String   @db.Uuid @map("ingrediente_id")
  dataCompra          DateTime @db.Date @map("data_compra")
  mercado             String
  marca               String
  qtdeEmbalagens      Decimal  @db.Decimal(10, 3) @map("qtde_embalagens")
  tamanhoEmbalagem    Decimal  @db.Decimal(12, 3) @map("tamanho_embalagem")
  precoPorEmbalagem   Decimal  @db.Decimal(19, 4) @map("preco_por_embalagem")
  qtdeTotalBase       Decimal  @db.Decimal(14, 3) @map("qtde_total_base")
  precoTotal          Decimal  @db.Decimal(19, 4) @map("preco_total")
  custoPorUnidadeBase Decimal  @db.Decimal(19, 6) @map("custo_por_unidade_base")
  criadoEm            DateTime @default(now()) @db.Timestamptz(6) @map("criado_em")
  ingrediente Ingrediente @relation(fields: [ingredienteId], references: [id], onDelete: Restrict)
  usos        LoteUsoIngrediente[]
  @@index([ingredienteId, dataCompra(sort: Desc)])
  @@map("ingrediente_compras")
}

model Receita {
  id               String   @id @default(uuid()) @db.Uuid
  nome             String
  rendimentoPadrao Int      @map("rendimento_padrao")
  custoGas         Decimal? @db.Decimal(19, 4) @map("custo_gas")
  validadeDias     Int?     @map("validade_dias")
  criadoEm         DateTime @default(now()) @db.Timestamptz(6) @map("criado_em")
  atualizadoEm     DateTime @updatedAt @db.Timestamptz(6) @map("atualizado_em")
  itens   ReceitaIngrediente[]
  produto Produto?
  lotes   Lote[]
  @@map("receitas")
}

model ReceitaIngrediente {
  receitaId     String  @db.Uuid @map("receita_id")
  ingredienteId String  @db.Uuid @map("ingrediente_id")
  qtde          Decimal @db.Decimal(14, 3)
  receita     Receita     @relation(fields: [receitaId], references: [id], onDelete: Cascade)
  ingrediente Ingrediente @relation(fields: [ingredienteId], references: [id], onDelete: Restrict)
  @@id([receitaId, ingredienteId])
  @@map("receita_ingredientes")
}

model Produto {
  id                   String      @id @default(uuid()) @db.Uuid
  nome                 String
  descricao            String
  categoria            String
  tipo                 TipoProduto
  precoVenda           Decimal     @db.Decimal(19, 4) @map("preco_venda")
  margemMinimaOverride Decimal?    @db.Decimal(5, 2) @map("margem_minima_override")
  receitaId            String?     @unique @db.Uuid @map("receita_id")
  criadoEm             DateTime    @default(now()) @db.Timestamptz(6) @map("criado_em")
  atualizadoEm         DateTime    @updatedAt @db.Timestamptz(6) @map("atualizado_em")
  receita  Receita? @relation(fields: [receitaId], references: [id], onDelete: Restrict)
  kitItens ProdutoKitItem[] @relation("kit")
  emKits   ProdutoKitItem[] @relation("componente")
  lotes    Lote[]
  @@map("produtos")
}

model ProdutoKitItem {
  kitId        String @db.Uuid @map("kit_id")
  componenteId String @db.Uuid @map("componente_id")
  qtde         Int
  kit        Produto @relation("kit", fields: [kitId], references: [id], onDelete: Cascade)
  componente Produto @relation("componente", fields: [componenteId], references: [id], onDelete: Restrict)
  @@id([kitId, componenteId])
  @@map("produto_kit_items")
}

model Lote {
  id                       String   @id @default(uuid()) @db.Uuid
  produtoId                String   @db.Uuid @map("produto_id")
  receitaId                String   @db.Uuid @map("receita_id")
  multiplicador            Decimal  @db.Decimal(6, 2)
  rendimentoReal           Int      @map("rendimento_real")
  produzidoEm              DateTime @default(now()) @db.Timestamptz(6) @map("produzido_em")
  validade                 DateTime @db.Date
  qtdeDisponivel           Int      @map("qtde_disponivel")
  qtdeReservada            Int      @default(0) @map("qtde_reservada")
  custoGasCongelado        Decimal  @db.Decimal(19, 4) @map("custo_gas_congelado")
  custoTotalCongelado      Decimal  @db.Decimal(19, 4) @map("custo_total_congelado")
  custoPorUnidadeCongelado Decimal  @db.Decimal(19, 6) @map("custo_por_unidade_congelado")
  criadoEm                 DateTime @default(now()) @db.Timestamptz(6) @map("criado_em")
  produto Produto @relation(fields: [produtoId], references: [id], onDelete: Restrict)
  receita Receita @relation(fields: [receitaId], references: [id], onDelete: Restrict)
  usos    LoteUsoIngrediente[]
  @@index([validade])
  @@map("lotes")
}

model LoteUsoIngrediente {
  id                     String  @id @default(uuid()) @db.Uuid
  loteId                 String  @db.Uuid @map("lote_id")
  ingredienteCompraId    String  @db.Uuid @map("ingrediente_compra_id")
  qtdeUsada              Decimal @db.Decimal(14, 3) @map("qtde_usada")
  marcaSnapshot          String  @map("marca_snapshot")
  custoUnitarioCongelado Decimal @db.Decimal(19, 6) @map("custo_unitario_congelado")
  custoCongelado         Decimal @db.Decimal(19, 4) @map("custo_congelado")
  lote   Lote              @relation(fields: [loteId], references: [id], onDelete: Cascade)
  compra IngredienteCompra @relation(fields: [ingredienteCompraId], references: [id], onDelete: Restrict)
  @@index([ingredienteCompraId])
  @@map("lote_uso_ingredientes")
}

model Configuracao {
  id                 Int     @id @default(1)
  margemMinimaPadrao Decimal @default(30) @db.Decimal(5, 2) @map("margem_minima_padrao")
  @@map("configuracoes")
}
```

CRITICAL: `LoteUsoIngrediente.ingredienteCompraId` é NOT NULL com FK para `ingrediente_compras` (a COMPRA, nunca o ingrediente — Pitfall 5.5 / LOTE-02). `onDelete: Restrict` na relação compra.

Depois, gerar a migration SEM aplicar:
```bash
rtk npm run db:migrate:dev -- --create-only --name motor_financeiro
```
(se o wrapper rtk engolir as flags, fallback: `node node_modules/prisma/build/index.js migrate dev --create-only --name motor_financeiro`)

Anexar ao FIM do `migration.sql` gerado este SQL custom VERBATIM (RESEARCH §Code Examples):

```sql
-- ING-07 / D-03: compra vira imutável a partir do 1º lote que a referencia.
CREATE OR REPLACE FUNCTION bloquear_compra_referenciada() RETURNS trigger AS $$
BEGIN
  IF EXISTS (
    SELECT 1 FROM lote_uso_ingredientes WHERE ingrediente_compra_id = OLD.id
  ) THEN
    RAISE EXCEPTION 'compra % imutavel: referenciada por lote (custo congelado)', OLD.id;
  END IF;
  IF (TG_OP = 'DELETE') THEN RETURN OLD; END IF;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_compra_imutavel
  BEFORE UPDATE OR DELETE ON "ingrediente_compras"
  FOR EACH ROW EXECUTE FUNCTION bloquear_compra_referenciada();

-- LOTE-04: estoque nunca negativo (defesa contra race da Phase 4 também)
ALTER TABLE "lotes" ADD CONSTRAINT lotes_qtde_disponivel_nao_negativa CHECK (qtde_disponivel >= 0);
ALTER TABLE "lotes" ADD CONSTRAINT lotes_qtde_reservada_nao_negativa  CHECK (qtde_reservada  >= 0);

-- Config single-row (D-10)
ALTER TABLE "configuracoes" ADD CONSTRAINT configuracoes_singleton CHECK (id = 1);
```
  </action>
  <verify>
    <automated>grep -c "trg_compra_imutavel" prisma/migrations/*motor_financeiro/migration.sql && grep -c "lotes_qtde_disponivel_nao_negativa" prisma/migrations/*motor_financeiro/migration.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "model " prisma/schema.prisma` retorna ≥ 14 (5 da Phase 1 + 9 novos)
    - `grep "ingrediente_compra_id" prisma/migrations/*motor_financeiro/migration.sql` mostra coluna NOT NULL com FK REFERENCES "ingrediente_compras"
    - migration.sql contém as 3 strings exatas: `trg_compra_imutavel`, `lotes_qtde_reservada_nao_negativa`, `configuracoes_singleton`
    - `grep -c "DECIMAL(19,4)" prisma/migrations/*motor_financeiro/migration.sql` ≥ 5 e `grep -c "DECIMAL(19,6)" ...` ≥ 3 (zero float/real/double: `grep -ciE "real|double precision|float" migration.sql` == 0)
    - Migration ainda NÃO aplicada (é --create-only; a aplicação é a Task 2)
  </acceptance_criteria>
  <done>Schema com 9 models novos validando (`node node_modules/prisma/build/index.js validate` sai 0) e migration.sql gerada contendo o SQL custom anexado.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar a migration no banco de dev e regenerar o client</name>
  <files>prisma/migrations/ (estado aplicado)</files>
  <read_first>
    - prisma/migrations/*motor_financeiro/migration.sql (confirmar SQL custom presente ANTES de aplicar)
  </read_first>
  <action>
Aplicar a migration de verdade (build/tsc passam sem o banco atualizado — o falso-positivo clássico; esta task elimina isso):

```bash
rtk npm run db:migrate:dev
```

Se `migrate dev` pedir reset por drift, NÃO aceitar reset silencioso sem investigar — o banco de dev é também o de teste. Depois de aplicar, garantir client regenerado (migrate dev já gera; se necessário: `node node_modules/prisma/build/index.js generate`).

Verificar que o trigger e os CHECKs chegaram no banco VIVO (não só no arquivo), via query nos catálogos do Postgres.
  </action>
  <verify>
    <automated>node node_modules/prisma/build/index.js migrate status 2>&1 | grep -i "up to date\|database schema is up to date" && rtk npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `prisma migrate status` reporta schema em dia (nenhuma migration pendente)
    - `rtk npx tsc --noEmit` sai 0 com os types novos (ex.: `prisma.loteUsoIngrediente` existe no client)
    - Task 3 (smoke test) prova trigger/CHECK no banco vivo — se o smoke test falhar com "trigger does not exist", esta task foi feita com db push ou o SQL custom não foi anexado: refazer
  </acceptance_criteria>
  <done>Migration aplicada no Postgres de dev, client Prisma 7 regenerado, tsc limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Atualizar harness de testes (children-first) + smoke test de schema no Postgres real</name>
  <files>tests/setup.ts, tests/conftest.ts, tests/financeiro/schema.test.ts</files>
  <read_first>
    - tests/setup.ts (lista atual de deleteMany, linhas 19–27)
    - tests/conftest.ts (função truncateAll — mesma lista)
    - tests/auth/admin-guard.test.ts (analog de teste de integração com DB real)
  </read_first>
  <behavior>
    - Test 1 (ING-07/trigger): criar via prisma direto a cadeia ingrediente → compra → receita → produto → lote → loteUsoIngrediente; `prisma.ingredienteCompra.update` na compra referenciada rejeita com /imutavel/; `delete` também rejeita
    - Test 2 (D-03): compra NÃO referenciada aceita update e delete normalmente
    - Test 3 (LOTE-04): `prisma.$executeRaw` UPDATE lotes SET qtde_disponivel = -1 rejeita (CHECK violation); mesmo para qtde_reservada
    - Test 4 (singleton): INSERT em configuracoes com id=2 rejeita (CHECK configuracoes_singleton)
    - Test 5 (catálogo): `SELECT tgname FROM pg_trigger WHERE tgname = 'trg_compra_imutavel'` retorna 1 row
  </behavior>
  <action>
1. Em `tests/setup.ts` (bloco afterEach) E `tests/conftest.ts#truncateAll` (as DUAS listas ficam idênticas), PREPENDER os deleteMany dos models novos ANTES dos da Phase 1, nesta ordem exata children-first (Pitfall 5):
```typescript
prisma.loteUsoIngrediente.deleteMany(),
prisma.lote.deleteMany(),
prisma.produtoKitItem.deleteMany(),
prisma.produto.deleteMany(),
prisma.receitaIngrediente.deleteMany(),
prisma.receita.deleteMany(),
prisma.ingredienteCompra.deleteMany(),
prisma.ingrediente.deleteMany(),
prisma.configuracao.deleteMany(),
// ...depois os 5 da Phase 1 já existentes
```
2. Criar `tests/financeiro/schema.test.ts` implementando os 5 comportamentos do bloco behavior, usando `prisma` direto de `@/lib/db/client` (sem fixtures — elas nascem no plano 02-03) e `beforeEach(truncateAll)` importado de `../conftest`. Valores Decimal passados como string (ex.: `precoPorEmbalagem: '5.80'`). Asserções de erro do trigger: `await expect(...).rejects.toThrow(/imutavel/)`.
3. NÃO usar watch mode. Comparações de Decimal por `.toFixed(n)` string (Pitfall 8).
  </action>
  <verify>
    <automated>rtk npx vitest run tests/financeiro/schema.test.ts --reporter=dot && rtk npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "loteUsoIngrediente.deleteMany" tests/setup.ts` == 1 e `grep -c "loteUsoIngrediente.deleteMany" tests/conftest.ts` == 1
    - Em ambos os arquivos, a linha `loteUsoIngrediente.deleteMany` aparece ANTES de `lote.deleteMany`, que aparece antes de `produto.deleteMany` (ordem children-first verificável por número de linha)
    - `tests/financeiro/schema.test.ts` contém `rejects.toThrow(/imutavel/)` e `trg_compra_imutavel`
    - Suite completa verde: os 73/75 testes da Phase 1 continuam passando + schema.test.ts passa
  </acceptance_criteria>
  <done>Harness isola os 9 models novos; trigger, CHECKs e singleton provados contra o Postgres vivo; suite completa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Postgres | Qualquer código de app (ou Prisma Studio, ou script manual) pode tentar reescrever histórico financeiro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | ingrediente_compras (histórico financeiro) | mitigate | Trigger `trg_compra_imutavel` BEFORE UPDATE OR DELETE + FK RESTRICT em lote_uso_ingredientes — sobrevive a bug de app/acesso direto |
| T-02-02 | Tampering | lotes.qtde_disponivel/qtde_reservada | mitigate | CHECK ≥ 0 no schema (transação aborta mesmo com race/bug — base para Phase 4) |
| T-02-03 | Tampering | configuracoes | mitigate | CHECK (id = 1) impede rows extras que confundiriam o threshold de margem |
</threat_model>

<verification>
- `node node_modules/prisma/build/index.js migrate status` — schema em dia
- `rtk npx tsc --noEmit` — 0 erros
- `rtk npm test` — suite completa verde (Phase 1 + schema.test.ts)
- `grep -c "CREATE TRIGGER trg_compra_imutavel" prisma/migrations/*motor_financeiro/migration.sql` == 1
</verification>

<success_criteria>
- 9 models + 3 enums no schema seguindo convenções Phase 1 (uuid PK, snake_case, Timestamptz(6), Decimal 19,4/19,6)
- Migration aplicada no banco vivo com trigger + 3 CHECKs (provado por teste, não por leitura de arquivo)
- FK de lote_uso_ingredientes aponta para a COMPRA (ingrediente_compra_id NOT NULL, onDelete Restrict)
- Harness de testes atualizado nos DOIS arquivos, children-first
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-financeiro/02-01-SUMMARY.md`
Incluir no SUMMARY: handoff LOTE-06 para Phase 4 (trigger de validade imutável entra junto com a tabela de reservas).
</output>
