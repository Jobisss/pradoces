---
phase: 02-motor-financeiro
plan: 08
type: execute
wave: 4
depends_on: ["02-02", "02-04"]
files_modified:
  - app/(admin)/admin/ingredientes/page.tsx
  - app/(admin)/admin/ingredientes/novo/page.tsx
  - app/(admin)/admin/ingredientes/[id]/editar/page.tsx
  - app/(admin)/admin/ingredientes/[id]/page.tsx
  - components/admin/ingrediente-form.tsx
  - components/admin/compra-acoes.tsx
  - app/(admin)/admin/compras/nova/page.tsx
  - components/admin/mercado-flow.tsx
autonomous: true
requirements: [ING-01, ING-02, ING-06, ING-08]
must_haves:
  truths:
    - "Mãe cadastra ingrediente (e embalagem pelo MESMO form) em 3 campos"
    - "Detalhe do ingrediente mostra a última compra destacada + histórico cronológico (ING-08); compras usadas em lote aparecem travadas com cadeado, editáveis mostram Corrigir/Excluir"
    - "Fluxo 'ida ao mercado': mercado+data fixos no topo, cada 'Adicionar item' salva NA HORA (fechar a aba não perde nada), derivação aparece ao vivo ('= 790g por R$ 11,60 → R$ 0,0147 por g')"
    - "Marca e mercado sugerem valores já usados (combobox com entrada livre)"
  artifacts:
    - path: "components/admin/mercado-flow.tsx"
      provides: "Fluxo D-01/D-02 completo"
      min_lines: 150
    - path: "app/(admin)/admin/ingredientes/[id]/page.tsx"
      provides: "Histórico ING-08 + estado de imutabilidade D-03"
  key_links:
    - from: "components/admin/mercado-flow.tsx"
      to: "lib/actions/compras.ts"
      via: "registrarCompra por item + sugestoesMarca/sugestoesMercado"
      pattern: "registrarCompra"
    - from: "components/admin/ingrediente-form.tsx"
      to: "lib/actions/ingredientes.ts"
      via: "useActionState"
      pattern: "useActionState"
---

<objective>
UI de ingredientes e compras (surfaces 2–6 do UI-SPEC): lista, form simples de ingrediente, detalhe com histórico e ações guardadas (D-03), e o fluxo "ida ao mercado" (D-01/D-02/D-04) — a tela mais usada pela mãe.

Purpose: primeira superfície de dados reais do motor financeiro. Toda mutação já existe (02-04); este plano é composição de UI sobre as actions.
Output: 5 rotas admin funcionais com a copy exata do UI-SPEC.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-financeiro/02-UI-SPEC.md
@.planning/phases/02-motor-financeiro/02-PATTERNS.md

⚠️ Pitfall 3: Decimal→string na fronteira RSC→client.
⚠️ Inputs de dinheiro/quantidade: `inputMode="decimal"`, NUNCA `type="number"`.
⚠️ Copy: usar as tabelas do UI-SPEC §"Ingredientes e compras (surfaces 2–6)" VERBATIM — as strings-chave estão embutidas nas tasks abaixo.
</context>

<interfaces>
Actions do 02-04 (lib/actions/ingredientes.ts, lib/actions/compras.ts): `criarIngrediente(_prev, formData)`, `editarIngrediente(id, _prev, formData)` (useActionState); `registrarCompra(_prev, formData)` → `{ ok, message: 'Anotado!', item }`; `corrigirCompra(id, _prev, formData)`, `excluirCompra(id)`; `sugestoesMarca(prefix)`, `sugestoesMercado(prefix)` (args simples, via useTransition).
Analog form simples: app/(admin-auth)/admin/entrar/page.tsx (useActionState + Label/Input + pending).
Analog fluxo multi-etapa: app/(public)/cadastro/page.tsx (useTransition + estado client + FieldError + sticky CTA).
Analog lista RSC: app/(admin)/admin/auditoria/page.tsx.
Toast: sonner já instalado (`import { toast } from 'sonner'`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Lista de ingredientes + form novo/editar (useActionState)</name>
  <files>app/(admin)/admin/ingredientes/page.tsx, app/(admin)/admin/ingredientes/novo/page.tsx, app/(admin)/admin/ingredientes/[id]/editar/page.tsx, components/admin/ingrediente-form.tsx</files>
  <read_first>
    - app/(admin-auth)/admin/entrar/page.tsx (analog useActionState)
    - app/(admin)/admin/auditoria/page.tsx (analog lista)
    - lib/actions/ingredientes.ts (assinaturas reais)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §surfaces 2–3
  </read_first>
  <action>
1. Lista (RSC): h1 "Ingredientes e embalagens"; CTA accent "Novo ingrediente" → /admin/ingredientes/novo. Query `prisma.ingrediente.findMany({ orderBy: { nome: 'asc' }, include: { _count: { select: { compras: true } } } })`. Linha: nome (link pro detalhe /admin/ingredientes/[id]), unidade-base, badge "Embalagem" (variant muted) quando tipo=EMBALAGEM (ING-06), caption "{n} compras". Empty state: "Nenhum ingrediente ainda" + "Cadastra o primeiro — nome e unidade (g, ml ou unidade) bastam. Forminha e caixa também entram aqui, como embalagem."
2. `components/admin/ingrediente-form.tsx` (`'use client'`, useActionState): campos com copy exata — "Nome"; "Como você mede" (Select com "gramas (g)" / "mililitros (ml)" / "unidade (un)" → values g/ml/un); "É ingrediente ou embalagem?" (radio/select: "Ingrediente" / "Embalagem (forminha, caixa, fita...)"); CTA "Salvar ingrediente" (pending "Salvando...", largura preservada). Erro role="alert". Props `defaults?` p/ modo edição (bind de editarIngrediente com id); quando o ingrediente tem compras, renderizar unidade como texto travado (a action já bloqueia — a UI antecipa).
3. Pages novo/ e [id]/editar/ (RSC finas): carregam defaults quando editar (`notFound()` se ausente) e renderizam o form.
Touch targets ≥44px; inputs 48px mobile; 1 coluna max-w 560px.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "Ingredientes e embalagens" "app/(admin)/admin/ingredientes/page.tsx" && grep -c "Salvar ingrediente" components/admin/ingrediente-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Copy exata presente: "Como você mede", "É ingrediente ou embalagem?", "Forminha e caixa também entram aqui"
    - `grep -c "useActionState" components/admin/ingrediente-form.tsx` == 1
    - Badge de embalagem presente na lista (grep "Embalagem" na page)
  </acceptance_criteria>
  <done>CRUD de ingrediente navegável ponta a ponta.</done>
</task>

<task type="auto">
  <name>Task 2: Detalhe do ingrediente — histórico de compras + ações D-03</name>
  <files>app/(admin)/admin/ingredientes/[id]/page.tsx, components/admin/compra-acoes.tsx</files>
  <read_first>
    - lib/actions/compras.ts (corrigirCompra/excluirCompra — assinaturas e copy de erro)
    - components/ui/dialog.tsx (API real)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §surfaces 4/6 e §Destructive actions inventory
  </read_first>
  <action>
1. RSC `[id]/page.tsx`: query `prisma.ingrediente.findUniqueOrThrow` + `prisma.ingredienteCompra.findMany({ where: { ingredienteId }, orderBy: [{ dataCompra: 'desc' }, { criadoEm: 'desc' }], include: { _count: { select: { usos: true } } } })` (o `_count.usos` decide editável vs travada — checagem preventiva D-03 na UI; a action re-checa).
   - Topo: preço corrente destacado — "Última compra: {marca}, {R$ X,XX} ({R$ 0,0000}/{un-base}) em {dd/mm}" (primeira row da query).
   - h2 "Compras desse ingrediente"; cada linha: "{dd/mm} · {marca} · {mercado} · {2} × {395}{g} · {R$ 11,60}" com tabular-nums.
   - Compra com usos > 0: ícone `Lock` (lucide) + texto muted "Usada num lote — não dá mais pra mudar" (estado, NÃO destructive). Compra com usos == 0: botões de TEXTO "Corrigir" / "Excluir" (Excluir em destructive).
   - Empty state: "Nenhuma compra registrada ainda" + "Toca em 'Fui ao mercado' na tela inicial pra registrar a primeira."
   - Decimais formatados server-side (Intl pt-BR) — nenhuma Decimal cruza como prop.
2. `components/admin/compra-acoes.tsx` (`'use client'`): recebe `{ compraId, resumo: string, editHref }`. "Excluir" abre Dialog: título "Excluir essa compra?", body "Ela ainda não foi usada em nenhum lote, então pode sair sem dó.", CTA destructive "Excluir compra" (chama excluirCompra via useTransition; sucesso → toast + router.refresh()), secundário "Deixa quieto". "Corrigir" navega para um form de edição — implementar edição inline simples: reutilizar o padrão do mercado-flow ou uma rota `[id]/compras/[compraId]/corrigir` com form useActionState dos 5 campos (ingrediente fixo); escolher a rota dedicada (mais simples de guardar). Se a action retornar a copy de imutável (corrida: lote criado entre render e clique), exibi-la como alert.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "Usada num lote" "app/(admin)/admin/ingredientes/[id]/page.tsx" && grep -c "Deixa quieto" components/admin/compra-acoes.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Copy exata: "Compras desse ingrediente", "Excluir essa compra?", "pode sair sem dó", "Excluir compra", "Deixa quieto"
    - Ações só renderizam quando `_count.usos === 0` (grep "usos" na page mostra o branch)
    - Estado travado usa ícone Lock + muted, NÃO destructive
  </acceptance_criteria>
  <done>Histórico ING-08 com preço corrente destacado e D-03 visível e seguro.</done>
</task>

<task type="auto">
  <name>Task 3: Fluxo "ida ao mercado" (D-01/D-02/D-04)</name>
  <files>app/(admin)/admin/compras/nova/page.tsx, components/admin/mercado-flow.tsx</files>
  <read_first>
    - app/(public)/cadastro/page.tsx (useTransition + estado multi-etapa + FieldError + sticky)
    - lib/actions/compras.ts (registrarCompra, sugestoesMarca, sugestoesMercado)
    - components/ui/combobox.tsx (API real)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §surface 5 e §Interaction (fluxo ida ao mercado)
  </read_first>
  <action>
1. Page (RSC fina): h1 "Fui ao mercado"; carrega ingredientes (`findMany` orderBy nome, id+nome+unidadeBase) e renderiza `<MercadoFlow ingredientes={...} />`.
2. `components/admin/mercado-flow.tsx` (`'use client'`):
   - **Header fixo (sticky top)**: "Onde você comprou" (combobox entrada livre com sugestoesMercado via useTransition, debounce ~250ms) + "Quando" (`type="date"`, default hoje BRT). Ficam em estado client e NÃO limpam entre itens.
   - **Form do item**: "O que você comprou" (combobox de ingredientes — mostra unidade-base do selecionado); "Marca" (combobox livre com sugestoesMarca); trio D-02 em grid 3 col ≥sm / empilhado mobile: "Quantas embalagens" × "Tamanho de cada uma ({un-base})" + "Preço de cada uma (R$)" — todos `inputMode="decimal"`, aceitam vírgula.
   - **Derivação ao vivo** (D-02/ING-03, decimal.js no client): "= {790}{g} por {R$ 11,60} → {R$ 0,0147} por {g}" — recálculo onChange; some quando algum campo tá vazio/inválido.
   - **CTA "Adicionar item"**: monta FormData (mercado+data do header + campos do item) e chama `registrarCompra` via useTransition. Sucesso: toast "Anotado!", limpa SÓ os campos do item (ingrediente/marca/trio), foco volta pro campo "O que você comprou", e a linha entra na lista abaixo: "{Ingrediente} — {marca} · {2} × {395}{g} · {R$ 11,60}". Erro: exibir `error`/`fieldErrors` da action inline.
   - **Lista de itens salvos** cresce abaixo do form (estado client alimentado pelos retornos; cada item JÁ está no banco).
   - **CTA de saída** secundário: "Terminei as compras" → router.push('/admin').
   - Sem RHF aqui — estado client simples + useTransition (o fluxo é 1 item por vez; padrão do cadastro).
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "Fui ao mercado" "app/(admin)/admin/compras/nova/page.tsx" && grep -c "Adicionar item" components/admin/mercado-flow.tsx && grep -c "inputMode=\"decimal\"" components/admin/mercado-flow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Copy exata: "Onde você comprou", "Quando", "O que você comprou", "Quantas embalagens", "Preço de cada uma (R$)", "Terminei as compras", "Anotado!"
    - `grep -c "type=\"number\"" components/admin/mercado-flow.tsx` == 0
    - `grep -c "new Decimal" components/admin/mercado-flow.tsx` ≥ 1 (derivação ao vivo via decimal.js)
    - Cada "Adicionar item" dispara UMA action (grep registrarCompra) — sem batch acumulado no client
    - `rtk npm test` continua verde
  </acceptance_criteria>
  <done>Fluxo de compras espelhando como a mãe pensa no mercado, com persistência imediata por item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser da mãe → actions de compras | UI é cortesia; validação e autorização já vivem nas actions (02-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Tampering | derivação ao vivo no client | mitigate | Preview client é só UX; a action recomputa ING-03 server-side (02-04) — nada do preview é enviado |
| T-02-18 | Information Disclosure | páginas de compras/ingredientes | mitigate | Rotas sob app/(admin)/admin/* — gate de role no layout (Phase 1) cobre as PÁGINAS; actions cobrem mutações |
| T-02-19 | Spoofing (CSRF-like) | dialog de exclusão | accept | Server Actions do Next 16 exigem same-origin (allowedOrigins INFRA-05, Phase 1); exclusão só pré-lote e reversível re-registrando |
</threat_model>

<verification>
- `rtk npx tsc --noEmit` — 0 erros
- `rtk npm test` — suite completa verde
- Verificação visual/funcional consolidada no checkpoint do 02-11
</verification>

<success_criteria>
- Surfaces 2–6 do UI-SPEC completas com copy verbatim
- D-01 (persistência por item), D-02 (qtde×tamanho + derivação viva), D-03 (ações condicionais), D-04 (autocomplete) visíveis e funcionais
- Zero type="number", zero Decimal como prop, touch ≥44px
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-financeiro/02-08-SUMMARY.md`
</output>
