---
phase: 02-motor-financeiro
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - lib/validation/receitas.ts
  - lib/actions/receitas.ts
  - tests/financeiro/receitas.test.ts
  - app/(admin)/admin/receitas/page.tsx
  - app/(admin)/admin/receitas/nova/page.tsx
  - app/(admin)/admin/receitas/[id]/editar/page.tsx
  - components/admin/receita-form.tsx
autonomous: true
requirements: [REC-01, REC-02, REC-03, REC-04, REC-05]
must_haves:
  truths:
    - "Mãe cadastra receita com N linhas de ingrediente × qtde, rendimento, gás opcional e 'dura X dias'"
    - "Form mostra custo do lote e custo por unidade AO VIVO enquanto edita (calculado com a última compra de cada ingrediente)"
    - "Lista de receitas mostra custo por unidade de cada uma SEM N+1"
    - "Ingrediente sem compra aparece avisado, não quebra o cálculo"
  artifacts:
    - path: "lib/actions/receitas.ts"
      provides: "criarReceita, editarReceita (transação com itens)"
    - path: "components/admin/receita-form.tsx"
      provides: "Form RHF + useFieldArray com resumo de custo ao vivo"
      min_lines: 100
    - path: "app/(admin)/admin/receitas/page.tsx"
      provides: "Lista com custo/un por receita"
  key_links:
    - from: "components/admin/receita-form.tsx"
      to: "lib/actions/receitas.ts"
      via: "handleSubmit(async (data) => await serverAction(data))"
      pattern: "criarReceita|editarReceita"
    - from: "app/(admin)/admin/receitas/nova/page.tsx"
      to: "lib/custo/corrente.ts"
      via: "ultimasCompras serializadas como string p/ o client calcular preview"
      pattern: "toFixed"
---

<objective>
Slice vertical de receitas: validação, actions transacionais (receita + itens), form RHF com useFieldArray e resumo de custo ao vivo (REC-05/D-09), lista com custo por unidade em batch.

Purpose: a receita é a ponte entre compras e lotes — define o que o lote consome e o rendimento padrão que ancora o custo corrente. Primeira utilização real de RHF no projeto.
Output: /admin/receitas (lista), /admin/receitas/nova e /admin/receitas/[id]/editar funcionais + teste de action.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-financeiro/02-UI-SPEC.md
@.planning/phases/02-motor-financeiro/02-RESEARCH.md
@.planning/phases/02-motor-financeiro/02-PATTERNS.md

⚠️ Pitfall 3: `Prisma.Decimal` NÃO atravessa a fronteira RSC→Client. Custos vão como string (`.toFixed()`), client reconstrói `new Decimal(s)`.
⚠️ Pitfall 2: receita é MUTÁVEL — nada financeiro do lote pode ler a receita depois do INSERT (isso é responsabilidade do 02-07, mas não introduzir aqui nenhum helper que incentive o contrário).
⚠️ Test DB == dev DB: rodar vitest apaga dados do dev.
</context>

<interfaces>
Do 02-03: `custoCorrenteReceita(receitaId)` → `{ total: Decimal; porUnidade: Decimal; faltamCompras: string[] }`; `ultimasCompras(ids)` → `Map<string, UltimaCompra>`; `requireAdmin()`; `zQtdeBRL`/`zDecimalBRL`.
Do 02-02: `components/ui/form.tsx` (Form, FormField...), `combobox`, `select`.
Skeleton de action: lib/actions/auth.ts (mesma ordem do plano 02-04).
Submit RHF (RESEARCH Pattern 6): `handleSubmit(async (data) => { const res = await criarReceita(data); ... })` — action de args tipados (não FormData) é permitida para RHF; validar com o MESMO schema Zod server-side.
Analog form complexo: app/(public)/cadastro/page.tsx (FieldError, aria-describedby/aria-invalid, sticky CTA `fixed inset-x-0 bottom-0 ... md:static`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Validação + actions de receitas (transação receita + itens) com teste</name>
  <files>lib/validation/receitas.ts, lib/actions/receitas.ts, tests/financeiro/receitas.test.ts</files>
  <read_first>
    - lib/actions/auth.ts (skeleton) e lib/actions/compras.ts se já existir na branch (wave 3 paralela — NÃO depender dele)
    - tests/financeiro/fixtures.ts (factories)
    - tests/auth/password-reset.test.ts (mocks next/headers p/ testar action)
  </read_first>
  <action>
1. `lib/validation/receitas.ts`:
```typescript
export const ReceitaSchema = z.object({
  nome: z.string().trim().min(1, OBRIGATORIO),
  rendimentoPadrao: z.coerce.number().int().min(1, 'Precisa render pelo menos 1 unidade.'),
  custoGas: zDecimalBRL.optional().or(z.literal('').transform(() => undefined)),
  validadeDias: z.coerce.number().int().min(1).optional().or(z.literal('').transform(() => undefined)),
  itens: z.array(z.object({ ingredienteId: z.string().uuid(), qtde: zQtdeBRL })).min(1, 'A receita precisa de pelo menos um ingrediente.'),
})
```
(rendimentoPadrao/validadeDias são contagens inteiras — coerce é seguro; dinheiro NUNCA usa coerce.)
2. `lib/actions/receitas.ts` (`'use server'`): `criarReceita(input: unknown)` e `editarReceita(id: string, input: unknown)` — recebem objeto (RHF), validam com ReceitaSchema.safeParse, requireAdmin no try, rate limit igual aos demais. Persistência em transação: create da receita com nested `itens: { create: [...] }` (qtde como `.toFixed(3)` string); editar = `$transaction` com `receitaIngrediente.deleteMany({ where: { receitaId } })` + update da receita + createMany dos itens novos (replace-all é aceitável: itens de receita não carregam histórico — o histórico congelado vive no lote). Rejeitar ingredienteId duplicado no array (refine). `revalidatePath('/admin/receitas')`. Retornar `{ ok: true, id }` ou `{ error, fieldErrors }`. Sem audit obrigatório (não é preço nem lote), mas registrar `logAudit action: 'receita_alterada'` no editar é bem-vindo — se registrar, adicionar copy no ACTION_COPY fica pro 02-11 (anotar no SUMMARY).
3. `tests/financeiro/receitas.test.ts` (padrão 01-08, sessão admin): criar receita com 2 itens → rows em receita_ingredientes; editar substitui itens; `custoCorrenteReceita` da receita criada bate com o esperado (`.toFixed`); EoP: customer recebe `{ error }`.
  </action>
  <verify>
    <automated>rtk npx vitest run tests/financeiro/receitas.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requireAdmin()" lib/actions/receitas.ts` ≥ 2
    - `grep -c "zDecimalBRL\|zQtdeBRL" lib/validation/receitas.ts` ≥ 2 e `grep -c "z.coerce" lib/validation/receitas.ts` ≤ 2 (só rendimento/validadeDias)
    - `grep -c "\$transaction\|itens: { create" lib/actions/receitas.ts` ≥ 1
    - receitas.test.ts verde incluindo caso EoP
  </acceptance_criteria>
  <done>Actions de receita transacionais e testadas.</done>
</task>

<task type="auto">
  <name>Task 2: Lista de receitas (custo/un em batch, empty state)</name>
  <files>app/(admin)/admin/receitas/page.tsx</files>
  <read_first>
    - app/(admin)/admin/auditoria/page.tsx (analog RSC list: query direta, empty state, divide-y)
    - lib/custo/corrente.ts (ultimasCompras p/ batch)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §"Receitas (surfaces 7–8)" e §Mobile-First (cards no mobile)
  </read_first>
  <action>
RSC `app/(admin)/admin/receitas/page.tsx`:
- h1: "Receitas"; CTA topo (accent): link "Nova receita" → /admin/receitas/nova.
- Query: `prisma.receita.findMany({ include: { itens: true, produto: { select: { nome: true } } }, orderBy: { nome: 'asc' } })` + UMA chamada `ultimasCompras` com todos os ingredienteIds coletados (Pitfall 9 — nunca custoCorrenteReceita por row). Compor custo total/porUnidade por receita em memória com Decimal (mesma fórmula do lib/custo — se preferir, exportar de lib/custo um helper batch `custosCorrentesReceitas(receitas, ultimas)` e usar aqui; escolher UM lugar, nunca duplicar a fórmula: adicionar o helper em lib/custo/corrente.ts é o correto).
- Cada linha (card no mobile): nome, "Custo do lote hoje: R$ X,XX · R$ X,XX por unidade" (tabular-nums), rendimento, produto vinculado se houver, link editar. Ingrediente sem compra → caption com a copy: "{Ingrediente} ainda não tem compra registrada — o custo fica incompleto até você registrar uma."
- Empty state: heading "Nenhuma receita ainda" + body "A receita diz quanto de cada ingrediente vai num lote — é com ela que a gente calcula o custo de cada doce."
- Dinheiro formatado com `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })`.
NOTA files_modified: se adicionar o helper batch, tocar lib/custo/corrente.ts aqui é permitido (mesma wave não tem outro plano tocando lib/custo — 02-04/06/07 apenas importam).
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "Nenhuma receita ainda" "app/(admin)/admin/receitas/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "include" "app/(admin)/admin/receitas/page.tsx"` ≥ 1 e ZERO chamadas de custoCorrenteReceita dentro de map/loop (grep "custoCorrenteReceita" no arquivo == 0)
    - Contém "Custo do lote hoje" e "Nova receita"
    - `grep -c "tabular-nums" "app/(admin)/admin/receitas/page.tsx"` ≥ 1
  </acceptance_criteria>
  <done>Lista de receitas com custo em batch, copy do UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Form de receita (RHF + useFieldArray + custo ao vivo) para nova e editar</name>
  <files>components/admin/receita-form.tsx, app/(admin)/admin/receitas/nova/page.tsx, app/(admin)/admin/receitas/[id]/editar/page.tsx</files>
  <read_first>
    - app/(public)/cadastro/page.tsx (FieldError, aria wiring, sticky CTA, useTransition)
    - components/ui/form.tsx e components/ui/combobox.tsx (APIs reais recém-vendoradas)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §"Receitas (surfaces 7–8)" (copy exata) e §Interaction (foco no useFieldArray)
  </read_first>
  <action>
1. `components/admin/receita-form.tsx` (`'use client'`): RHF com `zodResolver`/`standardSchemaResolver` sobre um schema client (mesmos campos como strings; a validação de verdade é server). Props (todas serializáveis — Pitfall 3):
```typescript
type ReceitaFormProps = {
  ingredientes: Array<{ id: string; nome: string; unidadeBase: 'g'|'ml'|'un'; custoPorUnidadeBase: string | null }> // null = sem compra
  defaults?: { id: string; nome: string; rendimentoPadrao: number; custoGas: string | null; validadeDias: number | null; itens: Array<{ ingredienteId: string; qtde: string }> }
}
```
Campos e copy EXATOS do UI-SPEC: "Nome da receita"; h2 "Ingredientes (pra um lote)"; por linha: select/combobox "Ingrediente" + input "Quantidade ({un-base})" (`inputMode="decimal"`, aceita vírgula); botão X remover com `aria-label="Remover ingrediente"` (hitbox ≥44px); "Mais um ingrediente" adiciona linha (foco vai pro primeiro campo da nova linha; ao remover, foco pra linha anterior); "Rende quantas unidades"; "Gasto de gás/energia por lote (R$) — se quiser"; "Dura quantos dias"; CTA sticky "Salvar receita" (pending → "Salvando...", largura preservada).
Resumo ao vivo (REC-05): rodapé com "Custo do lote hoje: {R$ X,XX} · {R$ X,XX} por unidade" + caption "calculado com a última compra de cada ingrediente" — calculado NO CLIENT com decimal.js a partir de `custoPorUnidadeBase` (string→Decimal) × qtde digitada (vírgula→ponto) + gás, ÷ rendimento; recálculo onChange com debounce ≤300ms. Ingrediente sem compra: exibir a copy "…ainda não tem compra registrada — o custo fica incompleto…".
Submit: `handleSubmit(async (data) => { const res = defaults ? await editarReceita(defaults.id, data) : await criarReceita(data); if (res?.error) setError(...); else router.push('/admin/receitas') })`.
2. `nova/page.tsx` (RSC): carrega ingredientes (`prisma.ingrediente.findMany` orderBy nome) + `ultimasCompras` para servir custoPorUnidadeBase como `.toFixed(6)` string (null se sem compra); renderiza `<ReceitaForm ingredientes={...} />`. h1 "Nova receita" — na verdade h1 da page é o padrão: usar "Nova receita".
3. `[id]/editar/page.tsx` (RSC): idem + `defaults` da receita carregada (Decimal→string na fronteira); notFound() se id inexistente.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "useFieldArray" components/admin/receita-form.tsx && grep -c "Remover ingrediente" components/admin/receita-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "inputMode=\"decimal\"" components/admin/receita-form.tsx` ≥ 2 e `grep -c "type=\"number\"" components/admin/receita-form.tsx` == 0
    - Contém as strings exatas: "Ingredientes (pra um lote)", "Mais um ingrediente", "Salvar receita", "calculado com a última compra de cada ingrediente"
    - `grep -c "new Decimal" components/admin/receita-form.tsx` ≥ 1 (preview via decimal.js, nunca +/*)
    - `grep -cE "Prisma.Decimal|@prisma/client" components/admin/receita-form.tsx` == 0 (client não importa Prisma)
    - `rtk npm test` continua verde
  </acceptance_criteria>
  <done>Form de receita completo (novo/editar) com linhas dinâmicas acessíveis e custo ao vivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → actions de receita | Objeto RHF (não FormData) cruza a fronteira — validação server obrigatória |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege | criarReceita/editarReceita | mitigate | requireAdmin() + teste EoP na Task 1 |
| T-02-11 | Tampering | input RHF tipado | mitigate | safeParse com ReceitaSchema server-side (nunca confiar no schema client); uuids validados |
| T-02-12 | Information Disclosure | preview de custo no client | accept | Custo corrente serializado só em páginas atrás do gate admin; não é PII e a audiência é a própria dona |
</threat_model>

<verification>
- `rtk npx vitest run tests/financeiro --reporter=dot` — verde
- `rtk npx tsc --noEmit` — 0 erros
- `rtk npm test` — suite completa verde
</verification>

<success_criteria>
- REC-01..05 completos: receita ligada a produto (via Produto.receitaId — vínculo feito no form de produto do 02-09), itens × qtde, rendimento, gás, custo automático
- Zero N+1 na lista; zero Decimal cruzando para client
- Copy 100% do UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-financeiro/02-05-SUMMARY.md`
Anotar no SUMMARY se `receita_alterada` foi adicionada ao audit (o 02-11 adiciona a copy no viewer).
</output>
