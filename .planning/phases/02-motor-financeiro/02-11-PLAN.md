---
phase: 02-motor-financeiro
plan: 11
type: execute
wave: 5
depends_on: ["02-04", "02-05", "02-06", "02-07", "02-08", "02-09", "02-10"]
files_modified:
  - app/(admin)/admin/page.tsx
  - app/(admin)/admin/auditoria/page.tsx
autonomous: false
requirements: [PROD-08]
must_haves:
  truths:
    - "Home admin abre com os 2 atalhos grandes 'Fui ao mercado' / 'Produzi hoje' acima de tudo"
    - "'Precisa de atenção' lista produtos cuja margem corrente caiu abaixo do mínimo (embrião do ADM-01 — discretion do CONTEXT), computado on-read sem tabela nova"
    - "Viewer de auditoria traduz compra_registrada, lote_criado e preco_alterado pra pt-BR"
    - "A mãe (via dev) consegue percorrer o ciclo completo: ingrediente → compra → receita → produto → lote, no celular"
  artifacts:
    - path: "app/(admin)/admin/page.tsx"
      provides: "Home admin com atalhos 1-toque + ações pendentes de margem"
      contains: "Precisa de atenção"
    - path: "app/(admin)/admin/auditoria/page.tsx"
      provides: "ACTION_COPY com as 3 actions novas"
      contains: "lote_criado"
  key_links:
    - from: "app/(admin)/admin/page.tsx"
      to: "lib/custo/corrente.ts"
      via: "margensCorrentesBatch filtrado por margem < minima"
      pattern: "margensCorrentesBatch"
---

<objective>
Fechar a fase: home admin com atalhos 1-toque e a lista "Precisa de atenção" (alerta de margem caída — discretion resolvida conforme recomendação do CONTEXT), tradução das actions novas no viewer de auditoria, gate de suite completa e checkpoint de verificação humana do ciclo inteiro.

Purpose: a home é o ponto de entrada diário da mãe; o checkpoint é a única verificação visual/funcional da fase antes do /gsd-verify-work.
Output: home admin final + auditoria atualizada + aprovação humana do fluxo completo.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-financeiro/02-UI-SPEC.md

⚠️ Discretion (CONTEXT + RESEARCH Open Question 2): alerta de margem = item computado on-read na home. SEM tabela de notificações, SEM job pg-boss — isso seria scope creep; é o embrião do ADM-01 da Fase 7.
</context>

<interfaces>
Do 02-03: `margensCorrentesBatch()` → inclui `minima` (override ?? global) por produto.
ACTION_COPY atual (app/(admin)/admin/auditoria/page.tsx:11-19): `Record<string, string>` — adicionar chaves novas no mesmo formato.
Home atual (app/(admin)/admin/page.tsx): placeholder da Phase 1 (max-w container, font-display h1, nav de Links) — ESTENDER, não recomeçar do zero.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Home admin — atalhos 1-toque + "Precisa de atenção"</name>
  <files>app/(admin)/admin/page.tsx</files>
  <read_first>
    - app/(admin)/admin/page.tsx (placeholder atual — preservar container/estilo base)
    - lib/custo/corrente.ts (margensCorrentesBatch)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §"Home admin (surface 1)" + §Color (atalhos = accent)
  </read_first>
  <action>
Reescrever a home (RSC) per UI-SPEC:
1. h1: "Oi! O que você fez hoje?"
2. Grid 2 col de cards de atalho (SEMPRE acima da lista; fundo `--primary` rosa, texto chocolate, ~96px de altura, touch-friendly):
   - "Fui ao mercado" + caption "Registrar as compras" → /admin/compras/nova
   - "Produzi hoje" + caption "Registrar um lote" → /admin/lotes/produzir
3. Seção h2 "Precisa de atenção": `margensCorrentesBatch()` filtrado por `margem !== null && margem.lt(minima)`. Cada item (padrão destructive multi-canal: borda esquerda 4px + TriangleAlert + texto): "{Produto}: a margem caiu pra {X}% — o preço de um ingrediente subiu" + link "Ver produto" → /admin/produtos/{id}/editar.
4. Empty state: "Tudo em dia por aqui 🎉" + body "Quando algum doce ficar com margem baixa, a gente te avisa nesta lista."
Manter quaisquer links administrativos existentes do placeholder que ainda façam sentido (Auditoria já está na nav do 02-02 — pode remover da home).
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "Oi! O que você fez hoje?" "app/(admin)/admin/page.tsx" && grep -c "Precisa de atenção" "app/(admin)/admin/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Copy exata: "Fui ao mercado", "Registrar as compras", "Produzi hoje", "Registrar um lote", "Tudo em dia por aqui 🎉", "a margem caiu pra"
    - 1 chamada de margensCorrentesBatch; zero tabela/model novo; zero job pg-boss (grep "pgboss\|pg-boss" no arquivo == 0)
    - Atalhos vêm ANTES da seção de atenção no JSX
  </acceptance_criteria>
  <done>Home admin final: 1 toque pros 2 fluxos quentes + embrião do ADM-01.</done>
</task>

<task type="auto">
  <name>Task 2: ACTION_COPY das actions novas + gate de suite completa</name>
  <files>app/(admin)/admin/auditoria/page.tsx</files>
  <read_first>
    - app/(admin)/admin/auditoria/page.tsx (mapa ACTION_COPY, linhas 11–19)
    - SUMMARYs 02-04/02-05/02-06/02-07 (confirmar se receita_alterada/outras actions extras foram registradas)
  </read_first>
  <action>
1. Adicionar ao ACTION_COPY (voz "vizinha", mesmo formato das entradas existentes):
```typescript
compra_registrada: 'registrou uma compra',
lote_criado: 'registrou um lote produzido',
preco_alterado: 'mudou o preço de um produto',
```
Se os SUMMARYs dos planos anteriores registrarem actions adicionais (ex.: `receita_alterada`), incluir também ('mexeu numa receita').
2. Gate de fase: rodar suite completa + tsc + build de produção. Corrigir qualquer quebra encontrada (é o último plano autônomo antes da verificação humana).
  </action>
  <verify>
    <automated>grep -c "lote_criado" "app/(admin)/admin/auditoria/page.tsx" && rtk npm test && rtk npx tsc --noEmit && rtk next build</automated>
  </verify>
  <acceptance_criteria>
    - As 3 chaves novas presentes no ACTION_COPY com copy pt-BR
    - `rtk npm test` 100% verde (incl. LOTE-08); `rtk next build` conclui sem erro
  </acceptance_criteria>
  <done>Auditoria fala a língua da fase; suite e build verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana do ciclo completo do motor financeiro</name>
  <action>Pausar a execução e apresentar ao usuário o roteiro de verificação abaixo (how-to-verify). Não prosseguir sem o sinal de retomada.</action>
  <verify>Usuário respondeu "approved" ou listou problemas (que viram gaps para replanejamento)</verify>
  <done>Aprovação humana registrada do ciclo ingrediente → compra → receita → produto → lote no mobile</done>
  <what-built>Motor financeiro completo: ingredientes (com embalagens), compras imutáveis com fluxo "ida ao mercado", receitas com custo ao vivo, produtos com margem em tempo real e bloqueio de preço-piso, produção de lote com custo congelado, filtros de lote, ajustes de margem, home com atalhos e alertas, aviso ANVISA e auditoria traduzida.</what-built>
  <how-to-verify>
Com `rtk npm run dev` rodando e logado como admin (npm run dev:login se precisar), preferencialmente em viewport mobile (DevTools ~390px):

1. /admin — home mostra "Oi! O que você fez hoje?" + 2 cards rosa; menu hamburger abre com 8 itens
2. /admin/ingredientes → "Novo ingrediente": criar "Leite condensado" (gramas) e "Forminha nº4" (unidade, tipo Embalagem — badge aparece na lista)
3. Home → "Fui ao mercado": mercado "Extra", hoje; adicionar "Leite condensado, marca Moça, 2 × 395g a 5,80" → derivação "= 790g por R$ 11,60 → R$ 0,0147 por g" aparece ao vivo; toast "Anotado!"; item na lista; adicionar segundo item sem redigitar mercado/data; "Terminei as compras"
4. /admin/ingredientes/[Leite condensado] — última compra destacada no topo; linha com "Corrigir"/"Excluir" visíveis (ainda sem lote)
5. /admin/receitas → "Nova receita": "Brigadeiro", 395g de leite condensado + 1 forminha…, rendimento 20, gás 2,00, dura 5 dias → resumo "Custo do lote hoje" reage ao digitar; salvar
6. /admin/produtos → "Novo produto": "Brigadeiro", doce único, receita Brigadeiro; digitar preço alto → bloco de margem saudável; baixar até ficar abaixo de 30% → linha vermelha com TriangleAlert; baixar abaixo do custo → alerta de bloqueio + botão desabilitado; tentar salvar barato (via devtools re-enable) → server rejeita com a mesma copy; salvar com preço ok
7. Home → "Produzi hoje": receita Brigadeiro; compra Moça pré-selecionada; "Trocar compra" lista as compras; multiplicador 1,5× reescala qtde na tela; rendimento real 28; validade sugerida = hoje+5d com "vence dd/mm" ao lado; "Registrar produção" → toast "O custo ficou guardado do jeitinho que foi hoje."
8. /admin/ingredientes/[Leite condensado] — a compra usada agora mostra cadeado "Usada num lote — não dá mais pra mudar" (sem botões)
9. /admin/lotes — lote em "Vigentes" com "vence dd/mm · 28 disponíveis"; filtros trocam
10. Registrar compra nova mais cara do leite condensado → /admin/produtos mostra margem RECALCULADA (corrente), mas o lote antigo em /admin/lotes mantém o custo congelado
11. /admin/ajustes — mudar margem mínima pra 50% → lista de produtos e home "Precisa de atenção" passam a acusar o Brigadeiro
12. /admin/ajuda — aviso ANVISA presente; /admin/auditoria — eventos "registrou uma compra", "registrou um lote produzido", "mudou o preço de um produto" legíveis
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| home admin → lib/custo | Leitura agregada de margens (sem input do usuário) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Information Disclosure | home com margens | mitigate | Rota sob (admin) com role-gate; nenhum dado financeiro em rota pública |
| T-02-20 | Repudiation | viewer de auditoria | mitigate | Metadata renderizada como TEXTO (React escapa — padrão já existente no arquivo); nenhuma mudança nesse comportamento |
</threat_model>

<verification>
- `rtk npm test` — suite completa verde (Phase 1 + financeiro incl. LOTE-08)
- `rtk npx tsc --noEmit` e `rtk next build` — limpos
- Checkpoint human-verify aprovado (ciclo completo no mobile)
</verification>

<success_criteria>
- Home per UI-SPEC (atalhos acima, atenção computada on-read, empty state)
- 3 actions novas traduzidas na auditoria
- Os 5 success criteria do ROADMAP Phase 2 verificáveis manualmente pelo roteiro do checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-financeiro/02-11-SUMMARY.md`
Registrar no SUMMARY os handoffs da fase: LOTE-05 cron (Phase 5 usa listarLotes/hojeSaoPaulo), LOTE-06 trigger de validade (Phase 4), PROD-03..07 (Phase 3), "Precisa de atenção" → ADM-01 completo (Phase 7).
</output>
