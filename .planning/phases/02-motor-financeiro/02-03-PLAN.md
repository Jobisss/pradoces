---
phase: 02-motor-financeiro
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/auth/require-admin.ts
  - lib/validation/decimal.ts
  - lib/custo/corrente.ts
  - lib/custo/congelado.ts
  - tests/financeiro/fixtures.ts
  - tests/financeiro/decimal.test.ts
  - tests/financeiro/custo-corrente.test.ts
autonomous: true
requirements: [ING-03, ING-05, REC-05, PROD-08, LOTE-03]
must_haves:
  truths:
    - "TODA aritmética de dinheiro da fase vive em lib/custo (decimal.js server-only) — nenhum componente faz conta"
    - "zDecimalBRL aceita '5,80' e '5.80' e rejeita qualquer coisa que Number() aceitaria errado"
    - "Custo corrente de receita = Σ(qtde × custo da ÚLTIMA compra de cada ingrediente) + gás, com rendimento PADRÃO (D-09)"
    - "Custo de kit = soma dos custos correntes dos componentes (D-11)"
    - "Margem = (preço − custo) ÷ preço × 100 (A1 — margem sobre preço de venda)"
  artifacts:
    - path: "lib/auth/require-admin.ts"
      provides: "Guard de autorização para TODA Server Action da fase"
      exports: ["requireAdmin"]
    - path: "lib/validation/decimal.ts"
      provides: "zDecimalBRL (string pt-BR → Decimal, nunca Number)"
      exports: ["zDecimalBRL"]
    - path: "lib/custo/corrente.ts"
      provides: "ultimasCompras (DISTINCT ON), custoCorrenteReceita, custoCorrenteKit, margemPercent, margens em batch"
    - path: "lib/custo/congelado.ts"
      provides: "computeLoteSnapshot (D-06/D-07: linhas escaladas + custo congelado)"
    - path: "tests/financeiro/fixtures.ts"
      provides: "Factories: criarIngrediente, registrarCompra, criarReceita, criarProduto, produzirLote"
  key_links:
    - from: "lib/custo/corrente.ts"
      to: "ingrediente_compras"
      via: "$queryRaw DISTINCT ON (tagged template)"
      pattern: "DISTINCT ON \\(ingrediente_id\\)"
    - from: "lib/custo/corrente.ts"
      to: "server-only"
      via: "import 'server-only'"
      pattern: "server-only"
---

<objective>
Construir o núcleo monetário da fase: o guard `requireAdmin()`, o schema `zDecimalBRL` (vírgula pt-BR → Decimal), a lib `lib/custo/` (chokepoint de TODA aritmética de custo — corrente e congelado), e as factories de teste + testes unitários do cálculo.

Purpose: mesma postura do `logAudit` da Phase 1 (helper que absorve o invariante): nada fora de lib/custo faz conta de dinheiro; nada fora de zDecimalBRL parseia dinheiro. Os 4 planos da wave 3 consomem estes módulos.
Output: 4 módulos server + fixtures + 2 suites de teste verdes.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-financeiro/02-RESEARCH.md
@.planning/phases/02-motor-financeiro/02-PATTERNS.md

⚠️ Test DB == dev DB: rodar vitest apaga dados do dev.
⚠️ `$queryRaw` tem ZERO usos no codebase hoje — este plano introduz o primeiro. Só tagged templates (auto-parametrizado); NUNCA `$queryRawUnsafe`.
</context>

<interfaces>
Analog do guard (combinar os dois trechos existentes):

`app/(admin)/admin/layout.tsx:14-16`:
```typescript
const session = await auth.api.getSession({ headers: await nextHeaders() })
```
Import de auth: `import { auth } from '@/lib/auth/server'`.

Shape alvo do requireAdmin (RESEARCH Pattern 1, verbatim):
```typescript
export async function requireAdmin() {
  const session = await auth.api.getSession({ headers: await headers() })
  const user = session?.user as { id: string; role?: string } | undefined
  if (!user || user.role !== 'admin') {
    throw new Error('UNAUTHORIZED') // a action captura e retorna erro genérico
  }
  return user
}
```

zDecimalBRL (RESEARCH Pattern 2, verbatim):
```typescript
export const zDecimalBRL = z
  .string()
  .trim()
  .regex(/^\d{1,13}([.,]\d{1,4})?$/, 'Esse número não parece certo. Usa vírgula pros centavos: 5,80')
  .transform((s) => new Decimal(s.replace(',', '.')))
```

DISTINCT ON (RESEARCH Pattern 3, verbatim — ordenação A2: data_compra DESC, desempate created_at DESC):
```typescript
const ultimas = await prisma.$queryRaw<UltimaCompraRow[]>`
  SELECT DISTINCT ON (ingrediente_id)
         id, ingrediente_id, marca, custo_por_unidade_base, data_compra
  FROM ingrediente_compras
  WHERE ingrediente_id = ANY(${ingredienteIds}::uuid[])
  ORDER BY ingrediente_id, data_compra DESC, criado_em DESC
`
// $queryRaw devolve numeric como Prisma.Decimal e uuid como string.
```
NOTA: a coluna de desempate no schema deste projeto chama `criado_em` (@map de criadoEm) — usar `criado_em`, não `created_at`.

Factory style (tests/conftest.ts:64-85 — objeto de args opcionais, defaults sensatos, retorna shape mínimo).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: requireAdmin + zDecimalBRL (com testes)</name>
  <files>lib/auth/require-admin.ts, lib/validation/decimal.ts, tests/financeiro/decimal.test.ts</files>
  <read_first>
    - app/(admin)/admin/layout.tsx (session fetch + role check existente)
    - lib/validation/auth.ts (convenção: consts de copy pt-BR + Schema exportado + z.infer)
  </read_first>
  <behavior>
    - zDecimalBRL: parse('5,80') → Decimal igual a 5.8; parse('5.80') → 5.8; parse('0,0147') → 0.0147; parse('1234567890123,4567') aceita (13 int + 4 frac)
    - zDecimalBRL rejeita: '', 'abc', '5,80,90', '-5', '5e3', '1.234,56' (separador de milhar não suportado — D-02 nunca pede número grande com milhar)
    - Mensagem de erro EXATA (UI-SPEC): "Esse número não parece certo. Usa vírgula pros centavos: 5,80"
  </behavior>
  <action>
1. Criar `lib/auth/require-admin.ts` com o shape do bloco interfaces (verbatim), importando `auth` de `@/lib/auth/server` e `headers` de `next/headers`. Doc-comment: "Server Actions são endpoints POST públicos — o layout (admin) protege PÁGINAS, não actions. Toda mutação da Phase 2+ chama isto na primeira linha."
2. Criar `lib/validation/decimal.ts` com `zDecimalBRL` (verbatim do bloco interfaces) + variante `zQtdeBRL` idêntica mas permitindo até 3 casas decimais para quantidades (`/^\d{1,11}([.,]\d{1,3})?$/`). Import: `import Decimal from 'decimal.js'` e `import { z } from 'zod'`.
3. Criar `tests/financeiro/decimal.test.ts` cobrindo o behavior acima (teste puro, sem DB). Comparações via `.toFixed(n)` string (Pitfall 8): `expect(parsed.toFixed(4)).toBe('5.8000')`.
requireAdmin NÃO é testado aqui (exige runtime Next mockado — coberto no produtos.test.ts do plano 02-06 via padrão 01-08).
  </action>
  <verify>
    <automated>rtk npx vitest run tests/financeiro/decimal.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "z.number()" lib/validation/decimal.ts` == 0 e `grep -c "parseFloat\|Number(" lib/validation/decimal.ts` == 0
    - `grep -c "UNAUTHORIZED" lib/auth/require-admin.ts` == 1
    - decimal.test.ts verde com ≥ 8 asserções (aceites + rejeições)
  </acceptance_criteria>
  <done>Guard e parser prontos; nenhum caminho de Number()/parseFloat para dinheiro existe.</done>
</task>

<task type="auto">
  <name>Task 2: lib/custo — corrente (DISTINCT ON, receita, kit, margem, batch) e congelado (snapshot de lote)</name>
  <files>lib/custo/corrente.ts, lib/custo/congelado.ts</files>
  <read_first>
    - lib/audit/log.ts (postura de chokepoint: doc-comment do invariante + funções tipadas exportadas)
    - lib/db/client.ts (singleton prisma — nunca new PrismaClient)
    - .planning/phases/02-motor-financeiro/02-RESEARCH.md §Pattern 3, §Pattern 4, §"Custo corrente de receita"
  </read_first>
  <action>
Ambos os arquivos começam com `import 'server-only'` e doc-comment: "TODA aritmética de custo do projeto mora aqui. Nada fora de lib/custo soma/multiplica dinheiro."

**`lib/custo/corrente.ts`** exporta:

1. `type UltimaCompra = { id: string; ingredienteId: string; marca: string; custoPorUnidadeBase: Decimal; dataCompra: Date }`
2. `ultimasCompras(ingredienteIds: string[]): Promise<Map<string, UltimaCompra>>` — DISTINCT ON verbatim do bloco interfaces (desempate por `criado_em DESC` — assunção A2). Retorna Map por ingredienteId. Array vazio → Map vazio sem query.
3. `custoCorrenteReceita(receitaId: string): Promise<{ total: Decimal; porUnidade: Decimal; faltamCompras: string[] }>` — carrega receita com `include: { itens: { include: { ingrediente: true } } }` (anti N+1), chama ultimasCompras UMA vez, computa `total = Σ(item.qtde × ultima.custoPorUnidadeBase) + (custoGas ?? 0)`; `porUnidade = total ÷ rendimentoPadrao` (D-09: custo CORRENTE usa rendimento PADRÃO; o REAL só entra no congelado — D-06). Ingredientes sem compra entram em `faltamCompras` (nomes) e contribuem 0 ao total.
4. `custoCorrenteProduto(produtoId: string)` — UNITARIO: delega para custoCorrenteReceita da receita vinculada; KIT: `Σ(qtde × custoCorrente do componente unitário)` (D-11). Retorna `{ custo: Decimal; faltamCompras: string[] }` (para kit, custo = soma dos custos POR UNIDADE × qtde de cada componente).
5. `margemPercent(preco: Decimal, custo: Decimal): Decimal` — definição A1: `(preco − custo) ÷ preco × 100`; preco ≤ 0 → retorna Decimal(0). Doc-comment com a definição ("de cada R$10, R$3 ficam com você").
6. `margensCorrentesBatch(): Promise<Array<{ produtoId: string; nome: string; precoVenda: Decimal; custo: Decimal | null; margem: Decimal | null; minima: Decimal }>>` — para lista de produtos e home admin SEM N+1 (Pitfall 9): 1 query de produtos com `include: { receita: { include: { itens: true } }, kitItens: { include: { componente: { include: { receita: { include: { itens: true } } } } } } }`, 1 chamada ultimasCompras com TODOS os ingredienteIds coletados, 1 findUnique de Configuracao (id=1, default margemMinimaPadrao=30 se row ausente); compõe em memória. `minima` = override do produto ?? global. `custo/margem` null quando faltam compras em todos os itens.

**`lib/custo/congelado.ts`** exporta:

`computeLoteSnapshot(args: { linhas: Array<{ compra: { id: string; marca: string; custoPorUnidadeBase: Decimal }; qtdeUsada: Decimal }>; custoGas: Decimal; rendimentoReal: number })` → retorna
```typescript
{
  usos: Array<{ ingredienteCompraId: string; qtdeUsada: string; marcaSnapshot: string; custoUnitarioCongelado: string; custoCongelado: string }>,
  custoTotalCongelado: string,        // .toFixed(4)
  custoPorUnidadeCongelado: string,   // .toFixed(6) — total ÷ rendimentoReal (D-06)
  custoGasCongelado: string,          // .toFixed(4)
}
```
Função PURA (sem DB) — a transação do plano 02-07 usa o retorno num nested create. `qtdeUsada` já vem escalada pelo multiplicador (D-07) — o caller escala; esta função só congela. custoCongelado por linha = `qtdeUsada.mul(custoPorUnidadeBase).toFixed(4)`; custoUnitarioCongelado = `.toFixed(6)`.

Toda conta via Decimal (decimal.js / Prisma.Decimal — API compatível). ZERO `+`/`*` de JS com dinheiro.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "server-only" lib/custo/corrente.ts lib/custo/congelado.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DISTINCT ON (ingrediente_id)" lib/custo/corrente.ts` == 1 e `grep -c "queryRawUnsafe" lib/` (recursivo) == 0
    - `grep -c "import 'server-only'" lib/custo/corrente.ts` == 1 (idem congelado.ts)
    - `grep -cE "rendimentoPadrao" lib/custo/corrente.ts` ≥ 1 e `grep -cE "rendimentoReal" lib/custo/congelado.ts` ≥ 1 (D-09 vs D-06 separados)
    - Nenhuma função de congelado.ts consulta o DB (`grep -c "prisma" lib/custo/congelado.ts` == 0)
  </acceptance_criteria>
  <done>lib/custo completa: corrente (batch anti-N+1) + congelado (puro), tsc limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fixtures + testes unitários/integrados do cálculo de custo</name>
  <files>tests/financeiro/fixtures.ts, tests/financeiro/custo-corrente.test.ts</files>
  <read_first>
    - tests/conftest.ts (factory style createTestUser, truncateAll)
    - tests/financeiro/schema.test.ts (como o plano 02-01 montou a cadeia de dados via prisma direto)
    - lib/custo/corrente.ts e lib/custo/congelado.ts (recém-criados — assinaturas reais)
  </read_first>
  <behavior>
    - REC-05 exato: ingrediente 'Leite condensado' (g), compra 1×395g a R$3,95 → custo/g = 0.010000; receita com 395g + gás R$2,00, rendimento 20 → total = '5.9500', porUnidade = '0.297500'
    - REC-05 sem gás: total sem o adendo
    - Dízima (1/3 × 3): 3 itens de 333.333g a custo 0.000003/g não explodem precisão — asserção via .toFixed
    - ING-05/A2: duas compras do mesmo ingrediente, datas diferentes → ultimasCompras retorna a de data maior; mesma data → a de criado_em maior
    - D-11: kit com 2 componentes unitários (qtde 2 e 1) → custo = 2×porUnidade(A) + 1×porUnidade(B)
    - A1: margemPercent(Decimal(10), Decimal(7)).toFixed(2) === '30.00'
    - faltamCompras: receita com ingrediente sem compra → nome aparece em faltamCompras e total ignora o item
    - congelado: computeLoteSnapshot com rendimentoReal 18 (≠ padrão 20) divide por 18 (D-06)
  </behavior>
  <action>
1. Criar `tests/financeiro/fixtures.ts` com factories no estilo conftest (args opcionais + defaults), usando prisma direto + lib/custo (NÃO Server Actions — não existem ainda):
   - `criarIngrediente({ nome?, unidadeBase?, tipo? })` → row
   - `registrarCompra({ ingredienteId, marca?, mercado?, dataCompra?, qtdeEmbalagens?, tamanhoEmbalagem?, precoPorEmbalagem? })` → computa qtdeTotalBase/precoTotal/custoPorUnidadeBase via Decimal (mesma derivação ING-03 que a action do 02-04 fará) e insere
   - `criarReceita({ nome?, rendimentoPadrao?, custoGas?, validadeDias?, itens: [{ingredienteId, qtde}] })`
   - `criarProduto({ nome?, tipo?, precoVenda?, receitaId?, categoria?, descricao?, margemMinimaOverride?, kitItens? })`
   - `produzirLote({ receitaId, produtoId, multiplicador?, rendimentoReal, validade? })` → carrega itens da receita + ultimasCompras, escala por multiplicador, chama computeLoteSnapshot, faz `prisma.lote.create` com nested `usos: { create: [...] }` — espelho fiel do que a action do 02-07 fará
2. Criar `tests/financeiro/custo-corrente.test.ts` implementando TODOS os casos do behavior, com `beforeEach(truncateAll)` para os casos com DB. Decimal SEMPRE comparado por `.toFixed(n)` string.
  </action>
  <verify>
    <automated>rtk npx vitest run tests/financeiro --reporter=dot && rtk npm test</automated>
  </verify>
  <acceptance_criteria>
    - fixtures.ts exporta as 5 factories (grep dos 5 nomes)
    - custo-corrente.test.ts contém as strings de asserção `'0.010000'` e `'30.00'`
    - Suite completa verde (Phase 1 + schema + decimal + custo-corrente)
  </acceptance_criteria>
  <done>Cálculo de custo provado (corrente, kit, margem, congelado puro); factories prontas para os planos 02-04..02-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | Input não confiável (valores monetários, IDs) cruza aqui |
| action → Postgres via $queryRaw | Primeira introdução de SQL raw no projeto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege | toda Server Action da fase | mitigate | `requireAdmin()` criado aqui; planos 02-04..07 o chamam na primeira linha; teste EoP no 02-06 |
| T-02-07 | Tampering (SQLi) | ultimasCompras ($queryRaw) | mitigate | Só tagged template auto-parametrizado; ids passados como array parametrizado `ANY(${ids}::uuid[])`; zero $queryRawUnsafe (gate por grep) |
| T-02-08 | Tampering | valores monetários do client | mitigate | zDecimalBRL: string→Decimal com regex estrita; nenhum Number()/parseFloat em caminho de dinheiro |
</threat_model>

<verification>
- `rtk npx vitest run tests/financeiro --reporter=dot` — verde
- `rtk npm test` — suite completa verde
- `grep -rc "queryRawUnsafe" lib/` == 0
</verification>

<success_criteria>
- lib/custo é o único lugar com aritmética de dinheiro; server-only nos dois módulos
- ultimasCompras em 1 round-trip (DISTINCT ON) com desempate A2
- margemPercent implementa A1 com doc da definição
- Fixtures reutilizáveis pelos 4 planos da wave 3
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-financeiro/02-03-SUMMARY.md`
</output>
