---
phase: 02-motor-financeiro
plan: 09
type: execute
wave: 4
depends_on: ["02-02", "02-06"]
files_modified:
  - app/(admin)/admin/produtos/page.tsx
  - app/(admin)/admin/produtos/novo/page.tsx
  - app/(admin)/admin/produtos/[id]/editar/page.tsx
  - components/admin/produto-form.tsx
  - app/(admin)/admin/ajustes/page.tsx
  - components/admin/ajustes-form.tsx
autonomous: true
requirements: [PROD-01, PROD-02, PROD-08, PROD-09]
must_haves:
  truths:
    - "Lista de produtos mostra margem corrente por produto e a linha abaixo do mínimo salta em vermelho (borda + ícone + texto — nunca cor sozinha)"
    - "Form de produto mostra a margem AO VIVO enquanto digita o preço, com a linha vermelha (PROD-08) e o bloqueio visual + server de preço<custo (PROD-09)"
    - "Ajustes salva a margem mínima global (D-10)"
    - "Kit compõe componentes unitários e o custo do kit soma os componentes (D-11)"
  artifacts:
    - path: "components/admin/produto-form.tsx"
      provides: "Form RHF com bloco de margem reativo (PROD-08/09, D-09/D-10/D-11)"
      min_lines: 150
    - path: "app/(admin)/admin/produtos/page.tsx"
      provides: "Lista com margens em batch (Pitfall 9)"
    - path: "app/(admin)/admin/ajustes/page.tsx"
      provides: "Margem mínima global (D-10)"
  key_links:
    - from: "app/(admin)/admin/produtos/page.tsx"
      to: "lib/custo/corrente.ts"
      via: "margensCorrentesBatch (1 query batch, não por-row)"
      pattern: "margensCorrentesBatch"
    - from: "components/admin/produto-form.tsx"
      to: "lib/actions/produtos.ts"
      via: "criarProduto/editarProduto via handleSubmit"
      pattern: "criarProduto|editarProduto"
---

<objective>
UI de produtos e ajustes (surfaces 9, 10, 13): lista com margem batch e linha vermelha, form com margem em tempo real + bloqueio de preço-piso, e a tela de ajustes da margem global.

Purpose: é a tela onde a mãe VÊ o lucro — o núcleo do core value. O alerta de margem e o bloqueio já existem no server (02-06); aqui eles ganham a manifestação visual do UI-SPEC.
Output: /admin/produtos (lista/novo/editar) e /admin/ajustes funcionais.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-financeiro/02-UI-SPEC.md
@.planning/phases/02-motor-financeiro/02-PATTERNS.md

⚠️ Pitfall 9: margem na lista SEM N+1 — usar margensCorrentesBatch (02-03).
⚠️ Pitfall 3: custo corrente cruza como string `.toFixed()`.
⚠️ Regra de cor (UI-SPEC): margem baixa = borda esquerda 4px destructive + ícone TriangleAlert + texto — NUNCA só cor. Margem saudável usa foreground normal (NÃO verde — success é reservado pra Phase 4).
</context>

<interfaces>
Do 02-03: `margensCorrentesBatch()` → `Array<{ produtoId, nome, precoVenda: Decimal, custo: Decimal|null, margem: Decimal|null, minima: Decimal }>`.
Do 02-06: `criarProduto(input)`, `editarProduto(id, input)` (objeto RHF) → `{ ok, id } | { error, fieldErrors }` — o erro PROD-09 vem como `error` com a copy "Esse preço tá abaixo do custo..."; `salvarMargemGlobal(_prev, formData)` (useActionState); `sugestoesCategoria(prefix)`.
Copy do bloco de margem (UI-SPEC, EXATA):
- Saudável: "Custa {R$ X,XX} pra fazer hoje. Vendendo a {R$ Y,YY}, ficam {R$ Z,ZZ} com você ({W}% de margem)."
- Abaixo do mínimo: "Margem abaixo do mínimo ({30}%): de cada R$ 10 vendidos, menos de R$ 3 ficam com você. Vale subir o preço ou rever a receita."
- Bloqueio: "Esse preço tá abaixo do custo ({R$ X,XX}) — você pagaria pra vender. Aumenta o preço pra salvar."
- Caption A1: "Margem = quanto do preço fica com você, já descontado o custo."
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Lista de produtos com margem em batch e linha vermelha</name>
  <files>app/(admin)/admin/produtos/page.tsx</files>
  <read_first>
    - lib/custo/corrente.ts (assinatura real de margensCorrentesBatch)
    - app/(admin)/admin/auditoria/page.tsx (analog lista RSC)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §surface 9 + §Color (destructive reserved)
  </read_first>
  <action>
RSC: h1 "Produtos"; CTA "Novo produto". Chamar `margensCorrentesBatch()` UMA vez (grep-gate: zero custoCorrente* dentro de map). Mobile: cards empilhados; ≥md: `table` shadcn com colunas nome/categoria/tipo/preço/margem (tabular-nums).
Linha com `margem !== null && margem < minima`: borda esquerda 4px `border-l-destructive` + ícone `TriangleAlert` + texto da margem em `text-destructive` (ex.: "22% — abaixo de 30%"). Margem saudável: foreground normal. `custo === null` (sem compras): caption muted "custo incompleto — falta compra de ingrediente".
Badge "Kit" para tipo=KIT. Empty state: "Nenhum produto ainda" + "Produto é o que vai pra vitrine: um doce (ligado a uma receita) ou um kit com vários." Link editar por linha. Formatação BRL via Intl pt-BR server-side.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "margensCorrentesBatch" "app/(admin)/admin/produtos/page.tsx" && grep -c "TriangleAlert" "app/(admin)/admin/produtos/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Exatamente 1 chamada de margensCorrentesBatch; `grep -c "custoCorrenteReceita\|custoCorrenteProduto" page.tsx` == 0
    - Linha vermelha tem os 3 canais: borda + ícone + texto (grep confirma TriangleAlert e destructive)
    - Copy exata do empty state presente
  </acceptance_criteria>
  <done>Lista de produtos com margem visível e alerta multi-canal, sem N+1.</done>
</task>

<task type="auto">
  <name>Task 2: Form de produto com margem em tempo real (RHF, PROD-08/09, D-09/D-10/D-11)</name>
  <files>components/admin/produto-form.tsx, app/(admin)/admin/produtos/novo/page.tsx, app/(admin)/admin/produtos/[id]/editar/page.tsx</files>
  <read_first>
    - components/admin/receita-form.tsx (padrão RHF já estabelecido no 02-05 — replicar estrutura)
    - lib/actions/produtos.ts (assinaturas + shape do erro PROD-09)
    - components/ui/form.tsx, components/ui/combobox.tsx, components/ui/select.tsx
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §surface 10
  </read_first>
  <action>
1. `components/admin/produto-form.tsx` (`'use client'`, RHF). Props serializáveis:
```typescript
type ProdutoFormProps = {
  receitas: Array<{ id: string; nome: string; custoPorUnidade: string | null }>       // p/ UNITARIO (custo corrente por unidade, .toFixed(6), null = faltam compras)
  unitarios: Array<{ id: string; nome: string; custoPorUnidade: string | null }>      // componentes p/ KIT
  margemMinimaGlobal: string                                                          // ex. "30.00"
  defaults?: { id: string; nome: string; descricao: string; categoria: string; tipo: 'UNITARIO'|'KIT'; precoVenda: string; margemMinimaOverride: string | null; receitaId: string | null; kitItens: Array<{ componenteId: string; qtde: number }> }
}
```
Campos (copy exata UI-SPEC): "Nome" · "Descrição" (textarea) · "Categoria" (combobox livre + sugestoesCategoria) · "É doce único ou kit?" ("Doce único" / "Kit") · "Receita" (select, só UNITARIO) · "Itens do kit" (useFieldArray de componente+qtde, só KIT) · "Preço de venda (R$)" (`inputMode="decimal"`) · "Margem mínima só desse produto (%) — se quiser diferente do padrão".
**Bloco Margem (D-09, reativo ≤300ms debounce, decimal.js):** custo = receita selecionada (custoPorUnidade) ou Σ kit (D-11). Três estados:
   - saudável: copy "Custa {R$ X,XX} pra fazer hoje. Vendendo a {R$ Y,YY}, ficam {R$ Z,ZZ} com você ({W}% de margem)." + caption "Margem = quanto do preço fica com você, já descontado o custo."
   - margem < mínima (override ?? global): container com `border-l-4 border-destructive` + TriangleAlert + copy "Margem abaixo do mínimo ({X}%): de cada R$ 10 vendidos, menos de R$ 3 ficam com você. Vale subir o preço ou rever a receita." em 16px text-destructive
   - preço < custo (PROD-09): Alert destructive acima do CTA com a copy do bloqueio + CTA `disabled` (cortesia — o server é quem bloqueia). Sem custo (null): sem bloco, caption "custo incompleto".
Margem calculada com a MESMA definição A1: `(preco − custo) ÷ preco × 100` via Decimal.
Submit: handleSubmit → criarProduto/editarProduto; `res.error` renderizado em Alert (inclui o caso do server rejeitar PROD-09). Sucesso → router.push('/admin/produtos'). CTA sticky "Salvar produto".
2. Pages novo/ e [id]/editar/ (RSC): carregam receitas disponíveis (sem produto vinculado OU a do próprio produto no editar — Produto.receitaId é @unique), unitarios (`tipo: 'UNITARIO'`), config global (`configuracao.findUnique({ where: { id: 1 } })` default 30) e serializam custos correntes com `ultimasCompras`/helpers do lib/custo em BATCH (1 chamada). `notFound()` no editar sem row.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "pra fazer hoje" components/admin/produto-form.tsx && grep -c "você pagaria pra vender" components/admin/produto-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Copy exata dos 3 estados do bloco de margem + caption A1 presentes
    - `grep -c "new Decimal" components/admin/produto-form.tsx` ≥ 1 e `grep -cE "parseFloat|Number\(" components/admin/produto-form.tsx` == 0 em caminho de dinheiro
    - `grep -c "useFieldArray" components/admin/produto-form.tsx` == 1 (itens do kit)
    - `grep -c "disabled" components/admin/produto-form.tsx` ≥ 1 no CTA quando preço<custo
    - `grep -c "type=\"number\"" components/admin/produto-form.tsx` == 0
  </acceptance_criteria>
  <done>Form de produto com margem viva nos 3 estados e kit funcional.</done>
</task>

<task type="auto">
  <name>Task 3: Ajustes — margem mínima global (D-10)</name>
  <files>app/(admin)/admin/ajustes/page.tsx, components/admin/ajustes-form.tsx</files>
  <read_first>
    - lib/actions/config.ts (salvarMargemGlobal)
    - app/(admin-auth)/admin/entrar/page.tsx (analog useActionState)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §surface 13
  </read_first>
  <action>
1. Page (RSC): h1 "Ajustes"; carrega `configuracao.findUnique({ where: { id: 1 } })` (default 30 se ausente) e passa o valor atual como string.
2. `components/admin/ajustes-form.tsx` (`'use client'`, useActionState com salvarMargemGlobal): label "Margem mínima padrão (%)" + caption "Abaixo disso, o produto ganha um aviso vermelho. Hoje: {30}%." (interpolar valor atual); input `inputMode="decimal"`; CTA "Salvar ajustes" (pending "Salvando..."); sucesso → toast "Salvo!" (sonner).
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "Margem mínima padrão" components/admin/ajustes-form.tsx && grep -c "Salvar ajustes" components/admin/ajustes-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Copy exata: "Margem mínima padrão (%)", "ganha um aviso vermelho", "Salvar ajustes", toast "Salvo!"
    - `grep -c "useActionState" components/admin/ajustes-form.tsx` == 1
    - `rtk npm test` continua verde
  </acceptance_criteria>
  <done>/admin/ajustes salva o global; produto usa override quando presente (já provado no 02-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → actions de produto/config | Preview de margem é client-side; decisão de salvar é server (02-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | bypass do disabled do CTA (PROD-09) | mitigate | Bloqueio real é server-side (02-06, testado); o disabled é cortesia declarada como tal no código |
| T-02-18 | Information Disclosure | custos serializados nas pages | mitigate | Pages sob (admin) com role-gate; custos nunca renderizados em rota pública |
</threat_model>

<verification>
- `rtk npx tsc --noEmit` — 0 erros
- `rtk npm test` — suite completa verde
- Verificação visual dos 3 estados de margem no checkpoint do 02-11
</verification>

<success_criteria>
- Lista com margem batch e alerta multi-canal (borda+ícone+texto)
- Form com margem reativa, override D-10, kit D-11 e dupla camada PROD-09 (client cortesia + server real)
- Ajustes global funcional com copy exata
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-financeiro/02-09-SUMMARY.md`
</output>
