---
phase: 02-motor-financeiro
plan: 06
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - lib/validation/produtos.ts
  - lib/validation/config.ts
  - lib/actions/produtos.ts
  - lib/actions/config.ts
  - tests/financeiro/produtos.test.ts
autonomous: true
requirements: [PROD-01, PROD-02, PROD-08, PROD-09]
must_haves:
  truths:
    - "Salvar produto com preço < custo absoluto é BLOQUEADO server-side (não é só o client que impede)"
    - "Mudança de preço gera audit preco_alterado com antes/depois em metadata"
    - "Margem mínima global (default 30%) é editável e o override por produto tem precedência"
    - "Kit valida componentes UNITARIO e custo do kit = soma dos componentes (D-11)"
  artifacts:
    - path: "lib/actions/produtos.ts"
      provides: "criarProduto, editarProduto com lookup de custo server-side (PROD-09)"
    - path: "lib/actions/config.ts"
      provides: "salvarMargemGlobal (singleton id=1, D-10)"
    - path: "tests/financeiro/produtos.test.ts"
      provides: "PROD-09 block, EoP, kit D-11, config"
  key_links:
    - from: "lib/actions/produtos.ts"
      to: "lib/custo/corrente.ts"
      via: "custoCorrenteProduto/custoCorrenteReceita ANTES de salvar"
      pattern: "custoCorrente"
    - from: "lib/actions/produtos.ts"
      to: "lib/audit/log.ts"
      via: "logAudit preco_alterado com metadata { antes, depois }"
      pattern: "preco_alterado"
---

<objective>
Backend de produtos e configuração: schemas, actions com o bloqueio preço<custo server-side (PROD-09 — o requisito de segurança de negócio da fase), audit de preço, config singleton de margem mínima (D-10), e a suite de testes incluindo o teste de EoP com requireAdmin.

Purpose: o preço é a única alavanca de lucro da mãe — o sistema impede venda no prejuízo por construção. A UI (02-09) só exibe o que estas actions decidem.
Output: 4 arquivos lib + produtos.test.ts verde.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-financeiro/02-RESEARCH.md
@.planning/phases/02-motor-financeiro/02-PATTERNS.md

⚠️ Test DB == dev DB: rodar vitest apaga dados do dev.
⚠️ PROD-09 é validação SERVER-SIDE na action (precisa lookup de custo no DB) — Zod sozinho não basta; o client é só cortesia.
</context>

<interfaces>
Do 02-03: `custoCorrenteProduto(produtoId)` (para editar) e `custoCorrenteReceita(receitaId)` / soma de componentes (para criar — o produto ainda não existe; compor com os mesmos helpers), `margemPercent`, `requireAdmin`, `zDecimalBRL`.
Skeleton de action e ActionState: lib/actions/auth.ts (mesma ordem dos planos 02-04/05).
logAudit: `logAudit({ actorType: 'admin', actorId, action: 'preco_alterado', entityType: 'produto', entityId, metadata: { antes: '10.0000', depois: '12.0000' }, rawIp, rawUa })`.
Copy PROD-09 (UI-SPEC, EXATA): "Esse preço tá abaixo do custo ({R$ X,XX}) — você pagaria pra vender. Aumenta o preço pra salvar."
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Validação + actions de produtos e config</name>
  <files>lib/validation/produtos.ts, lib/validation/config.ts, lib/actions/produtos.ts, lib/actions/config.ts</files>
  <read_first>
    - lib/actions/auth.ts (skeleton)
    - lib/custo/corrente.ts (assinaturas reais de custoCorrenteReceita/custoCorrenteProduto/margemPercent)
    - lib/audit/log.ts (assinatura logAudit)
    - .planning/phases/02-motor-financeiro/02-UI-SPEC.md §"Produtos e margem (surfaces 9–10)" (copy)
  </read_first>
  <action>
1. `lib/validation/produtos.ts`:
```typescript
export const ProdutoSchema = z.object({
  nome: z.string().trim().min(1, OBRIGATORIO),
  descricao: z.string().trim().min(1, OBRIGATORIO),
  categoria: z.string().trim().min(1, OBRIGATORIO),
  tipo: z.enum(['UNITARIO', 'KIT']),
  precoVenda: zDecimalBRL,
  margemMinimaOverride: zDecimalBRL.optional().or(z.literal('').transform(() => undefined)),
  receitaId: z.string().uuid().optional().or(z.literal('').transform(() => undefined)),
  kitItens: z.array(z.object({ componenteId: z.string().uuid(), qtde: z.coerce.number().int().min(1) })).optional(),
}).superRefine((v, ctx) => {
  // UNITARIO exige receitaId e proíbe kitItens; KIT exige kitItens (≥1) e proíbe receitaId (D-11/REC-01)
})
```
2. `lib/validation/config.ts`: `ConfigSchema = z.object({ margemMinimaPadrao: zDecimalBRL.refine(v => v.gte(0) && v.lt(100), 'Usa um número entre 0 e 99.') })`.
3. `lib/actions/produtos.ts` (`'use server'`): `criarProduto(input: unknown)` e `editarProduto(id, input: unknown)` (objeto RHF), skeleton padrão (context, rate limit, requireAdmin em try, safeParse).
   **PROD-09 (core):** ANTES de salvar, computar custo server-side: UNITARIO → `custoCorrenteReceita(receitaId).total ÷ rendimentoPadrao` (via helper — usar exatamente o que lib/custo expõe); KIT → Σ(qtde × custo por unidade do componente) via helpers de lib/custo (D-11). Se `precoVenda.lt(custo)` → retornar `{ error: 'Esse preço tá abaixo do custo (R$ {custo com 2 casas pt-BR}) — você pagaria pra vender. Aumenta o preço pra salvar.' }` e NÃO salvar. Se faltamCompras cobre TODOS os itens (custo nulo/zero indeterminado) → permitir salvar SEM bloqueio (não há custo pra comparar) mas sem falso alerta.
   **KIT:** validar que todo componenteId referencia produto `tipo === 'UNITARIO'` (app-level — CHECK não cruza rows); persistir em transação: produto + `produtoKitItem.createMany` (replace-all no editar).
   **Audit:** no editar, se `precoVenda` mudou → `logAudit preco_alterado` com metadata `{ antes: old.toFixed(4), depois: novo.toFixed(4) }`. No criar → sem preco_alterado (não há "antes").
   `revalidatePath('/admin/produtos')`. Helper `sugestoesCategoria(prefix)` (analog sugestoesMarca do 02-04, `SELECT DISTINCT categoria FROM produtos WHERE categoria ILIKE ... LIMIT 10`, requireAdmin com catch → []).
4. `lib/actions/config.ts` (`'use server'`): `salvarMargemGlobal(_prev, formData)` — skeleton padrão (FormData; form simples useActionState). `prisma.configuracao.upsert({ where: { id: 1 }, create: { id: 1, margemMinimaPadrao }, update: { margemMinimaPadrao } })`. `revalidatePath('/admin')` + `revalidatePath('/admin/produtos')` + `revalidatePath('/admin/ajustes')`. Retorna `{ ok: true, message: 'Salvo!' }`.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit && grep -c "preco_alterado" lib/actions/produtos.ts && grep -c "upsert" lib/actions/config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requireAdmin()" lib/actions/produtos.ts` ≥ 3 e `grep -c "requireAdmin()" lib/actions/config.ts` == 1
    - `grep -c "Esse preço tá abaixo do custo" lib/actions/produtos.ts` ≥ 1
    - `grep -c "UNITARIO" lib/actions/produtos.ts` ≥ 1 (validação de componente de kit)
    - `grep -cE "z\.coerce.*precoVenda|parseFloat|Number\(" lib/actions/produtos.ts lib/validation/produtos.ts` == 0 em caminhos de dinheiro (coerce só em qtde int de kit)
  </acceptance_criteria>
  <done>Actions de produto com bloqueio de preço-piso server-side, kit validado, audit de preço e config singleton.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes de produtos e config (PROD-09, D-11, EoP, audit)</name>
  <files>tests/financeiro/produtos.test.ts</files>
  <read_first>
    - tests/auth/password-reset.test.ts (padrão de mocks 01-08)
    - tests/financeiro/fixtures.ts (criarIngrediente, registrarCompra, criarReceita, criarProduto)
    - lib/actions/produtos.ts e lib/actions/config.ts (assinaturas reais)
  </read_first>
  <behavior>
    - PROD-09 bloqueia: receita com custo/un 0.50 (fixture), criarProduto UNITARIO com precoVenda '0,40' → { error } contendo "abaixo do custo" e NENHUMA row criada
    - PROD-09 permite: precoVenda '2,00' → { ok }, row criada com precoVenda '2.0000'
    - D-11 kit: 2 componentes unitários com custos conhecidos; criarProduto KIT com preço abaixo da soma → bloqueado; acima → ok
    - Kit inválido: componenteId apontando pra outro KIT → { error } (app-level check)
    - Audit: editarProduto mudando preço 2,00 → 3,50 gera audit_log row com action 'preco_alterado' e metadata contendo '2.0000' e '3.5000'
    - Config: salvarMargemGlobal '25' → configuracoes.id=1 com margemMinimaPadrao '25.00'; segunda chamada '35' faz update (continua 1 row)
    - EoP (T-02-06): sessão customer → criarProduto e salvarMargemGlobal retornam { error } genérico e nada é gravado — este é O teste de requireAdmin da fase
  </behavior>
  <action>
Criar `tests/financeiro/produtos.test.ts` no padrão 01-08 (vi.hoisted ctx com cookie mutável, mock next/headers com cookie de sessão real mintada via createTestUser({role:'admin'}) + signInAsAdmin, mock next/navigation, IP randomizado por teste, beforeEach(truncateAll)). Implementar todos os casos do behavior. Decimal via `.toFixed(n)`.
  </action>
  <verify>
    <automated>rtk npx vitest run tests/financeiro/produtos.test.ts --reporter=dot && rtk npm test</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo contém "abaixo do custo", "preco_alterado" e um caso com signInAsCustomer
    - Caso de audit verifica metadata com os dois valores ('2.0000' e '3.5000')
    - Suite completa verde
  </acceptance_criteria>
  <done>PROD-09, D-10, D-11 e a autorização admin provados por integração.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → actions de produto/config | Preço e margem vêm do client; custo NUNCA vem do client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege | criarProduto/editarProduto/salvarMargemGlobal | mitigate | requireAdmin() + teste EoP dedicado (Task 2) |
| T-02-13 | Tampering | bypass do bloqueio PROD-09 | mitigate | Custo recomputado server-side via lib/custo a partir das rows FK'adas; client manda só IDs+preço; teste prova o bloqueio |
| T-02-14 | Repudiation | mudança de preço | mitigate | logAudit preco_alterado com metadata antes/depois + IP/UA hasheados (AUTH-11) |
| T-02-15 | Tampering | margem global | mitigate | Singleton CHECK (id=1, do 02-01) + upsert where id:1; validação 0≤v<100 |
</threat_model>

<verification>
- `rtk npx vitest run tests/financeiro --reporter=dot` — verde
- `rtk npm test` — suite completa verde
- `rtk npx tsc --noEmit` — 0 erros
</verification>

<success_criteria>
- PROD-09 é impossível de contornar via client (provado por teste)
- D-10: global default 30 + override por produto persistidos
- D-11: kit soma componentes e valida tipo UNITARIO
- preco_alterado auditado com antes/depois
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-financeiro/02-06-SUMMARY.md`
</output>
