---
phase: 01-foundation
plan: 09
type: execute
gap_closure: true
wave: 3
depends_on: [03, 04]
autonomous: true
files_modified:
  - scripts/seed-admin.ts
  - tests/admin/seed-admin.test.ts
requirements: [AUTH-10, AUTH-11]
threat_model:
  - id: T-01-09-01
    severity: high
    category: Information Disclosure
    note: "Senha inicial/reset do admin vazando em log ou audit"
    mitigation: "Senha lida só de env (ADMIN_INITIAL_PASSWORD/ADMIN_RESET_PASSWORD); audit grava apenas email_hash/admin_id (nunca a senha); console.log não imprime senha; D-05 entrega pessoalmente"
  - id: T-01-09-02
    severity: high
    category: Elevation of Privilege
    note: "Múltiplos admins ou reset no admin errado"
    mitigation: "D-07: --reset exige EXATAMENTE 1 admin (falha se 0 ou >1); seed é idempotente (não recria se admin já existe)"
  - id: T-01-09-03
    severity: medium
    category: Repudiation
    note: "Bootstrap/reset sem trilha"
    mitigation: "logAudit admin_seed_via_cli / admin_password_reset_via_cli (actorType='cli', actorId=null)"

must_haves:
  truths:
    - "pnpm seed:admin cria o admin único (role=admin, emailVerified=true) se não existir"
    - "pnpm seed:admin é idempotente: roda 2x não cria um segundo admin"
    - "pnpm seed:admin --reset troca a senha do admin e revoga sessões ativas"
    - "--reset falha se houver 0 ou >1 admin"
    - "Ambos os caminhos gravam em audit_log (admin_seed_via_cli / admin_password_reset_via_cli) sem expor a senha"
  artifacts:
    - path: "scripts/seed-admin.ts"
      provides: "CLI bootstrap + break-glass reset"
      contains: "admin_seed_via_cli"
  key_links:
    - from: "scripts/seed-admin.ts"
      to: "lib/auth/server.ts"
      via: "auth.api.signUpEmail / setUserPassword / revokeUserSessions"
      pattern: "auth\\.api\\.(signUpEmail|setUserPassword|revokeUserSessions)"
    - from: "scripts/seed-admin.ts"
      to: "lib/audit/log.ts"
      via: "logAudit"
      pattern: "logAudit"
---

<objective>
Implementar o script CLI `scripts/seed-admin.ts` que cria o admin único da mãe (D-05) e faz reset break-glass de senha (D-07), com auditoria. O script é referenciado por `package.json` (`seed:admin`) mas o arquivo não existe — `npm run seed:admin` falha hoje, então o admin não pode ser criado.

Purpose: Sem este script não há como bootstrapar o admin único; AUTH-10 fica sem nenhum admin operável.
Output: scripts/seed-admin.ts (seed + --reset) + teste de integração.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-1-foundation/01-CONTEXT.md
@.planning/phase-1-foundation/01-RESEARCH.md
@.planning/phase-1-foundation/01-PATTERNS.md

<interfaces>
lib/auth/server.ts (Plan 03/04) — auth.api.signUpEmail({ body:{ email, password, name } }); admin plugin: auth.api.setUserPassword({ body:{ userId, newPassword } }), auth.api.revokeUserSessions({ body:{ userId } }).
lib/db/client.ts (Plan 02) — prisma.user.findFirst/findMany/update.
lib/audit/log.ts (Plan 04) — logAudit.
lib/env.ts (Plan 01) — env.ADMIN_EMAIL, env.ADMIN_INITIAL_PASSWORD?, env.ADMIN_RESET_PASSWORD?.
package.json (Plan 01) — script "seed:admin": "tsx scripts/seed-admin.ts" JÁ existe; tsx em devDeps. (NÃO modificar package.json — sem colisão de arquivo com o Plan 06 na wave 3.)
Verbatim: RESEARCH §Bootstrap CLI Scripts (lines 1838-1903) + §Auth Flows F6/F7 (889-925). PATTERNS §2.25.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/seed-admin.ts (seed D-05 + --reset D-07) + teste de integração</name>
  <files>scripts/seed-admin.ts, tests/admin/seed-admin.test.ts</files>
  <read_first>
    - .planning/phase-1-foundation/01-RESEARCH.md §Bootstrap CLI Scripts (lines 1838-1903) — código verbatim
    - .planning/phase-1-foundation/01-RESEARCH.md §Auth Flows F6/F7 (889-925)
    - .planning/phase-1-foundation/01-CONTEXT.md D-05, D-07
    - lib/auth/server.ts, lib/audit/log.ts, lib/env.ts
    - tests/conftest.ts — truncateAll/createTestUser
  </read_first>
  <behavior>
    - seedAdmin() com nenhum admin existente: cria User role=admin, emailVerified=true, isAdult=true, terms/privacyVersion='v1.0-shell' + accepted_at; logAudit admin_seed_via_cli (metadata.email_hash = sha256(email), nunca a senha)
    - seedAdmin() rodando de novo com admin existente: NÃO cria segundo admin (idempotente); loga "já existe"
    - resetAdmin() com exatamente 1 admin: setUserPassword + revokeUserSessions + logAudit admin_password_reset_via_cli
    - resetAdmin() com 0 admins: lança erro; com >1 admins: lança erro
    - Nenhum caminho imprime/persiste a senha em plaintext
  </behavior>
  <action>
    Criar `scripts/seed-admin.ts` per RESEARCH 1838-1903, refatorado para testabilidade: exportar `seedAdmin()` e `resetAdmin()` e ter um wrapper `main()` que lê `process.argv.includes('--reset')` e chama o caminho certo (`main().catch(e => { console.error(e); process.exit(1) })`).
    `seedAdmin()`: se `!env.ADMIN_INITIAL_PASSWORD` -> throw; `prisma.user.findFirst({ where:{ role:'admin', deletedAt:null } })` -> se existe, log e return; senão `auth.api.signUpEmail({ body:{ email: env.ADMIN_EMAIL, password: env.ADMIN_INITIAL_PASSWORD, name:'Administradora' } })` + `prisma.user.update` (role='admin', emailVerified:true, isAdult:true, terms/privacyVersion:'v1.0-shell', accepted_at:now()) + `logAudit({ actorType:'cli', actorId:null, action:'admin_seed_via_cli', metadata:{ email_hash: sha256(env.ADMIN_EMAIL) } })`.
    `resetAdmin()`: se `!env.ADMIN_RESET_PASSWORD` -> throw; `findMany({ where:{ role:'admin', deletedAt:null } })`; 0 -> throw "No admin found"; >1 -> throw "Multiple admins"; 1 -> `auth.api.setUserPassword({ body:{ userId, newPassword: env.ADMIN_RESET_PASSWORD } })` + `auth.api.revokeUserSessions({ body:{ userId } })` + logAudit admin_password_reset_via_cli (metadata.admin_id).
    NUNCA logar a senha. Não usar 'dotenv/config' se env já carrega via prisma.config/process — confirmar carregamento de .env no contexto do tsx (usar `process.loadEnvFile()` se necessário, como no vitest.config).
    Criar `tests/admin/seed-admin.test.ts` cobrindo os 5 comportamentos (truncateAll antes; setar env.ADMIN_* via vi/stub se preciso).
  </action>
  <acceptance_criteria>
    - `grep -c 'export async function seedAdmin' scripts/seed-admin.ts` == 1
    - `grep -c 'export async function resetAdmin' scripts/seed-admin.ts` == 1
    - `grep -c 'admin_seed_via_cli' scripts/seed-admin.ts` == 1
    - `grep -c 'admin_password_reset_via_cli' scripts/seed-admin.ts` == 1
    - `grep -c 'revokeUserSessions' scripts/seed-admin.ts` == 1
    - `grep -E -c 'Multiple admins|>1' scripts/seed-admin.ts` >= 1
    - `npx vitest run tests/admin/seed-admin.test.ts` PASS
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/admin/seed-admin.test.ts --reporter=dot</automated>
  </verify>
  <done>seed cria admin único idempotente; --reset troca senha + revoga sessões com guarda 0/1/>1; auditoria sem vazar senha; teste verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev (SSH) -> CLI seed | Único caminho de bootstrap/reset admin (sem rota web — D-05/D-07) |
| CLI -> audit_log | Trilha sem segredos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09-01 | Information Disclosure | scripts/seed-admin.ts | mitigate | senha só de env; audit grava email_hash/admin_id; sem print de senha (block-on:high) |
| T-01-09-02 | Elevation of Privilege | resetAdmin/seedAdmin | mitigate | exatamente-1-admin no reset; seed idempotente (block-on:high) |
| T-01-09-03 | Repudiation | logAudit | mitigate | admin_seed_via_cli / admin_password_reset_via_cli |
</threat_model>

<verification>
- Pré-requisito: `docker start doces-pg` + `.env` com ADMIN_EMAIL/ADMIN_INITIAL_PASSWORD (e ADMIN_RESET_PASSWORD para testar reset).
- `npx vitest run tests/admin/seed-admin.test.ts` verde + `npx vitest run` sem regressão.
- `npx tsc --noEmit` exit 0.
</verification>

<success_criteria>
- AUTH-10: admin único bootstrapável via CLI; D-05 e D-07 implementados.
- AUTH-11: admin_seed_via_cli e admin_password_reset_via_cli gravados.
</success_criteria>

<output>
Após completar, criar `.planning/phase-1-foundation/01-09-SUMMARY.md` usando @E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md.
</output>
</content>
