---
phase: 01-foundation
plan: 07
type: execute
gap_closure: true
wave: 4
depends_on: [03, 06]
autonomous: false
files_modified:
  - package.json
  - components.json
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - components/header.tsx
  - components/footer.tsx
  - components/toaster.tsx
  - components/ui/
  - app/(public)/termos/page.tsx
  - app/(public)/privacidade/page.tsx
  - tests/lgpd/footer-dpo.test.tsx
  - tests/lgpd/privacy-content.test.tsx
requirements: [LGPD-02, LGPD-03, LGPD-06]
threat_model:
  - id: T-01-07-01
    severity: medium
    category: Tampering
    note: "CSP nonce do proxy.ts precisa propagar para scripts nas novas páginas"
    mitigation: "Não usar <script> inline sem nonce; Next injeta nonce via proxy.ts (x-nonce); componentes são RSC/server por padrão; Toaster é o único 'use client'"
  - id: T-01-07-02
    severity: medium
    category: Information Disclosure
    note: "Header revela estado de sessão (cliente vs admin) com flicker/leak"
    mitigation: "Header é RSC lendo auth.api.getSession server-side (D-02/D-04); sem flicker, sem expor role em atributo client"
  - id: T-01-07-03
    severity: low
    category: Repudiation
    note: "Política de privacidade sem operadores reais viola LGPD art. 5/33"
    mitigation: "Conteúdo /privacidade lista Resend (EUA), Cloudflare (EUA), Hostinger (Lituânia) + retenção 5 anos + DPO (LGPD-03); teste regex garante presença"

must_haves:
  truths:
    - "shadcn inicializado com a paleta pink-bordô da UI-SPEC (accent #9D2D7A, background #FFF6FB)"
    - "Visitante não-logado em / vê CTAs 'Criar minha conta' + 'Entrar' (D-01)"
    - "Cliente logado em / vê 'Minha conta' + 'Sair' (D-02); admin vê 'Painel admin' + 'Sair' (D-04)"
    - "Footer global mostra o email do DPO dpo@docesvalentina.com.br (LGPD-06) e links Termos/Privacidade"
    - "Footer NÃO aparece em /admin/* (D-03)"
    - "/privacidade lista os operadores reais (Resend, Cloudflare, Hostinger) e retenção de 5 anos (LGPD-03)"
  artifacts:
    - path: "app/page.tsx"
      provides: "Landing 3-variant por sessão"
      contains: "Criar minha conta"
    - path: "components/footer.tsx"
      provides: "Footer global D-03 + DPO"
      contains: "dpo@docesvalentina.com.br"
    - path: "app/(public)/privacidade/page.tsx"
      provides: "Política descritiva LGPD-03"
      contains: "Resend"
    - path: "app/globals.css"
      provides: "Tokens de cor UI-SPEC"
      contains: "#9D2D7A"
  key_links:
    - from: "app/layout.tsx"
      to: "components/header.tsx + components/footer.tsx + components/toaster.tsx"
      via: "render no root layout"
      pattern: "Header|Footer|Toaster"
    - from: "components/header.tsx"
      to: "lib/auth/server.ts"
      via: "auth.api.getSession"
      pattern: "getSession"
    - from: "components/footer.tsx"
      to: "/termos + /privacidade"
      via: "links"
      pattern: "/privacidade"
---

<objective>
Estabelecer o shell visual de toda a Phase 1: inicializar shadcn com a paleta pink-bordô da UI-SPEC, montar header global (CTAs condicionais por sessão — D-01/D-02/D-04), footer global com DPO (D-03/LGPD-06), provider de toast (sonner), a landing da raiz, e as páginas públicas /termos e /privacidade (LGPD-02/LGPD-03). É o pré-requisito visual das páginas de auth (Plans 08/10) e LGPD (Plan 11).

Purpose: Os layouts role-gate existem mas não há nenhuma página, header, footer, toaster nem design system — a aplicação não renderiza nada além do scaffold Vercel.
Output: shadcn + globals.css (tokens) + layout/header/footer/toaster + landing + termos/privacidade.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-1-foundation/01-UI-SPEC.md
@.planning/phase-1-foundation/01-CONTEXT.md
@.planning/phase-1-foundation/01-RESEARCH.md
@.planning/phase-1-foundation/01-PATTERNS.md

<interfaces>
app/layout.tsx (scaffold) — atualmente lang="en", metadata "Create Next App", Geist Sans/Mono wired. MODIFICAR: lang="pt-BR", Fraunces, metadata real, <Header/>{children}<Footer/><Toaster/>.
app/globals.css (scaffold) — Tailwind v4 @import + @theme inline (font-sans/mono). MODIFICAR: tokens UI-SPEC §Color; REMOVER @media prefers-color-scheme dark; adicionar --font-display=Fraunces.
app/page.tsx (scaffold Vercel) — REPLACE por landing.
lib/auth/server.ts (Plan 03) — auth.api.getSession({ headers: await nextHeaders() }) (Next 16 async headers).
Footer NÃO no /admin/* (D-03): app/(admin)/admin/layout.tsx (Plan 03) é shell próprio sem footer — root layout NÃO deve forçar footer em admin (ver UI-SPEC §Mobile-First). Estratégia: admin layout sobrescreve; OU footer condicional. Preferir admin layout próprio sem render do Footer global.
</interfaces>

UI-SPEC é o contrato literal: §Color (12 tokens), §Typography (Fraunces wordmark), §Copywriting (landing/footer/termos/privacidade copy), §Component Inventory (11 shadcn), §Mobile-First Layout (header 56/64px). Conteúdo /privacidade: RESEARCH §LGPD-03 (lines 1231-1261, verbatim). Landing variants: CONTEXT D-01/D-02/D-04.
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn init + 11 componentes + tokens de cor UI-SPEC em globals.css</name>
  <files>package.json, components.json, app/globals.css, components/ui/</files>
  <read_first>
    - .planning/phase-1-foundation/01-UI-SPEC.md §Design System + §Color + §Component Inventory + §Registry Safety
    - .planning/phase-1-foundation/01-PATTERNS.md §3.16, §3.18
    - app/globals.css — estado atual (Tailwind v4 + @theme inline)
  </read_first>
  <action>
    Rodar `rtk npx shadcn@latest init` com preset new-york + baseColor neutral + CSS vars enabled (componente em `components/ui/`, alias `@/components`). Em seguida `rtk npx shadcn@latest add button input label form checkbox card alert sonner separator skeleton table` (SÓ registry oficial https://ui.shadcn.com — NÃO instalar dialog/select/combobox/popover/dropdown-menu). Instalar `rtk npm install lucide-react@1.14.0 sonner@2.0.7 date-fns@4.1.0`.
    Editar `app/globals.css`: substituir os defaults (`--background`/`--foreground`) pelos 12 tokens da UI-SPEC §Color (--color-background #FFF6FB, --color-surface #FFFFFF, --color-border #F5DCEC, --color-accent #9D2D7A, --color-accent-soft #ED91DB, --color-accent-foreground #FFFFFF, --color-destructive #B91C1C, --color-destructive-foreground #FFFFFF, --color-foreground #2A0F23, --color-foreground-muted #7A5868, --color-ring #9D2D7A, --color-success #15803D). REMOVER o bloco `@media (prefers-color-scheme: dark)` (Phase 1 não habilita dark mode). MANTER `@theme inline { --font-sans, --font-mono }`; adicionar `--font-display: var(--font-fraunces)`.
  </action>
  <acceptance_criteria>
    - `test -f components.json` exit 0
    - `ls components/ui/button.tsx components/ui/card.tsx components/ui/checkbox.tsx components/ui/sonner.tsx` exit 0
    - `grep -c '#9D2D7A' app/globals.css` >= 1
    - `grep -c '#FFF6FB' app/globals.css` >= 1
    - `grep -c 'prefers-color-scheme' app/globals.css` == 0
    - `grep -c 'lucide-react' package.json` == 1 && `grep -c 'date-fns' package.json` == 1
    - `grep -c 'dialog' components.json` == 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>shadcn inicializado (11 componentes oficiais); paleta pink-bordô nos tokens; dark-mode removido; type-check passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: layout root (header/footer/toaster/Fraunces/pt-BR) + landing + termos/privacidade + testes</name>
  <files>app/layout.tsx, app/page.tsx, components/header.tsx, components/footer.tsx, components/toaster.tsx, app/(public)/termos/page.tsx, app/(public)/privacidade/page.tsx, tests/lgpd/footer-dpo.test.tsx, tests/lgpd/privacy-content.test.tsx</files>
  <read_first>
    - .planning/phase-1-foundation/01-UI-SPEC.md §Copywriting (landing/footer/termos/privacidade literais) + §Mobile-First Layout + §Typography
    - .planning/phase-1-foundation/01-CONTEXT.md D-01/D-02/D-03/D-04
    - .planning/phase-1-foundation/01-RESEARCH.md §LGPD-03 (lines 1231-1261) — conteúdo verbatim de /privacidade
    - .planning/phase-1-foundation/01-PATTERNS.md §3.1, §3.7, §3.15, §3.17
    - app/(admin)/admin/layout.tsx (Plan 03) — confirmar que o admin shell NÃO renderiza o Footer global (D-03)
    - node_modules/next/dist/docs — next/font/google (Fraunces), metadata API Next 16
  </read_first>
  <behavior>
    - Render de <Footer/> contém o texto exato "dpo@docesvalentina.com.br" e links "Termos"/"Privacidade" (LGPD-06)
    - Render de /privacidade contém "Resend", "Cloudflare", "Hostinger" e "5 anos" (LGPD-03)
    - Header com sessão null -> textos "Criar minha conta" + "Entrar"; com role customer -> "Minha conta" + "Sair"; com role admin -> "Painel admin" + "Sair" (testes podem mockar auth.api.getSession)
  </behavior>
  <action>
    Editar `app/layout.tsx`: `lang="pt-BR"`; importar Fraunces de next/font/google (variable) + manter Geist; `metadata = { title: 'Doces Valentina', description: ... }`; viewport `width=device-width, initial-scale=1, viewport-fit=cover`; render `<Header/>{children}<Footer/><Toaster/>`.
    Criar `components/header.tsx` (RSC): wordmark "Doces Valentina" em Fraunces (UI-SPEC §Typography); lê `auth.api.getSession({ headers: await nextHeaders() })` e troca os CTAs conforme D-01/D-02/D-04. Logout via `POST /api/auth/sign-out` (botão "Sair").
    Criar `components/footer.tsx` (RSC estático) per D-03: `Doces Valentina · [Termos](/termos) · [Privacidade](/privacidade) · Dúvidas sobre seus dados? dpo@docesvalentina.com.br` (LGPD-06). 1 linha mobile / 2 linhas ≥md.
    Criar `components/toaster.tsx` (`'use client'`) envolvendo `Toaster` do sonner (top-center mobile / top-right ≥md, 5s success / 8s error).
    Criar `app/page.tsx` (RSC) landing per D-01: hero "Em breve: reserva os doces caseiros da Valentina" + wordmark Fraunces + 2 parágrafos institucionais (voz vizinha) + CTAs condicionais (mesma lógica do header). Apagar imports next/image + assets Vercel.
    Criar `app/(public)/termos/page.tsx`: intro literal UI-SPEC "Estes são os termos que valem desde [data]..." + shell de termos placeholder (version 'v1.0-shell').
    Criar `app/(public)/privacidade/page.tsx`: conteúdo verbatim de RESEARCH §LGPD-03 (coleta, base legal, operadores Resend/Cloudflare/Hostinger, retenção 5 anos, direitos art.18, DPO).
    Garantir (D-03) que o Footer global NÃO renderiza em /admin/* — o admin layout (Plan 03) é shell próprio; se o root layout renderiza Footer incondicionalmente, ajustar para que o admin segment não o herde (route group próprio para públicas+cliente OU o admin layout sobrescreve o shell). Documentar a escolha no SUMMARY.
    Criar os 2 testes (@testing-library/react) conforme <behavior>.
  </action>
  <acceptance_criteria>
    - `grep -c 'lang="pt-BR"' app/layout.tsx` == 1
    - `grep -c 'Create Next App' app/layout.tsx` == 0
    - `grep -c 'dpo@docesvalentina.com.br' components/footer.tsx` == 1
    - `grep -c 'Criar minha conta' app/page.tsx` == 1
    - `grep -c 'Painel admin' components/header.tsx` == 1
    - `grep -E -c 'Resend|Cloudflare|Hostinger' app/\(public\)/privacidade/page.tsx` >= 3
    - `npx vitest run tests/lgpd/footer-dpo.test.tsx tests/lgpd/privacy-content.test.tsx` PASS
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/lgpd/footer-dpo.test.tsx tests/lgpd/privacy-content.test.tsx --reporter=dot</automated>
  </verify>
  <done>Layout pt-BR com header/footer/toaster; landing 3-variant; /termos e /privacidade (operadores reais); testes verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Shell visual completo: header com CTAs por sessão, footer com DPO, landing, termos, privacidade — todos com a paleta pink-bordô.</what-built>
  <how-to-verify>
    `npm run dev` e abrir http://localhost:3000:
    1. Como visitante (sem login): hero "Em breve..." + CTAs "Criar minha conta" (accent #9D2D7A) e "Entrar". Footer mostra Termos/Privacidade + dpo@docesvalentina.com.br.
    2. Abrir /termos e /privacidade — conteúdo legível, /privacidade cita Resend, Cloudflare, Hostinger e retenção de 5 anos.
    3. Conferir paleta (fundo creme rosado #FFF6FB, accent vinho #9D2D7A), tipografia (wordmark Fraunces), e que não há dark mode.
    4. Abrir /admin/entrar (mesmo sem auth) — confirmar que o footer global NÃO aparece no segmento admin (D-03).
    5. Mobile (DevTools responsive): touch targets ≥44px, footer em 1 linha.
  </how-to-verify>
  <resume-signal>Digite "visual ok" ou descreva ajustes (cor/copy/layout).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server (RSC) -> Browser | Header lê sessão server-side; só Toaster é client |
| Público -> /privacidade, /termos | Conteúdo estático legal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | Tampering | CSP / scripts | mitigate | Sem inline script sem nonce; componentes RSC; proxy.ts injeta nonce |
| T-01-07-02 | Information Disclosure | components/header.tsx | mitigate | getSession server-side, sem flicker/leak de role |
| T-01-07-03 | Repudiation | app/(public)/privacidade | mitigate | operadores reais + retenção 5 anos (teste regex) |
</threat_model>

<verification>
- `npx vitest run` verde (footer-dpo + privacy-content + sem regressão).
- `npx tsc --noEmit` exit 0.
- Checkpoint humano valida paleta/copy/footer-ausente-no-admin.
</verification>

<success_criteria>
- LGPD-06: DPO visível no footer global.
- LGPD-03: política descritiva com operadores reais + retenção 5 anos.
- LGPD-02: páginas /termos e /privacidade versionadas (v1.0-shell).
- D-01/D-02/D-03/D-04: landing + header + footer corretos por sessão.
</success_criteria>

<output>
Após completar, criar `.planning/phase-1-foundation/01-07-SUMMARY.md` usando @E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md.
</output>
