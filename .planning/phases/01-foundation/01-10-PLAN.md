---
phase: 01-foundation
plan: 10
type: execute
gap_closure: true
wave: 6
depends_on: [07, 08]
autonomous: false
files_modified:
  - app/entrar/page.tsx
  - app/esqueci-minha-senha/page.tsx
  - app/esqueci-minha-senha/enviado/page.tsx
  - app/redefinir-senha/[token]/page.tsx
  - app/(admin-auth)/layout.tsx
  - app/(admin-auth)/admin/entrar/page.tsx
  - app/(admin)/admin/page.tsx
  - app/(admin)/admin/auditoria/page.tsx
  - tests/auth/password-reset.test.ts
  - tests/audit/auditoria-view.test.tsx
  - tests/admin/admin-login-no-loop.test.tsx
requirements: [AUTH-05, AUTH-07, AUTH-08, AUTH-10, AUTH-11]
threat_model:
  - id: T-01-10-01
    severity: high
    category: Information Disclosure
    note: "Enumeração via /entrar e /esqueci-minha-senha"
    mitigation: "AUTH-07: /entrar mostra erro genérico 'Email ou senha não conferem'; /esqueci sempre redireciona para /enviado com a mensagem genérica (a Server Action do Plan 08 não revela existência)"
  - id: T-01-10-02
    severity: high
    category: Tampering
    note: "Reuso de token de reset (replay)"
    mitigation: "AUTH-05: Better Auth resetPassword é single-use (DELETE verification) + expira em 1h + revokeSessionsOnPasswordReset; teste cobre 2º uso falhando"
  - id: T-01-10-03
    severity: medium
    category: Information Disclosure
    note: "Token de reset vazando via Referer ao navegar a partir de /redefinir-senha/[token]"
    mitigation: "Referrer-Policy: strict-origin-when-cross-origin (proxy.ts, Plan 03)"
  - id: T-01-10-04
    severity: medium
    category: Elevation of Privilege
    note: "Cliente acessando /admin/auditoria"
    mitigation: "Guarda 2 camadas para /admin/* PROTEGIDO (page, auditoria): proxy.ts (cookie) + app/(admin)/admin/layout.tsx (role check DB, Plan 03). Exceção INTENCIONAL: /admin/entrar é página pública de login e vive no grupo NÃO-guardado (admin-auth) — não há dado sensível nela e proxy.ts já a isenta da camada de cookie. Isso quebra o loop de redirect (B1) sem expor /admin protegido."

must_haves:
  truths:
    - "Cliente entra em /entrar e mantém sessão (AUTH-08)"
    - "/entrar com credenciais erradas mostra erro genérico (AUTH-07)"
    - "/esqueci-minha-senha sempre leva a /enviado com mensagem genérica (AUTH-07)"
    - "/redefinir-senha/[token] salva nova senha; token é single-use e revoga sessões (AUTH-05)"
    - "GET /admin/entrar SEM sessão renderiza o form de login (200), sem loop de redirect — a página fica fora do layout guardado (B1)"
    - "/admin/entrar permite login admin (AUTH-10); admin_login é auditado (via action do Plan 08)"
    - "/admin/auditoria lista os eventos do audit_log em pt-BR descritivo, desc por data (AUTH-11)"
  artifacts:
    - path: "app/entrar/page.tsx"
      provides: "Login cliente/admin"
      contains: "Esqueci minha senha"
    - path: "app/redefinir-senha/[token]/page.tsx"
      provides: "Form de nova senha"
      contains: "Salvar nova senha"
    - path: "app/(admin-auth)/admin/entrar/page.tsx"
      provides: "Login admin FORA do layout guardado (sem loop)"
      contains: "Entrar como administradora"
    - path: "app/(admin)/admin/auditoria/page.tsx"
      provides: "Viewer do audit log"
      contains: "Quem fez o quê"
  key_links:
    - from: "app/entrar/page.tsx"
      to: "lib/actions/auth.ts"
      via: "signinUser"
      pattern: "signinUser"
    - from: "app/redefinir-senha/[token]/page.tsx"
      to: "lib/actions/auth.ts"
      via: "resetPassword"
      pattern: "resetPassword"
    - from: "app/(admin)/admin/auditoria/page.tsx"
      to: "prisma.auditLog"
      via: "findMany orderBy ts desc"
      pattern: "auditLog.findMany"
---

<objective>
Completar as superfícies de auth restantes: login (cliente e admin), recuperação de senha (esqueci -> enviado -> redefinir) e as páginas do admin shell (entrar, home placeholder, viewer de auditoria). Consome as Server Actions do Plan 08 e a base de audit do Plan 04.

Purpose: Sem estas páginas o login, o reset OWASP e a visualização de auditoria não têm superfície — o admin não consegue entrar nem ver eventos, e clientes não conseguem recuperar senha.
Output: 5 páginas de auth + admin login (fora do layout guardado) + home + viewer de auditoria + 3 testes.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-1-foundation/01-UI-SPEC.md
@.planning/phase-1-foundation/01-RESEARCH.md
@.planning/phase-1-foundation/01-PATTERNS.md

<interfaces>
lib/actions/auth.ts (Plan 08) — signinUser, signinAdmin (grava admin_login), requestPasswordReset (anti-enum), resetPassword (single-use + revoke).

app/(admin)/admin/layout.tsx (Plan 03, NÃO modificar) — role gate: `const session = await auth.api.getSession(...)`; `if (!session) redirect('/admin/entrar')`; `if (session.user.role !== 'admin') -> 403 copy`. Esse layout guarda TUDO em `app/(admin)/admin/*`.

B1 — DECISÃO DE ESTRUTURA (abordagem b, escolhida por ser a de MENOR disrupção ao Plan 03):
  O login admin NÃO pode ficar sob `app/(admin)/admin/` porque o layout acima redireciona para /admin/entrar quando não há sessão -> visitar /admin/entrar sem sessão entraria em LOOP infinito (a camada 1 do proxy.ts isenta /admin/entrar, mas a camada 2 — o layout — não).
  Solução: criar o login admin num GRUPO IRMÃO não-guardado `app/(admin-auth)/` com um layout próprio SEM role-gate. A URL continua `/admin/entrar` (route groups não acrescentam segmento). Resultado: /admin/entrar renderiza sob `app/(admin-auth)/layout.tsx` (sem getSession/redirect) -> 200 sem loop; /admin e /admin/auditoria continuam 100% guardados pelo layout do Plan 03 (intocado).
  ATENÇÃO: NÃO criar `app/(admin)/admin/entrar/page.tsx` — só pode existir UMA página resolvendo para /admin/entrar (senão erro de rota paralela). O entrar vive EXCLUSIVAMENTE em `app/(admin-auth)/admin/entrar/page.tsx`.
  proxy.ts (Plan 03) já isenta /admin/entrar da camada de cookie -> não precisa mudar proxy.ts neste plano.

lib/db/client.ts (Plan 02) — prisma.auditLog.findMany.
date-fns + ptBR (Plan 07) — format de datas pt-BR.
components/ui/* (Plan 07) — button/input/label/form/alert/card/table/skeleton.
Better Auth verifyEmail/resetPassword tokens — single-use; reset 1h; verificação 24h (Plan 04).

NOTA W3 — copy de erro de login: o literal "Email ou senha não conferem" NÃO consta no UI-SPEC (lacuna). É adotado aqui como copy padrão de credencial inválida — AUTH-07-appropriado (não-enumerante, idêntico para email inexistente e senha errada). Preencher essa lacuna é aceitável; NÃO alterar o UI-SPEC. Manter idêntico ao Plan 08 (lib/actions/auth.ts).

Verbatim: RESEARCH §Audit Log UI (lines 1409-1453), §Auth Flows F3/F4/F5 (837-887). Copy literal: UI-SPEC §Copywriting + §Admin copy. PATTERNS §3.3, §3.4, §3.5, §3.12, §3.13, §3.14.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: páginas de login + recuperação de senha (entrar, esqueci, enviado, redefinir)</name>
  <files>app/entrar/page.tsx, app/esqueci-minha-senha/page.tsx, app/esqueci-minha-senha/enviado/page.tsx, app/redefinir-senha/[token]/page.tsx</files>
  <read_first>
    - .planning/phase-1-foundation/01-UI-SPEC.md §Copywriting (login, esqueci, anti-enum confirmation, redefinir success/expired) + §Interaction (autocomplete current-password/new-password) + §Destructive (reset revoga sessão copy)
    - .planning/phase-1-foundation/01-PATTERNS.md §3.3, §3.4, §3.5
    - .planning/phase-1-foundation/01-RESEARCH.md §Auth Flows F3/F5 (837-887)
    - lib/actions/auth.ts (Plan 08) — signinUser, requestPasswordReset, resetPassword
    - node_modules/next/dist/docs — forms.md (useActionState), params async (await params) Next 16
  </read_first>
  <action>
    Criar `app/entrar/page.tsx`: heading "Entrar"; campos email (autocomplete email) + senha (autocomplete current-password); CTA "Entrar" -> `signinUser`; link "Esqueci minha senha" -> /esqueci-minha-senha; link "Ainda não tem conta? Criar agora" -> /cadastro. Erro genérico AUTH-07 "Email ou senha não conferem" (mesmo literal do Plan 08 — NOTA W3).
    Criar `app/esqueci-minha-senha/page.tsx`: heading "Recuperar minha senha"; campo email; CTA "Enviar link de recuperação" -> `requestPasswordReset` -> redireciona para /esqueci-minha-senha/enviado SEMPRE (anti-enum).
    Criar `app/esqueci-minha-senha/enviado/page.tsx`: mensagem genérica literal "Se esse email estiver cadastrado, você vai receber um link em alguns minutos. Verifique também o spam."
    Criar `app/redefinir-senha/[token]/page.tsx` (`await params`): heading "Criar uma senha nova"; campo nova senha (autocomplete new-password); CTA "Salvar nova senha" -> `resetPassword({ token, newPassword })`; sucesso "Pronto! Sua senha foi atualizada. Pode entrar com a nova." + aviso de sessões encerradas; token expirado -> "Esse link expirou. Pede um novo abaixo." + link para /esqueci-minha-senha.
  </action>
  <acceptance_criteria>
    - `grep -c 'Email ou senha não conferem' app/entrar/page.tsx` == 1
    - `grep -c 'Esqueci minha senha' app/entrar/page.tsx` == 1
    - `grep -c 'Se esse email estiver cadastrado' app/esqueci-minha-senha/enviado/page.tsx` == 1
    - `grep -c 'Salvar nova senha' "app/redefinir-senha/[token]/page.tsx"` == 1
    - `grep -c 'autocomplete="new-password"' "app/redefinir-senha/[token]/page.tsx"` == 1
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Login + recuperação completos com copy literal e anti-enum; reset usa a action single-use; type-check passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: admin login fora do layout guardado (grupo admin-auth) + home + auditoria + testes (reset OWASP, viewer, no-loop)</name>
  <files>app/(admin-auth)/layout.tsx, app/(admin-auth)/admin/entrar/page.tsx, app/(admin)/admin/page.tsx, app/(admin)/admin/auditoria/page.tsx, tests/auth/password-reset.test.ts, tests/audit/auditoria-view.test.tsx, tests/admin/admin-login-no-loop.test.tsx</files>
  <read_first>
    - .planning/phase-1-foundation/01-UI-SPEC.md §Admin copy (entrar/auditoria heading/empty/row format) + §Surface Inventory
    - .planning/phase-1-foundation/01-RESEARCH.md §Audit Log UI (lines 1409-1453) — RSC findMany + ACTION_COPY verbatim
    - .planning/phase-1-foundation/01-PATTERNS.md §3.12, §3.13, §3.14
    - app/(admin)/admin/layout.tsx (Plan 03) — guard que NÃO deve envolver /admin/entrar (motivo do grupo irmão; ver interfaces B1)
    - lib/actions/auth.ts (Plan 08) — signinAdmin, resetPassword; lib/db/client.ts; date-fns ptBR (Plan 07)
    - tests/conftest.ts — generateResetToken/createTestUser/signInAsAdmin
    - node_modules/next/dist/docs — route groups + nested layouts (um layout de grupo NÃO renderiza <html>/<body>; o root layout já o faz)
  </read_first>
  <behavior>
    - GET /admin/entrar SEM sessão: a cadeia de layout da página (root + (admin-auth)/layout) NÃO chama getSession nem redirect -> a página renderiza (200), heading "Entrar como administradora" presente. (Prova de B1: sem loop.)
    - resetPassword(token, newPassword) com token válido: troca a senha e DELETA a verification (single-use); um 2º resetPassword com o MESMO token falha (AUTH-05)
    - resetPassword revoga as sessões do usuário (revokeSessionsOnPasswordReset)
    - Render de /admin/auditoria com 0 eventos -> empty state "Nenhum evento ainda" + body literal
    - Render com eventos (seed via logAudit) -> lista desc por ts, cada linha no formato {quando} — {quem} — {ação} com ACTION_COPY pt-BR (ex.: admin_login -> "entrou no painel")
  </behavior>
  <action>
    Criar `app/(admin-auth)/layout.tsx`: layout BARE de grupo (sem <html>/<body> — o root já provê), SEM `auth.api.getSession` e SEM `redirect`. Apenas envolve o conteúdo (ex.: um container centralizado para a tela de login). Comentar que este grupo é deliberadamente NÃO-guardado para a página pública de login admin (B1).
    Criar `app/(admin-auth)/admin/entrar/page.tsx`: heading "Entrar como administradora"; mesmo form de /entrar mas chamando `signinAdmin` (grava admin_login); CTA "Entrar". URL resultante = /admin/entrar.
    Criar `app/(admin)/admin/page.tsx`: home placeholder com copy "Phase 7 traz dashboard" + nav lateral (link "Auditoria" -> /admin/auditoria). (Fica SOB o layout guardado do Plan 03.)
    Criar `app/(admin)/admin/auditoria/page.tsx` (RSC) per RESEARCH 1411-1453: `prisma.auditLog.findMany({ orderBy:{ ts:'desc' }, take:200 })`; mapa ACTION_COPY pt-BR; heading "Quem fez o quê"; empty state literal; linhas `{format(ts,...,{locale:ptBR})} — {actorLabel} — {ACTION_COPY[action] ?? action}`. Renderizar metadata como text-only (sem dangerouslySetInnerHTML) — XSS-safe. (Fica SOB o layout guardado.)
    Criar `tests/auth/password-reset.test.ts` (single-use + revoke, conforme <behavior>) e `tests/audit/auditoria-view.test.tsx` (empty state + lista ordenada com ACTION_COPY).
    Criar `tests/admin/admin-login-no-loop.test.tsx` (B1): mockar `next/navigation` (spy em `redirect`); renderizar `app/(admin-auth)/admin/entrar/page.tsx` envolto por `app/(admin-auth)/layout.tsx` SEM mockar sessão; asserta que `redirect` NÃO foi chamado E que o heading "Entrar como administradora" aparece (prova: a página de login admin não dispara o guard -> sem loop / 200). Opcional: asserir via grep estrutural (acceptance) que (admin-auth)/layout.tsx não importa auth.api.
  </action>
  <acceptance_criteria>
    - `grep -c 'Entrar como administradora' "app/(admin-auth)/admin/entrar/page.tsx"` == 1
    - `grep -c 'signinAdmin' "app/(admin-auth)/admin/entrar/page.tsx"` == 1
    - `grep -c 'getSession' "app/(admin-auth)/layout.tsx"` == 0 (grupo NÃO-guardado — sem loop)
    - `test ! -f "app/(admin)/admin/entrar/page.tsx"` (entrar NÃO existe sob o grupo guardado — sem rota paralela / sem loop)
    - `grep -c 'Quem fez o quê' "app/(admin)/admin/auditoria/page.tsx"` == 1
    - `grep -c 'auditLog.findMany' "app/(admin)/admin/auditoria/page.tsx"` == 1
    - `grep -c 'dangerouslySetInnerHTML' "app/(admin)/admin/auditoria/page.tsx"` == 0
    - `npx vitest run tests/auth/password-reset.test.ts tests/audit/auditoria-view.test.tsx tests/admin/admin-login-no-loop.test.tsx` PASS (incl. no-loop: redirect NÃO chamado em /admin/entrar)
    - `npx vitest run` (suíte completa) PASS — sem regressão
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/auth/password-reset.test.ts tests/audit/auditoria-view.test.tsx tests/admin/admin-login-no-loop.test.tsx --reporter=dot</automated>
  </verify>
  <done>Admin login renderiza sem loop (fora do layout guardado, redirect não chamado); home/auditoria sob o guard; reset single-use+revoke provado; viewer ordena e traduz ações; testes verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Login cliente/admin, recuperação de senha OWASP ponta a ponta, e viewer de auditoria — fechando a superfície de auth da Phase 1.</what-built>
  <how-to-verify>
    `npm run dev` (doces-pg up; admin já criado via `npm run seed:admin`):
    1. /entrar: login cliente confirmado -> mantém sessão (vê CTAs de logado em /). Credenciais erradas -> "Email ou senha não conferem".
    2. /esqueci-minha-senha: enviar qualquer email -> sempre /enviado com a mensagem genérica. Para um email real, abrir o link do email -> /redefinir-senha/[token] -> salvar nova senha -> "Pronto!"; tentar reusar o mesmo link -> "Esse link expirou".
    3. /admin/entrar SEM estar logado: a página de login deve ABRIR normalmente (200), sem loop de redirect (B1). Logar como admin -> /admin (home placeholder).
    4. /admin/auditoria: ver eventos (admin_login, customer_signup, etc.) em pt-BR, mais recentes no topo. Como cliente, /admin/auditoria -> 403.
  </how-to-verify>
  <resume-signal>Digite "auth ok" ou descreva o que falhou (página/copy/fluxo).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /entrar, /esqueci, /redefinir | Entrada não confiável; actions com rate limit + anti-enum (Plan 08) |
| Link de reset -> /redefinir-senha/[token] | Token single-use, 1h |
| Cliente -> /admin/* protegido (page, auditoria) | Bloqueado por guard 2 camadas |
| Qualquer um -> /admin/entrar | Página pública de login (grupo admin-auth, não-guardado); sem dado sensível |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10-01 | Information Disclosure | /entrar, /esqueci | mitigate | Mensagem genérica + redirect /enviado (AUTH-07, block-on:high) |
| T-01-10-02 | Tampering | resetPassword | mitigate | single-use + 1h + revoke (teste cobre 2º uso, block-on:high) |
| T-01-10-03 | Information Disclosure | /redefinir-senha/[token] | mitigate | Referrer-Policy strict-origin (proxy.ts) |
| T-01-10-04 | Elevation of Privilege | /admin/auditoria, /admin (protegidos) | mitigate | guard 2 camadas (proxy + (admin)/layout role); /admin/entrar fica fora do grupo guardado por ser login público (sem dado sensível) -> quebra o loop B1 |
</threat_model>

<verification>
- Pré-requisito: `docker start doces-pg` + `.env` completo + admin seedado.
- `npx vitest run` verde (password-reset + auditoria-view + admin-login-no-loop + sem regressão).
- `npx tsc --noEmit` exit 0.
- Checkpoint humano valida login/reset/auditoria + abertura de /admin/entrar sem loop.
</verification>

<success_criteria>
- AUTH-08: login cliente com sessão.
- AUTH-07: anti-enumeração em login e recuperação.
- AUTH-05: reset OWASP completo (single-use, expira, revoga).
- AUTH-10: login admin via /admin/entrar (abre sem loop — B1 resolvido).
- AUTH-11: /admin/auditoria lista eventos.
</success_criteria>

<output>
Após completar, criar `.planning/phase-1-foundation/01-10-SUMMARY.md` usando @E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md.
</output>
</content>
