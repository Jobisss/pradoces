---
phase: 01-foundation
plan: 06
type: execute
gap_closure: true
wave: 3
depends_on: [04]
autonomous: true
files_modified:
  - package.json
  - lib/queue/boss.ts
  - instrumentation.ts
  - lib/ratelimit/memory.ts
  - proxy.ts
  - tests/instrumentation.test.ts
  - tests/ratelimit.test.ts
  - tests/proxy-auth-ratelimit.test.ts
requirements: [INFRA-11, INFRA-04]
threat_model:
  - id: T-01-06-01
    severity: high
    category: Denial of Service
    note: "Brute force / credential stuffing em /api/auth/* — o handler catch-all app/api/auth/[...all]/route.ts é diretamente alcançável e ficava sem rate limit (bypass das Server Actions)"
    mitigation: "Limite no BOUNDARY: proxy.ts (Node runtime) consome rateLimitAuth.consume(ip) para POST em /api/auth/* nas rotas sensíveis (sign-in/email, forget-password) -> 429 ao estourar (10 req/60s por IP). Defesa em profundidade: Server Actions do Plan 08 também consomem rateLimitAuth (10/60s IP) + rateLimitForgotEmail (3/15min email)."
  - id: T-01-06-02
    severity: low
    category: Denial of Service
    note: "pg-boss boota antes do Postgres pronto"
    mitigation: "instrumentation.register() roda após Prisma client conectado; boss.start() é idempotente; em PROD docker-compose usa depends_on service_healthy (Plan 05)"
  - id: T-01-06-03
    severity: low
    category: Tampering
    note: "Rate limit em memória reseta em restart (bypass momentâneo)"
    mitigation: "Aceito v1 (CONTEXT D / single VPS); migra para RateLimiterPostgres sem Redis quando o volume justificar"

must_haves:
  truths:
    - "instrumentation.register() inicializa pg-boss (boss.start()) uma vez no boot Node"
    - "O 11º request em 60s no limiter de auth é bloqueado (consume rejeita)"
    - "O 4º pedido de reset em 15min para o mesmo email é bloqueado"
    - "O 11º POST direto em /api/auth/sign-in/email (mesmo IP, dentro de 60s) recebe 429 no proxy.ts — o handler catch-all deixa de ser bypass (INFRA-04/SC4)"
    - "pg-boss usa schema dedicado 'pgboss' (não polui public)"
  artifacts:
    - path: "lib/queue/boss.ts"
      provides: "PgBoss singleton"
      contains: "schema: 'pgboss'"
    - path: "instrumentation.ts"
      provides: "Boot hook pg-boss"
      contains: "boss.start()"
    - path: "lib/ratelimit/memory.ts"
      provides: "rateLimitAuth + rateLimitForgotEmail"
      exports: ["rateLimitAuth", "rateLimitForgotEmail"]
  key_links:
    - from: "instrumentation.ts"
      to: "lib/queue/boss.ts"
      via: "dynamic import + boss.start()"
      pattern: "boss.start\\(\\)"
    - from: "lib/ratelimit/memory.ts"
      to: "rate-limiter-flexible"
      via: "RateLimiterMemory"
      pattern: "RateLimiterMemory"
    - from: "proxy.ts"
      to: "lib/ratelimit/memory.ts"
      via: "rateLimitAuth.consume(ip) em POST /api/auth/* sensível -> 429"
      pattern: "rateLimitAuth\\.consume"
---

<objective>
Montar o harness de jobs (pg-boss via instrumentation.ts), as instâncias de rate limit em memória, E aplicar o rate limit no boundary `/api/auth/*` dentro do `proxy.ts`. Fecha INFRA-11 e fecha INFRA-04 de verdade — não basta criar o limiter; o handler catch-all do Better Auth (`app/api/auth/[...all]/route.ts`) é diretamente alcançável e precisa ser throttled na borda (as Server Actions do Plan 08 sozinhas não cobrem o POST direto).

Purpose: INFRA-11 está totalmente ausente (instrumentation.ts não existe, pg-boss não instalado) e, sem o wiring no proxy, `/api/auth/sign-in/email` aceitaria brute-force ilimitado (bypass das Server Actions).
Output: lib/queue/boss.ts, instrumentation.ts, lib/ratelimit/memory.ts, wiring de rate limit em proxy.ts + testes (incl. teste de 429 no POST direto).
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-1-foundation/01-RESEARCH.md
@.planning/phase-1-foundation/01-PATTERNS.md

<interfaces>
lib/env.ts (Plan 01) — env.DATABASE_URL disponível.
lib/db/client.ts (Plan 02) — Prisma já conectado via driver adapter.
proxy.ts (Plan 03) — JÁ existe na raiz, runtime Node em Next 16. Hoje faz: nonce/CSP + cookie-presence guard para /admin/* (exceto /admin/entrar) e /minha-conta/*. O matcher roda em TODAS as rotas exceto assets estáticos -> /api/auth/* JÁ passa pelo proxy. NÃO existe lógica de rate limit nele ainda — Task 3 adiciona.
app/api/auth/[...all]/route.ts (Plan 03) — handler catch-all do Better Auth (`export const { GET, POST } = toNextJsHandler(auth)`). É a rota diretamente alcançável que o Task 3 protege.
RESEARCH override (Pitfall): @upstash/ratelimit NÃO tem modo memory — usar rate-limiter-flexible@11.0.1 RateLimiterMemory.
Verbatim: RESEARCH §pg-boss Boot Harness (lines 1530-1560), §Rate Limit (lines 1180-1209). PATTERNS §2.15, §2.16, §2.19.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rate-limiter-flexible + lib/ratelimit/memory.ts + teste</name>
  <files>package.json, lib/ratelimit/memory.ts, tests/ratelimit.test.ts</files>
  <read_first>
    - .planning/phase-1-foundation/01-RESEARCH.md §Rate Limit (lines 1180-1209) — config verbatim dos 2 limiters
    - .planning/phase-1-foundation/01-PATTERNS.md §2.15 — override Upstash -> rate-limiter-flexible
  </read_first>
  <behavior>
    - rateLimitAuth.consume(ip) 10x seguidas resolve; a 11ª (mesma key, dentro de 60s) rejeita (catch retorna null no padrão da Server Action)
    - rateLimitForgotEmail.consume(email) 3x resolve; a 4ª (mesma key, 15min) rejeita
    - keys distintas não interferem entre si
  </behavior>
  <action>
    Rodar `rtk npm install rate-limiter-flexible@11.0.1`.
    Criar `lib/ratelimit/memory.ts` per RESEARCH 1184-1203: `export const rateLimitAuth = new RateLimiterMemory({ keyPrefix:'auth-ip', points:10, duration:60, blockDuration:60 })` e `export const rateLimitForgotEmail = new RateLimiterMemory({ keyPrefix:'forgot-email', points:3, duration:60*15, blockDuration:60*15 })`.
    Criar `tests/ratelimit.test.ts` conforme <behavior> (loop de consume; a chamada que estoura deve rejeitar — usar try/catch ou `.catch(() => 'BLOCKED')`).
  </action>
  <acceptance_criteria>
    - `grep -c 'rate-limiter-flexible' package.json` == 1
    - `grep -c 'export const rateLimitAuth' lib/ratelimit/memory.ts` == 1
    - `grep -c 'export const rateLimitForgotEmail' lib/ratelimit/memory.ts` == 1
    - `grep -c '@upstash/ratelimit' package.json` == 0 (override respeitado)
    - `npx vitest run tests/ratelimit.test.ts` PASS
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/ratelimit.test.ts --reporter=dot</automated>
  </verify>
  <done>2 limiters exportados; 11ª/4ª request bloqueadas; teste verde; Upstash não usado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/queue/boss.ts + instrumentation.ts + teste de boot</name>
  <files>package.json, lib/queue/boss.ts, instrumentation.ts, tests/instrumentation.test.ts</files>
  <read_first>
    - .planning/phase-1-foundation/01-RESEARCH.md §pg-boss Boot Harness (lines 1530-1560) — código verbatim de boss.ts + instrumentation.ts
    - .planning/phase-1-foundation/01-PATTERNS.md §2.16, §2.19
    - node_modules/next/dist/docs/01-app/02-guides/instrumentation.md — assinatura register() em Next 16
  </read_first>
  <behavior>
    - register() retorna cedo (sem chamar boss.start) quando process.env.NEXT_RUNTIME !== 'nodejs'
    - register() com NEXT_RUNTIME='nodejs' chama boss.start() exatamente 1x (teste mocka o módulo ./lib/queue/boss)
    - Phase 1: ZERO workers de domínio registrados (apenas prova que liga)
  </behavior>
  <action>
    Rodar `rtk npm install pg-boss@12.18.1`.
    Criar `lib/queue/boss.ts` per RESEARCH 1532-1544: singleton `globalForBoss` + `new PgBoss({ connectionString: env.DATABASE_URL, schema: 'pgboss' })`; reatribui global só fora de production.
    Criar `instrumentation.ts` na RAIZ per RESEARCH 1547-1556: `export async function register() { if (process.env.NEXT_RUNTIME !== 'nodejs') return; const { boss } = await import('./lib/queue/boss'); await boss.start() }` com comentário de que Phase 4 adiciona boss.work(...).
    Criar `tests/instrumentation.test.ts` conforme <behavior>: usar `vi.mock('../lib/queue/boss', ...)` com um spy de start; setar/limpar process.env.NEXT_RUNTIME nos dois casos.
  </action>
  <acceptance_criteria>
    - `grep -c 'pg-boss' package.json` == 1
    - `grep -c "schema: 'pgboss'" lib/queue/boss.ts` == 1
    - `grep -c "NEXT_RUNTIME !== 'nodejs'" instrumentation.ts` == 1
    - `grep -c 'boss.start()' instrumentation.ts` == 1
    - `npx vitest run tests/instrumentation.test.ts` PASS
    - `npx vitest run` (suíte completa) PASS — sem regressão
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --reporter=dot</automated>
  </verify>
  <done>instrumentation chama boss.start() 1x só no runtime Node; schema pgboss dedicado; suíte verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire rateLimitAuth no proxy.ts (boundary /api/auth/*) + teste de 429 no POST direto</name>
  <files>proxy.ts, tests/proxy-auth-ratelimit.test.ts</files>
  <read_first>
    - proxy.ts — estrutura atual: `export default async function proxy(request: NextRequest)`, deriva `const { pathname } = request.nextUrl`, monta CSP e retorna `NextResponse.next(...)`. NÃO remover/alterar a lógica de nonce/CSP/cookie-guard existente.
    - lib/ratelimit/memory.ts (Task 1) — `rateLimitAuth.consume(key)` rejeita ao estourar.
    - app/api/auth/[...all]/route.ts — o handler catch-all que ficava sem throttle.
    - .planning/phase-1-foundation/01-UI-SPEC.md — copy de rate limit ("Muitas tentativas seguidas...")
  </read_first>
  <behavior>
    - 10 POSTs em /api/auth/sign-in/email do mesmo IP (x-forwarded-for) em 60s passam (proxy segue para NextResponse.next)
    - o 11º POST do mesmo IP retorna NextResponse status 429 (corpo JSON com a copy de rate limit), SEM chegar ao handler
    - GET em /api/auth/* NÃO é throttled (só métodos sensíveis de escrita); rotas não-sensíveis de /api/auth/* (ex.: get-session) não são throttladas
    - rotas fora de /api/auth/* continuam intocadas (nonce/CSP/cookie-guard preservados)
  </behavior>
  <action>
    Editar `proxy.ts`: importar `import { rateLimitAuth } from '@/lib/ratelimit/memory'` no topo.
    Logo após derivar `const { pathname } = request.nextUrl`, adicionar um bloco ANTES do retorno de NextResponse.next:
      `if (request.method === 'POST' && pathname.startsWith('/api/auth')) {`
      `  const sensitive = pathname.includes('/sign-in/email') || pathname.includes('/forget-password')`
      `  if (sensitive) {`
      `    const ip = (request.headers.get('x-forwarded-for') ?? '').split(',')[0].trim() || 'unknown'`
      `    try { await rateLimitAuth.consume(ip) }`
      `    catch { return new NextResponse(JSON.stringify({ message: 'Muitas tentativas seguidas. Tenta de novo daqui a pouco.' }), { status: 429, headers: { 'content-type': 'application/json' } }) }`
      `  }`
      `}`
    Manter TODO o resto (nonce/CSP/headers/cookie-guard) inalterado. O proxy roda Node runtime, então o RateLimiterMemory em processo é compartilhado com as Server Actions (defesa em profundidade — Plan 08 mantém o seu consume).
    Criar `tests/proxy-auth-ratelimit.test.ts`: importar `proxy` de '../proxy'; construir um `new NextRequest('http://localhost/api/auth/sign-in/email', { method:'POST', headers:{ 'x-forwarded-for':'9.9.9.9' } })`; chamar `proxy(req)` 10x (asserta que NÃO é 429) e na 11ª asserta `res.status === 429`. Usar um IP distinto para provar isolamento de key. (Importar rateLimitAuth e resetar/usar key fresca se necessário.)
  </action>
  <acceptance_criteria>
    - `grep -c "rateLimitAuth.consume" proxy.ts` == 1
    - `grep -c "/api/auth" proxy.ts` >= 1
    - `grep -c "status: 429" proxy.ts` == 1
    - `grep -v '^//' proxy.ts | grep -c "getSessionCookie" ` >= 1 (cookie-guard preservado)
    - `grep -c "Content-Security-Policy" proxy.ts` == 1 (CSP preservado)
    - `npx vitest run tests/proxy-auth-ratelimit.test.ts` PASS (11º POST -> 429)
    - `npx vitest run` (suíte completa) PASS — sem regressão
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/proxy-auth-ratelimit.test.ts --reporter=dot</automated>
  </verify>
  <done>Proxy throttla POST sensível em /api/auth/* (11º -> 429); handler catch-all não é mais bypass; CSP/cookie-guard preservados; teste verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente -> /api/auth/* (handler catch-all) | Entrada não confiável; rate limit aplicado no proxy.ts (boundary) para POST sensível |
| Cliente -> Server Action de auth | Mesmo limiter consumido (defesa em profundidade, Plan 08) |
| App boot -> Postgres | pg-boss cria schema pgboss.* na primeira boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Denial of Service | proxy.ts + lib/ratelimit/memory.ts + app/api/auth/[...all]/route.ts | mitigate | rateLimitAuth.consume(ip) no proxy para POST /api/auth/{sign-in/email,forget-password} -> 429 (10/60s); handler catch-all coberto; Server Actions (Plan 08) consomem o mesmo limiter como defesa em profundidade (block-on:high) |
| T-01-06-02 | Denial of Service | instrumentation.ts | accept | boss.start() idempotente; compose depends_on service_healthy (Plan 05) |
| T-01-06-03 | Tampering | rate limit em memória | accept | v1 single-VPS; migra RateLimiterPostgres depois (sem Redis) |
</threat_model>

<verification>
- Pré-requisito: `docker start doces-pg` e `.env` com DATABASE_URL.
- `npx vitest run` verde (instrumentation + ratelimit + proxy-auth-ratelimit + sem regressão).
- grep gates confirmam pgboss schema, guard de NEXT_RUNTIME, override de Upstash e o wiring de rate limit no proxy (429).
</verification>

<success_criteria>
- INFRA-11: instrumentation.ts boota pg-boss no runtime Node, 1x, idempotente.
- INFRA-04 / SC4: rate limit ATIVO no boundary /api/auth/* (proxy.ts) — POST direto ao handler catch-all é throttled (429); Server Actions do Plan 08 mantêm o limite como defesa em profundidade.
</success_criteria>

<output>
Após completar, criar `.planning/phase-1-foundation/01-06-SUMMARY.md` usando @E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md.
</output>
</content>
