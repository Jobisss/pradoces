---
phase: 01-foundation
plan: 11
type: execute
gap_closure: true
wave: 5
depends_on: [03, 04, 07]
autonomous: false
files_modified:
  - lib/validation/lgpd.ts
  - lib/lgpd/export.ts
  - lib/lgpd/anonymize.ts
  - app/api/me/export/route.ts
  - app/api/me/delete/route.ts
  - app/minha-conta/meus-dados/page.tsx
  - app/minha-conta/excluir/page.tsx
  - tests/lgpd/export.test.ts
  - tests/lgpd/anonymize.test.ts
requirements: [LGPD-04, LGPD-05, AUTH-09]
threat_model:
  - id: T-01-11-01
    severity: high
    category: Information Disclosure
    note: "Usuário exportando/excluindo dados de OUTRO usuário (IDOR)"
    mitigation: "Export e delete usam SEMPRE session.user.id de auth.api.getSession (nunca id vindo do request); 401 sem sessão; delete exige typed-email == session.user.email"
  - id: T-01-11-02
    severity: high
    category: Tampering
    note: "Exclusão por DELETE quebra histórico fiscal (retenção 5 anos)"
    mitigation: "LGPD-05: anonimização por UPDATE (placeholder email/name, telefone null, deletedAt/anonymizedAt) preservando o ID e o histórico; nunca DELETE do user"
  - id: T-01-11-03
    severity: medium
    category: Spoofing
    note: "Sessão antiga continua válida após exclusão"
    mitigation: "anonymizeUser deleta accounts + sessions na mesma TX (Pitfall #8)"
  - id: T-01-11-04
    severity: medium
    category: Information Disclosure
    note: "Export vaza campos internos (password hash, tokens)"
    mitigation: "exportUserData usa select whitelist explícito (nunca select:undefined); Cache-Control: no-store no download"

must_haves:
  truths:
    - "Cliente logado vê /minha-conta/meus-dados com botão 'Baixar meus dados em JSON' (LGPD-04/AUTH-09)"
    - "GET /api/me/export retorna JSON (cadastro) só do próprio usuário, como download attachment"
    - "Excluir conta anonimiza (placeholder email/name, telefone null, deletedAt/anonymizedAt) preservando o ID"
    - "Exclusão revoga todas as sessões e apaga credenciais (não dá pra logar depois)"
    - "Exclusão grava customer_account_deleted em audit_log"
    - "Excluir exige digitar o email completo (typed-email gate, sem dialog)"
  artifacts:
    - path: "lib/lgpd/export.ts"
      provides: "exportUserData(userId) com select whitelist"
      exports: ["exportUserData"]
    - path: "lib/lgpd/anonymize.ts"
      provides: "anonymizeUser(userId,...) TX + revoke + audit"
      exports: ["anonymizeUser"]
    - path: "app/api/me/export/route.ts"
      provides: "GET download JSON"
      exports: ["GET"]
    - path: "app/api/me/delete/route.ts"
      provides: "POST anonimização typed-email"
      exports: ["POST"]
  key_links:
    - from: "app/api/me/export/route.ts"
      to: "lib/lgpd/export.ts"
      via: "exportUserData(session.user.id)"
      pattern: "exportUserData"
    - from: "app/api/me/delete/route.ts"
      to: "lib/lgpd/anonymize.ts"
      via: "anonymizeUser(session.user.id, ...)"
      pattern: "anonymizeUser"
    - from: "lib/lgpd/anonymize.ts"
      to: "lib/audit/log.ts"
      via: "logAudit customer_account_deleted"
      pattern: "customer_account_deleted"
---

<objective>
Implementar o baseline LGPD funcional: página "Meus dados" (AUTH-09 shell) com export JSON (LGPD-04) e a página/fluxo de exclusão por anonimização (LGPD-05) que preserva histórico fiscal e revoga sessões. Hoje só existem as colunas no schema — nenhuma UI/rota/serviço.

Purpose: LGPD baseline funcional inexistente é um bloqueador legal explícito da Phase 1.
Output: lib/lgpd/export.ts + anonymize.ts + validação + rotas /api/me/export e /api/me/delete + páginas meus-dados/excluir + testes.
</objective>

<execution_context>
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/workflows/execute-plan.md
@E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-1-foundation/01-UI-SPEC.md
@.planning/phase-1-foundation/01-RESEARCH.md
@.planning/phase-1-foundation/01-PATTERNS.md

<interfaces>
app/minha-conta/layout.tsx (Plan 03) — role gate: sem sessão -> /entrar; role=admin -> /admin (D-06). As páginas meus-dados/excluir ficam SOB esse layout.
lib/auth/server.ts (Plan 03/04) — auth.api.getSession({ headers: await nextHeaders() }).
lib/db/client.ts (Plan 02) — prisma; User tem deletedAt/anonymizedAt; relations sessions/accounts onDelete Cascade.
lib/audit/log.ts (Plan 04) — logAudit.
components/ui/* (Plan 07) — card/button/input/label/alert.
Verbatim: RESEARCH §LGPD-04 (lines 1263-1308), §LGPD-05 (1310-1378). Copy literal: UI-SPEC §LGPD copy. PATTERNS §2.13, §2.14, §2.22, §2.23, §3.9, §3.10.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/lgpd (export + anonymize) + validação + rotas /api/me/export e /api/me/delete + testes</name>
  <files>lib/validation/lgpd.ts, lib/lgpd/export.ts, lib/lgpd/anonymize.ts, app/api/me/export/route.ts, app/api/me/delete/route.ts, tests/lgpd/export.test.ts, tests/lgpd/anonymize.test.ts</files>
  <read_first>
    - .planning/phase-1-foundation/01-RESEARCH.md §LGPD-04 (1263-1308) + §LGPD-05 (1310-1378) — código verbatim de export/anonymize/rotas
    - .planning/phase-1-foundation/01-PATTERNS.md §2.13, §2.14, §2.22, §2.23
    - prisma/schema.prisma — campos deletedAt/anonymizedAt + cascades
    - lib/auth/server.ts, lib/audit/log.ts
    - tests/conftest.ts — createTestUser/truncateAll/signInAsCustomer
  </read_first>
  <behavior>
    - exportUserData(userId) retorna { versao_export, gerado_em, cadastro:{...whitelist}, reservas:[], pontos:[] } — sem password/token (select whitelist)
    - GET /api/me/export sem sessão -> 401; com sessão -> 200 com Content-Disposition attachment + Cache-Control no-store; payload é do session.user.id
    - anonymizeUser(userId): após rodar, user.email vira placeholder (@anon.invalid), name='[anonimizado]', telefone=null, deletedAt/anonymizedAt set; accounts e sessions do user apagados; logAudit customer_account_deleted gravado
    - POST /api/me/delete sem sessão -> 401; com typedEmail != session.user.email -> 400; com typedEmail correto -> anonimiza e redireciona
    - Após anonimização, o MESMO email original pode ser recadastrado (checkEmailExists do Plan 08 trata deletedAt como inexistente)
  </behavior>
  <action>
    Criar `lib/validation/lgpd.ts`: `DeleteAccountSchema` (confirmacao: string) — a igualdade com o email da sessão é checada na rota.
    Criar `lib/lgpd/export.ts` per RESEARCH 1287-1308: `exportUserData(userId)` com `prisma.user.findUnique({ where:{ id }, select:{ id,email,name,telefone,isAdult,termsVersion,termsAcceptedAt,privacyVersion,privacyAcceptedAt,emailVerified,createdAt,updatedAt } })` e retorno { versao_export:'1.0', gerado_em, cadastro:user, reservas:[], pontos:[] }. NUNCA select:undefined.
    Criar `lib/lgpd/anonymize.ts` per RESEARCH 1312-1349: `anonymizeUser(userId, ipHash?, uaHash?)` -> `prisma.$transaction` (update placeholder email `deleted-${crypto.randomUUID()}@anon.invalid`, name '[anonimizado]', telefone null, deletedAt/anonymizedAt; deleteMany accounts; deleteMany sessions) + `logAudit({ actorType:'customer', actorId:userId, action:'customer_account_deleted', metadata:{ reason:'self-service-lgpd' }, ipHash, uaHash })` FORA da TX. SEMPRE incluir o deleteMany sessions (Pitfall #8).
    Criar `app/api/me/export/route.ts` per RESEARCH 1265-1284: GET com getSession (401 se ausente) -> exportUserData(session.user.id) -> Response JSON com Content-Disposition attachment + Cache-Control no-store.
    Criar `app/api/me/delete/route.ts` per RESEARCH 1352-1377: POST com getSession (401), formData confirmacao; 400 se != session.user.email; computa ipHash/uaHash (sha256 de x-forwarded-for/user-agent); anonymizeUser(session.user.id, ipHash, uaHash); redirect('/?msg=conta-excluida').
    Criar os 2 testes (export + anonymize) cobrindo o <behavior>, incluindo o caso de IDOR (export sempre do próprio id) e a checagem de que sessions/accounts são apagados.
  </action>
  <acceptance_criteria>
    - `grep -c 'export async function exportUserData' lib/lgpd/export.ts` == 1
    - `grep -c 'select:' lib/lgpd/export.ts` >= 1 && `grep -c 'password' lib/lgpd/export.ts` == 0
    - `grep -c 'export async function anonymizeUser' lib/lgpd/anonymize.ts` == 1
    - `grep -c 'session.deleteMany' lib/lgpd/anonymize.ts` == 1
    - `grep -c 'customer_account_deleted' lib/lgpd/anonymize.ts` == 1
    - `grep -c 'session.user.id' app/api/me/export/route.ts` == 1
    - `grep -c 'session.user.email' app/api/me/delete/route.ts` == 1
    - `npx vitest run tests/lgpd/export.test.ts tests/lgpd/anonymize.test.ts` PASS
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/lgpd/export.test.ts tests/lgpd/anonymize.test.ts --reporter=dot</automated>
  </verify>
  <done>Export whitelist (próprio id, 401 sem sessão); anonimização por UPDATE preserva ID + revoga sessões + audita; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: páginas /minha-conta/meus-dados e /minha-conta/excluir (typed-email gate)</name>
  <files>app/minha-conta/meus-dados/page.tsx, app/minha-conta/excluir/page.tsx</files>
  <read_first>
    - .planning/phase-1-foundation/01-UI-SPEC.md §LGPD copy (meus-dados heading/body/CTA; excluir heading/body/gate/CTA destructive; success toasts) + §Destructive actions inventory
    - .planning/phase-1-foundation/01-PATTERNS.md §3.9, §3.10
    - app/minha-conta/layout.tsx (Plan 03) — role gate já aplicado
    - app/api/me/export/route.ts + app/api/me/delete/route.ts (Task 1) + components/ui/*
  </read_first>
  <action>
    Criar `app/minha-conta/meus-dados/page.tsx` (RSC, AUTH-09 shell): heading "Seus dados, do seu jeito"; body intro literal UI-SPEC (retenção 5 anos); CTA primary "Baixar meus dados em JSON" via `<a href="/api/me/export">` (download); link "Excluir minha conta" (foreground+underline, NÃO destrutivo) -> /minha-conta/excluir; mostrar email do DPO. Toast de sucesso "Pronto! Seu arquivo está sendo baixado." (sonner) opcional.
    Criar `app/minha-conta/excluir/page.tsx`: heading "Excluir minha conta"; body literal UI-SPEC (anonimização, 5 anos, pode recadastrar); banner destrutivo "Essa ação não pode ser desfeita."; gate label "Para confirmar, digite seu email completo abaixo." (input typed-email — NÃO usar dialog "tem certeza?"); `<form method="post" action="/api/me/delete">` com input name="confirmacao"; CTA destrutivo "Excluir minha conta" (--color-destructive). Após sucesso o route redireciona para / com msg.
  </action>
  <acceptance_criteria>
    - `grep -c 'Seus dados, do seu jeito' app/minha-conta/meus-dados/page.tsx` == 1
    - `grep -c '/api/me/export' app/minha-conta/meus-dados/page.tsx` == 1
    - `grep -c 'Para confirmar, digite seu email completo' app/minha-conta/excluir/page.tsx` == 1
    - `grep -c '/api/me/delete' app/minha-conta/excluir/page.tsx` == 1
    - `grep -c 'name="confirmacao"' app/minha-conta/excluir/page.tsx` == 1
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Meus-dados com export + link excluir; excluir com typed-email gate (sem dialog) postando para a rota; type-check passa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Baseline LGPD funcional: export JSON do próprio usuário e exclusão por anonimização com gate de email digitado.</what-built>
  <how-to-verify>
    `npm run dev` (logado como cliente):
    1. /minha-conta/meus-dados -> clicar "Baixar meus dados em JSON" -> baixa um .json com seu cadastro (sem password/token).
    2. /minha-conta/excluir -> tentar excluir digitando um email errado -> bloqueia (400). Digitar o email correto -> redireciona para / com mensagem; tentar logar de novo -> falha (conta anonimizada/sessão revogada).
    3. Recadastrar com o MESMO email -> permitido (deletedAt tratado como inexistente).
    4. Em /admin/auditoria confirmar o evento customer_account_deleted.
  </how-to-verify>
  <resume-signal>Digite "lgpd ok" ou descreva o que falhou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente -> /api/me/export, /api/me/delete | Só age sobre session.user.id (sem IDOR) |
| App -> Postgres (anonimização) | UPDATE preserva histórico fiscal; sessions/accounts apagados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11-01 | Information Disclosure | rotas /api/me/* | mitigate | session.user.id sempre; 401/400 (block-on:high) |
| T-01-11-02 | Tampering | lib/lgpd/anonymize.ts | mitigate | UPDATE (não DELETE) preserva ID/histórico (block-on:high) |
| T-01-11-03 | Spoofing | anonymize TX | mitigate | deleteMany sessions+accounts na TX |
| T-01-11-04 | Information Disclosure | lib/lgpd/export.ts | mitigate | select whitelist + no-store |
</threat_model>

<verification>
- Pré-requisito: `docker start doces-pg` + `.env` completo.
- `npx vitest run` verde (export + anonymize + sem regressão).
- `npx tsc --noEmit` exit 0.
- Checkpoint humano valida export/exclusão/recadastro.
</verification>

<success_criteria>
- LGPD-04: "Meus dados" + export JSON do próprio usuário.
- LGPD-05: exclusão por anonimização (preserva fiscal, revoga sessões, audita).
- AUTH-09: cliente acessa painel próprio (shell meus-dados).
</success_criteria>

<output>
Após completar, criar `.planning/phase-1-foundation/01-11-SUMMARY.md` usando @E:/BRCONNECT/doces_mae/.claude/get-shit-done/templates/summary.md.
</output>
